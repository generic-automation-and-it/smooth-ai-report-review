{
  "id": "DR-015-gha-workflow-call-context",
  "kind": "must-not-flag",
  "fixture_note": "Rebuilt minimal after failing FOUR paid runs on four distinct, all-correct findings that had nothing to do with DR-015: a real script-injection vector, a dead push:tags trigger, a workflow_dispatch path that resolved an empty PR number, and a local action used without a checkout. Each repair added or moved surface and the next run found the next thing. What remains is only the DR-015 bait — a workflow_call-able workflow whose job if: gates on a caller event name and which reads github.event.pull_request.*. No actions, no outputs, no secrets, no second trigger path. Do not add scaffolding here.",
  "label": "DR-015",
  "note": "Reusable workflows inherit the caller's github context — event_name is never 'workflow_call' and github.event.pull_request.* is populated for PR-triggered callers; reviewer must NOT flag if-gates or event-payload references as broken for workflow_call.",
  "forbidden_claim": "(event_name|workflow_call|github\\.event\\.pull_request).{0,90}(never|not populated|empty|unavailable|will not|incorrect|wrong)"
}
