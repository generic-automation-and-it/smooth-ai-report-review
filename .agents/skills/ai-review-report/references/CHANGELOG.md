# OpenCode CLI Code Review — Changelog

> Historical record for the `ai-review-report` skill. Load only when updating the skill or auditing past decisions; not needed for routine skill execution.
>
> **Imported history:** entries dated before 2026-06-01 predate this polyrepo — they describe the skill's life inside its origin monorepo and reference legacy names/paths that **do not exist here**, preserved verbatim as an audit trail (not as current locations). In particular: the `.ai/…` prefix is dead — this repo uses `.agents/…`; and the old `manual-gemini-cli-code-review.yml` workflow / `gemini-code-review` / `open-code-review-report` skill dirs are now the `pipeline-code-review-report` gate under `.agents/skills/ai-review-report/`.

| Date | Change | Ref |
|:-----|:-------|:----|
| 2026-06-09 | **Security hardening from the bunker-procurement install review (PR #5419).** The gate reviewing its own copy in a consuming repo flagged issues; applied the ones genuinely valid for the canonical source. (1) **Review agent** (`opencode.json`): disabled `bash` (`tools.bash:false` + `permission.bash:deny`) so a prompt-injected PR diff cannot run arbitrary commands on the runner; kept `webfetch`/`websearch` enabled (intentional — the model verifies references during review) and updated **LADR-029** + the agent `description` to match. This closed a real config↔doc drift: the shipped config had `bash`/`webfetch`/`websearch` enabled while LADR-029's text claimed all three disabled (the gate flagged it Critical). (2) **`resolve-provider.sh` + workflow inline resolver**: stopped writing `OPENCODE_GATEWAY_API_KEY` to `$GITHUB_ENV` — nothing downstream reads it (opencode.json substitutes the provider-specific `{env:OPENCODE_*_API_KEY}`; the presence check uses the in-process value), so the secret no longer persists to every later workflow step. (3) **Workflow**: SHA-pinned `actions/cache/restore` + `save` to `5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3` (were mutable `@v4`; `checkout` + the eval workflow were already pinned). (4) **`copilot-review.sh`**: added `--paginate` to the Copilot-detection `gh api` calls and switched `-F`→`-f` for the GraphQL `owner`/`repo` String args. **Not applied** (bunker-copy artifacts or false positives against this repo's conventions): blanket `set -euo pipefail` (baseline here is `set -e`; `resolve-provider.sh` is intentionally `set`-less because it is `source`d), the static-`baseURL` "drift" (we inject at runtime — LADR-034), stale `DR-010`/`-v2` filename refs (already removed in the gate rename), and the `applyTo` frontmatter (required here for Copilot). Docs: SKILL.md + skill AGENTS.md LADR-029 Decision + transport Key Behavior. | NO-TICKET |
| 2026-06-09 | **Gemini-via-proxy fix — inject per-provider gateway `baseURL` at install time (LADR-034).** Symptom: installing this gate into a consuming repo with `OPENCODE_REVIEW_REPORT_GEMINI_URL=https://litellm.private.prod.zeronorth.app/` + `OPENCODE_GEMINI_API_KEY` holding a LiteLLM proxy key failed every review with `Primary review 'gemini-2.5-pro' not available … API key error` → all-models-failed REQUEST_CHANGES (failing run 27186819544; the `.ai` prototype run 27186819521 worked). Root cause: the 2026-06-06 BNKI-001 change removed all `baseURL` placeholders from `assets/opencode.json`, but `lib/resolve-provider.sh` + the workflow still read + presence-checked the per-provider URL var and exported it at job scope — **nothing fed it into `opencode.json`**, so `@ai-sdk/google` hit Google's native endpoint and rejected the proxy key. `/global/health` (LADR-028) does not validate the key, so it passed; the misroute surfaced only at the real model call. Fix: `lib/setup-opencode-config.sh` now injects each env-driven provider's `options.baseURL` into the installed `~/.config/opencode/opencode.json` from `OPENCODE_REVIEW_REPORT_<P>_URL` when non-empty (`gemini`/`github-copilot`/`openai`; the two OpenCode Go providers keep their hardcoded Zen base, LADR-027). Dynamic, **not** a static `{env:}` placeholder — an unset var would yield an empty-string `baseURL`; empty/unset → native SDK base. Injection only touches a config we manage (CI / local first-run / already-ours), never a hand-rolled personal config. Widened the `is_ours` self-heal `baseURL` clause to accept any `http(s)://` URL so an injected literal still reads as managed (apiKey `{env:OPENCODE_*}` stays the strong discriminator); idempotent + jq-absent-safe. No `opencode.json` edit and no Secret/Variable rename needed — the existing URL Variable + proxy API key now route through the proxy, and clearing the Variable reverts to native on the next run. Verified in sandbox: gemini-only / multi-provider / CI-mode injection all correct, go-* untouched, a repeat run is recognized-as-ours (self-heal intact), and an unset URL drops the `baseURL`. Docs: SKILL.md **LADR-034** + transport Key Behavior, DR-009 reworded (added "absent `baseURL` is not a bug" guard), root CLAUDE.md changelog. | NO-TICKET |
| 2026-06-07 | **Eval harness triage archive + fixture hygiene (follow-up to LADR-033).** First real `llm-eval-harness.yml` run (OPENCODE-GO-OPENAI / deepseek-v4-pro) failed precision 8/14 (recall 6/6). Triage found two of the six must-not-flag FAILs were **fixture defects**, not model regressions: (1) **DR-001** set get-only auto-props (`{ get; }`) via an object initializer in `Create()` — that does not compile (CS0200), so a reviewer flagging it was *correct* yet scored as a DR-001 re-raise; changed the props to `{ get; init; }`. (2) **DR-006** was the only must-not-flag fixture with **no inline "do NOT flag" steering comment** (all others have one), so the model had no in-file cue and full-SHA action-pinning — a legitimate hardening suggestion the repo itself follows — was indistinguishable from the DR-006 "@v4 is invalid" hallucination the scorer targets; added a steering comment (kept `@v4` to preserve the exact hallucination coverage rather than convert to SHA). The other four FAILs (DR-003/007/012/013) are clean fixtures = genuine model precision regressions (the eval working as intended). Also added a **triage archive**: `run-evals.sh` copies each fixture's review to `$EVAL_ARTIFACT_DIR/<id>.review.md` (+ infra-fail `.lastlog`) when set — the sandbox/`WORK_ROOT` are wiped on exit, so without this a precision FAIL left no record of WHAT was flagged; the workflow sets `EVAL_ARTIFACT_DIR=ci_temp/eval-artifacts` and uploads it via `actions/upload-artifact@ea165f8 # v4` with `if: always()`. Documented in code + SKILL.md LADR-033 that the eval's Crit/High/**Med** precision bar is intentionally stricter than the gate's Crit/High blocking. Self-test 17/17 green; archiving is gated on `EVAL_ARTIFACT_DIR` + skipped under `EVAL_SELFTEST`, so the default path is unchanged. **Also added a post-merge canary trigger**: `llm-eval-harness.yml` now runs on `push` to `main` **path-filtered** to the review-pipeline files that change the eval outcome (`.agents/skills/ai-review-report/**`, `.github/instructions/code-review-standards.instructions.md`, the harness workflow itself), in addition to `workflow_dispatch`. It's a post-merge alert (cannot block a merge), still **never `pull_request`**, and the path filter keeps it from making paid calls on PRs that don't touch the reviewer (the eval scores the model against a fixed corpus, so arbitrary PR content can't change the result). LADR-033 + the eval Key Behavior updated. | NO-TICKET |
| 2026-06-07 | **Added a `model_preset` dispatch dropdown + `minimax-m3` model.** (1) `assets/opencode.json`: updated the `go-anthropic` provider (`OpenCode Go (Anthropic-compatible)`) models map — added `minimax-m3` ("MiniMax M3") and `qwen3.6-pro` ("Qwen3.6 Pro"), removed `qwen3.6-plus`; map is now `minimax-m3`/`minimax-m2.7`/`qwen3.7-plus`/`qwen3.6-pro`. (2) `pipline-code-review-report.yml`: added a `workflow_dispatch` `model_preset` `choice` input (default `(repository default)` = no override) with five model presets — *OpenAI GPT-5.5*, *OpenCode DeepSeek V4 Pro*, *OpenCode GLM-5.1*, *OpenCode MiniMax m3*, *OpenCode Qwen3.7 Plus*. A chosen preset is resolved entirely in the job `env:` block (folded `>-` expressions, first `\|\|` term so it wins over the free-text `model` input and the `OPENCODE_REVIEW_REPORT_*` Variables): it sets `OPENCODE_REVIEW_REPORT_PROVIDER` (`OPENAI` / `OPENCODE-GO-OPENAI` / `OPENCODE-GO-ANTHROPIC`) + `_PROVIDER_ID` (`openai` / `go-openai` / `go-anthropic`) and pins **all three** model tiers (`_MODEL_PRIMARY`/`_SECONDARY`/`_ORCHESTRATOR`) to the one selected model id (`gpt-5.5` / `deepseek-v4-pro` / `glm-5.1` / `minimax-m3` / `qwen3.7-plus`). No new step — `resolve-provider.sh` + all consumers read the resulting job-scope env vars, and its model-family fail-fast accepts every preset id. The preset still needs the matching provider's API-key Secret. Workflow↔option coupling: adding/renaming an option requires editing both the `options` list and the five `env:` expressions in the same commit. Docs: README (go-anthropic model row + one-click-switch callout), SKILL.md (env table go-anthropic + MODEL_PRIMARY rows, LADR-027 `go-anthropic` model list, new Key Behavior), root CLAUDE.md changelog. | NO-TICKET |
| 2026-06-07 | **Eval harness env-var rename — complete the `OPENCODE_REVIEW_REPORT_*` namespacing for `scripts/eval/` (follow-up to #6/LADR-032).** The eval harness (LADR-033) landed on a branch in parallel with the #6 rename, so it kept the legacy `OPENCODE_PROVIDER` / `OPENCODE_MODEL_PRIMARY_REVIEW` / `_SECONDARY_REVIEW` / `_ORCHESTRATOR` / `OPENCODE_<P>_URL` / `OPENCODE_CLI_VERSION` names that #6 retired everywhere else — so after the merge the evals (a) never read the gate's designed-model **Variables** and (b) tripped `lib/resolve-provider.sh`'s fail-fast (`OPENCODE_REVIEW_REPORT_MODEL_PRIMARY unset`). Renamed all of them in `eval/run-evals.sh`, `eval/local-evals.sh`, `eval/README.md`, and `.github/workflows/llm-eval-harness.yml` so the eval sources the **same designed-model Variables + `OPENCODE_<P>_API_KEY` Secrets** the review gate uses. Also dropped the hardcoded `gemini-2.5-pro` / `gemini-3-flash-preview` secondary/orchestrator literals in the scripts: they now default to the designed **PRIMARY** model, so a non-GEMINI provider's chain stays same-family and the resolver no longer aborts (an explicit `_SECONDARY` / `_ORCHESTRATOR` Variable still wins; the CI workflow keeps the gate's `\|\| 'gemini-…'` parity fallbacks). API-key Secret names unchanged. Verified: 15/15 structural self-test (`test-evals.sh`) green; no legacy names remain in `eval/` config (corpus fixtures excluded — they are review input, not config). | #6 |
| 2026-06-07 | **Max-file-count gate + `OPENCODE_REVIEW_REPORT_*` env rename (LADR-032, issue #6).** (1) New **Block PR if Too Many Files Changed** workflow step (after **Generate PR Diff**): if the post-exclusion `files_changed` exceeds `OPENCODE_REVIEW_REPORT_MAX_FILE_COUNT` (repo/org **Variable**, default `100`; non-positive/non-integer values fall back to 100) it posts a `--request-changes` review ("too many files to review — split the PR, or raise the Variable") and emits `exceeded=true`. Six review-chain steps (AGENTS.md block, Skip-Review-Due-to-Blocking, chunked review, aggregation, minimize, post-review) gained `&& steps.file_count_gate.outputs.exceeded != 'true'` so the gate short-circuits without double-posting or fail-closing. (2) Renamed every non-key config env var `OPENCODE_*` → `OPENCODE_REVIEW_REPORT_*` (provider selector, three-model chain, gateway URLs, CLI version, health timeout, `MIN_FILE_COUNT_BEFORE_CHUNCKING` threshold, derived `PROVIDER_ID`/`GATEWAY_URL`) across the workflow, all `scripts/`+`scripts/lib/` shell, README, SKILL.md, AGENTS.md, CLAUDE.md. **API-key Secrets keep their names** (`OPENCODE_*_API_KEY`, incl. derived `OPENCODE_GATEWAY_API_KEY`) — they're provider credentials, not review-report config. Operational follow-up: the repo/org GitHub **Variables** must be renamed to the new names or the gate reads them empty and uses defaults (Secrets unaffected). Historical changelog/LADR mentions of old/removed names left intact. | #6 |
| 2026-06-07 | **Chunk-failure signal moved out-of-band to flag files (LADR-031).** Symptom: PR #15 (the LADR-030 PR) posted a clean review — 0 findings, `MACHINE_READABLE_ACTION: APPROVE` — yet GitHub showed `CHANGES_REQUESTED` and the PR was BLOCKED. Root cause: the fail-closed net in `aggregate-reviews.sh` decided "a chunk failed" by `grep -q "## ⚠️ Review Failed" combined_reviews.md` — but that marker string is documented in this repo's own files, so when the gate reviewed its own LADR-025/030 edits the chunk model **quoted the marker back** into the review body; the grep matched the quote and overrode APPROVE → REQUEST_CHANGES (run log: `A chunk failed to review — forcing REQUEST_CHANGES (fail-closed), overriding 'approve'`). Same self-referential class as LADR-029. Fix: `review-in-chunks.sh` now drops a zero-importance `ci_temp/reviews/chunk_<n>.failed` flag file at both failure sites (empty/tiny <200 B output, and non-zero/timeout exit) alongside the still-written visible `## ⚠️ Review Failed for Chunk:` marker; `aggregate-reviews.sh` fail-closes on `ls ci_temp/reviews/chunk_*.failed` (existence of any flag), never on review text. A flag file cannot be quoted into being, so doc/skill files may mention the marker freely. Per-chunk flags keep the parallel chunk loop race-free; flags stay in `ci_temp/` and never reach the posted review or git. Updated the stale header/Key-Behavior/LADR-025/030 cross-refs. Docs: SKILL.md (**LADR-031**, transport + holistic Key Behaviors, LADR-025/030 corrected), root AGENTS.md (LADR range → 031 + changelog). | NO-TICKET |
| 2026-06-07 | **Holistic/high-level aggregation now runs for every PR, including single-chunk (LADR-030, supersedes LADR-017).** Symptom: PR #10 (3 files → single chunk via the default `OPENCODE_REVIEW_MIN_FILE_COUNT_BEFORE_CHUNCKING=10`) posted only raw per-file chunk findings + placeholders ("No cross-chunk aggregation applies — this PR was reviewed as a single unit." / "Holistic Cross-Chunk Analysis: Not applicable") — no aggregated high-level report. Root cause: LADR-017's single-chunk short-circuit in `aggregate-reviews.sh` skipped the holistic LLM call and built the summary programmatically, on a now-stale "~15-min Pro pass" premise (LADR-022 had already moved aggregation onto the cheap orchestrator/Flash model, ~30 s). Fix: removed the `TOTAL_CHUNKS=1` short-circuit branch (and its closing `fi`) so the holistic aggregation runs for every PR, yielding the full Overall Summary / Positive Highlights / Issues Summary / Suggested Fixes / Recommendation / Holistic Cross-Chunk Analysis. Prompt phrasing made chunk-count-honest (a 1-chunk PR is "reviewed in a single chunk", not "multiple chunks") via a new `CHUNK_CONTEXT_INTRO` and reworded "Important" block. Both safety properties the short-circuit enforced were already downstream and chunk-count-agnostic — kept intact: the `## ⚠️ Review Failed` fail-closed net (reads `combined_reviews.md`, includes single-chunk `chunk_0`) and the workflow's incremental→`--comment`/never-`APPROVE` rule (LADR-004); the empty/tiny-summary REQUEST_CHANGES fail-safe also covers single-chunk now. Updated the stale fail-closed comment in `aggregate-reviews.sh`. Docs: SKILL.md (LADR-017 → Superseded, new **LADR-030**, Key Behavior rewritten, LADR-022 + LADR-025/read_file cross-refs corrected), root CLAUDE.md changelog. | NO-TICKET |
| 2026-06-07 | **Added an opt-in LLM eval harness for the chunk-review model (LADR-033).** New `scripts/eval/` scores the chunk-review LLM (the gate's blocking-findings call) on two axes against a labeled corpus so prompt/model/LADR changes are regression-tested instead of caught in production by adding another DR. **Precision (must-NOT-flag)**: one+ fixture per DR-001…DR-014 — the reviewer must not re-raise a known false positive at Critical/High/Medium; **zero tolerance** (any flag fails). **Recall (must-catch)**: 6 synthesized seeded-defect fixtures (NRE on materialized data, `Task.WhenAll` on a shared DbContext, a reachable deleted guard, SQL injection, a hardcoded secret, an irreversible migration) the reviewer should flag at ≥ its sidecar-`manifest.json` severity; fails below `EVAL_RECALL_THRESHOLD` (default 80%). The harness drives the **real** `review-in-chunks.sh` per fixture (git sandbox before→after) with the canonical DR standards (`.github/instructions/code-review-standards.instructions.md` + a DR-012…014 supplement) placed at their production dot-paths, and reuses the CI transport verbatim (`lib/resolve-provider.sh` + `lib/setup-opencode-config.sh` + `lib/opencode-health.sh` + two-tier `lib/opencode-with-fallback.sh`) — **no new transport**. Output parsed with the pipeline's grammar (LADR-012): only `[VERIFIED]` counts. Files: `eval/run-evals.sh` (core runner + gate), `eval/local-evals.sh` (local entrypoint: cred harvest + macOS `timeout` shim), `eval/lib/score-review.sh` (scorer), `eval/test-evals.sh` (15-case **structural self-test**, stubbed via the `EVAL_SELFTEST` seam — default-path-safe, no paid calls), `eval/corpus/` (20 fixtures + DR-012…014 context supplement), `eval/README.md`, and the `workflow_dispatch`-only `.github/workflows/llm-eval-harness.yml` (never on `pull_request`). Out of scope (follow-up): orchestrator-tier evals (semantic grouping, aggregation — LADR-022). Docs: SKILL.md **LADR-033** + Key Behavior, root AGENTS.md (changelog). | NO-TICKET |
| 2026-06-07 | **Chunk review runs on a locked-down `review` agent (LADR-029).** Root-caused a 0-byte `.github` chunk on PR #5 (fail-closed REQUEST_CHANGES despite an APPROVE summary): the default `build` agent auto-discovered this repo's own `ai-review-report` skill and the chunk under review — the gate's own workflow file — matched the skill's activation description, so the model **ran the skill instead of reviewing it** (`> build · qwen3.7-plus` → `→ Skill "ai-review-report"` → end-of-turn, empty stdout). Fix: new `agent.review` block in `assets/opencode.json` (`mode: primary`, **no `model` field** so `--model` still wins — verified `--agent review --model X` → `> review · X`) with `skill`/`task`/`edit`/`write`/`bash`/`webfetch`/`websearch` disabled via both the deprecated `tools` map and `permission: deny`, while `read`/`grep`/`glob`/`list`/`external_directory` stay allowed (LADR-025 access preserved). `lib/opencode-with-fallback.sh` now passes `--agent review` AND treats exit-0-but-empty (<200 bytes) output as a failure (`return 1`) so the fallback model is actually tried instead of a hollow success short-circuiting the chain; `review-in-chunks.sh` comment + empty-chunk failure-marker wording corrected (names agent tool-misfire, not just provider failure). Docs: SKILL.md **LADR-029** (+ supersede notes on the "no `--agent`" stance in LADR-023/025), root AGENTS.md (LADR range → 029 + changelog). | NO-TICKET |
| 2026-06-06 | **Health check moved to the opencode server (LADR-028).** Replaced the per-provider gateway probes (each provider's `/models`/`/v1beta/models`/`/health` path with its own auth header, duplicated across the workflow bootstrap, `lib/resolve-provider.sh`, and `local-review.sh`) with one provider-agnostic check: new `lib/opencode-health.sh` runs `opencode serve`, parses the printed listening URL, polls `<url>/global/health` until 200, then tears the server down. `resolve-provider.sh` no longer derives `OPENCODE_GATEWAY_HEALTH_URL` / `OPENCODE_GATEWAY_AUTH_STYLE` (both removed from its exports + `$GITHUB_ENV` writes + final echo); the `OPENCODE_API_HEALTH_OVERRIDE` Variable is removed everywhere (workflow env + comments, resolver, README, SKILL.md). Workflow: deleted the gateway-probe block from "Install Dependencies" (credential resolution + presence check retained) and added a non-blocking `opencode health (server /global/health)` step after "Install opencode + config + warm DB". `local-review.sh`: runs `setup-opencode-config.sh` then `opencode-health.sh` (fatal) instead of the 8s gateway curl preflight. Trade-off (documented in LADR-028): `/global/health` confirms opencode is up but does not validate gateway reachability or the API key — the local VPN-hang pre-empt is gone (the `timeout` shim still bounds hangs) and a bad key surfaces at the real model call. Marked LADR-024's preflight half + LADR-026's health-derivation superseded. | NO-TICKET |
| 2026-06-06 | **Added OpenCode Go as two providers split by SDK surface (LADR-027).** OpenCode Go (OpenCode Zen, [docs](https://opencode.ai/docs/go/)) is dual-surface — some models speak the OpenAI Chat Completions API, others the Anthropic Messages API — and one opencode.json block can pin only one `npm`. So `assets/opencode.json` gets **two** blocks: `go-openai` (`@ai-sdk/openai-compatible`, `baseURL` hardcoded `https://opencode.ai/zen/go/v1`, `apiKey={env:OPENCODE_GO_OPENAI_API_KEY}`; models `deepseek-v4-flash`, `deepseek-v4-pro`, `glm-5.1`) and `go-anthropic` (`@ai-sdk/anthropic`, same hardcoded `baseURL`, `apiKey={env:OPENCODE_GO_ANTHROPIC_API_KEY}`; models `minimax-m2.7`, `qwen3.7-plus`, `qwen3.6-plus`). The SDK appends `/chat/completions` vs `/messages`; same Zen key works for both. The base is a fixed public endpoint, so there is **no `OPENCODE_GO_*_URL` Variable** — only the API-key Secrets. Two selectors: `OPENCODE_REVIEW_REPORT_PROVIDER=OPENCODE-GO-OPENAI` → provider-id `go-openai`, `OPENCODE-GO-ANTHROPIC` → `go-anthropic`. Wired through `lib/resolve-provider.sh` (cases set a fixed URL literal for go + header doc + health-URL branch probing `<baseURL>/models`), the workflow (`OPENCODE_GO_OPENAI_API_KEY` / `OPENCODE_GO_ANTHROPIC_API_KEY` job env, `OPENCODE_REVIEW_REPORT_PROVIDER_ID` map, bootstrap creds case with fixed GW_URL, health-probe branch, error/comment strings, model-default + dispatch-input comments), `lib/setup-opencode-config.sh` (`is_ours` providers → `["gemini","github-copilot","go-anthropic","go-openai","openai"]`, baseURL clause widened to accept the hardcoded Zen base, + guidance), and `local-review.sh` (API-key harvest + `--help` + comments). Docs: README (Providers/Secrets/Variables/env tables + switching note + split callout), SKILL.md (env table, fail-fast, transport/local Key Behaviors, **LADR-027**), root AGENTS.md (System Context + changelog). | NO-TICKET |
| 2026-06-06 | **Renamed the Gemini provider key `litellm-gemini` → `gemini`** in `assets/opencode.json` and every functional usage (`OPENCODE_REVIEW_REPORT_PROVIDER_ID` value + fallback defaults in the workflow probe and `lib/opencode-with-fallback.sh`; `GEMINI` mapping in `lib/resolve-provider.sh`; the `is_ours` grep + jq provider-set in `lib/setup-opencode-config.sh`, now sorted `["gemini","github-copilot","openai"]`) plus doc references (README, SKILL.md). Also **removed all `baseURL` placeholders** from the three providers — each `@ai-sdk/*` now uses its native API base (a relaying gateway provider with its own baseURL may be added separately later); the `is_ours` predicate was relaxed so an absent baseURL passes while a present one must still be an `{env:OPENCODE_*}` placeholder. Dated entries below + LADR-023/024 narratives keep the old `litellm-gemini` name as historical record. | BNKI-001 |
| 2026-06-06 | **Applied PR #1 OpenCode review fixes.** Workflow: fail **closed** when review generation/aggregation produces no output or an empty `review_action` (was: fabricated body → default `--approve`); fork/manual-rerun handling — capture `head_repo`/`base_repo`, check out the head repo, and fetch the base branch from a new `upstream` remote (the base repo) instead of the fork `origin` at all four merge-base sites; `Post Error Comment` now `mkdir -p ci_temp` so the failure reporter survives an early failure. Scripts: `resolve-provider.sh` now validates the GEMINI model chain too (gemini-* prefix), not just non-GEMINI; `setup-opencode-config.sh` `is_ours` additionally requires every provider's URL/key to be `{env:OPENCODE_*}` placeholders so a customized personal config is never clobbered (self-heal preserved). Docs: `AGENTS.md` Secrets/Variables wording (URLs are Variables) + full skill-relative `assets/`/`references/` paths; `README.md` doc-gate row; `SKILL.md` TL;DR/System Context/C4/transport+local Key Behaviors made provider-selectable, added **LADR-026** (env-selected provider) + marked LADR-024 partially superseded, corrected context-discovery behavior and the `SKILL.md` self-reference typo; CHANGELOG imported-history disclaimer; narrowed the bundled quality-rule frontmatter scope to `*AGENTS.md`. | BNKI-001 |
| 2026-06-06 | **Retired the `/gemini-review` comment trigger — `/ai-review` is now the sole manual trigger.** The workflow `if:` condition and the commit-message scan (`grep -qiE "/ai-review"`) no longer match `/gemini-review`; the `pull_request` concurrency group was renamed `gemini-review-{pr}` → `ai-review-{pr}`; and every user-facing "re-run via `/gemini-review`" string (all-models-failed review, blocking-review skip comment, fatal-error comment) now says `/ai-review`. Docs updated to match: README review-states table + TL;DR, SKILL.md full-review-triggers / draft-PR / LADR-009 group name / LADR-021 + LADR-023 re-run wording, `MINIMIZE_REVIEWS.md`, `aggregate-reviews.sh` blocking-status note. Earlier dated changelog rows that mention `/gemini-review` are left as historical record. | BNKI-001 |
| 2026-06-06 | **Env-selected provider: GEMINI / COPILOT / OPENAI (LADR-026).** Promoted the three providers already declared in `assets/opencode.json` (`litellm-gemini`, `github-copilot`, `openai`) from config-only to runtime-selectable. New `OPENCODE_REVIEW_REPORT_PROVIDER` (GitHub **Variable** / `--provider`, default `GEMINI`) is mapped by the new `scripts/lib/resolve-provider.sh` to `OPENCODE_REVIEW_REPORT_PROVIDER_ID` (the opencode provider key prefixed onto the model) plus generic `OPENCODE_REVIEW_REPORT_GATEWAY_URL`/`_API_KEY` (the selected provider's creds, used by the `/health` + reachability checks). `lib/opencode-with-fallback.sh` now reads `${OPENCODE_REVIEW_REPORT_PROVIDER_ID:-litellm-gemini}` instead of a hardcoded `litellm-gemini`; the workflow probe uses the same. Workflow: all three credential pairs exported at job scope, `OPENCODE_REVIEW_REPORT_PROVIDER`/`OPENCODE_REVIEW_REPORT_PROVIDER_ID` computed in job `env:`, gateway creds + presence check + `/health` made provider-neutral, a post-checkout resolver step added, and the `workflow_dispatch` `model` input changed from a gemini-only `choice` to an optional free-form `string`. `local-review.sh` gained `--provider`, harvests all three cred pairs, and sources the resolver. **Fail-fast** (per user decision): non-GEMINI providers require the `OPENCODE_REVIEW_REPORT_MODEL_*` chain set explicitly to that provider's models — the run aborts if a `gemini*` model leaks through or the selected provider's creds are missing. Also fixed `setup-opencode-config.sh`'s `is_ours` self-heal predicate, which still expected a single `litellm-gemini` provider and would no longer match the committed three-provider config. | BNKI-001 |
| 2026-06-01 | **Fix failing chunks: allow `external_directory` reads (LADR-025).** Chunks failed with `## ⚠️ Review Failed for Chunk` because the prompt tells the model to `read_file` in-repo dot-path context files (`.github/*AGENTS.md`, `.docs/nfr/*`, `.ai/rules-scoped/*`), but opencode's `external_directory` permission defaults to `ask` → headless `opencode run` auto-rejects → the read errors → empty chunk → fail-closed forces REQUEST_CHANGES on clean PRs. Regression from dropping gemini-cli's `--yolo` (LADR-023). Fix: top-level `"permission": { "external_directory": "allow" }` in `assets/opencode.json` (the `--yolo` analog; no `--agent`, so the `--model` Gemini override is preserved). Widened `setup-opencode-config.sh`'s `is_ours` self-heal predicate to treat `permission` as part of our managed shape. Refreshed stale "auto-reject / --yolo" comments in `opencode-with-fallback.sh`, `review-in-chunks.sh`, `find-context-files.sh`. | BNKI-001 |
| 2026-05-29 | **Fix local "deadlock" on an unreachable gateway.** When the LiteLLM gateway (`*.private.prod`) was unreachable (e.g. not on VPN), every `opencode run` hung indefinitely and the review never progressed. Two fixes in `local-review.sh`: (1) a **gateway reachability preflight** — curls `$OPENCODE_LITELLM_URL/health` with an 8s timeout before any review work and aborts with "connect to VPN" guidance instead of hanging; (2) **rewrote the macOS `timeout` shim** to run the command in its own process group and `kill -KILL` the whole group on expiry. The previous `perl -e 'alarm; exec bash …'` shim never worked — bash traps SIGALRM and the `opencode` grandchild was orphaned and kept running, so the 60s/300s timeouts bounded nothing and leaked processes. | BNKI-001 |
| 2026-05-29 | **Zero-setup local runs.** `local-review.sh` now bootstraps its own credentials: when `OPENCODE_LITELLM_URL`/`OPENCODE_LITELLM_API_KEY` aren't already in the environment it harvests their allowlisted `export` lines from `~/.zshenv|.zprofile|.zshrc|.bash_profile|.bashrc|.profile` — non-`eval` value parsing so URL/query chars don't break it. This fixes the `/ai-review:local-review` skill, whose Haiku subagent runs a non-interactive shell that never sources `.zshrc`. The skill's Step 0 no longer pre-gates on credentials (tools only) — the script resolves + validates them. `setup-opencode-config.sh` now self-heals: an installed `litellm-gemini` config still referencing the old `{env:GEMINI_API_KEY}`/`{env:GOOGLE_GEMINI_BASE_URL}` names is refreshed to the committed `OPENCODE_LITELLM_*` version (hand-rolled personal configs without a litellm-gemini block are still left untouched). | BNKI-001 |
| 2026-05-29 | **Two named review tiers + explicit orchestrator.** Replaced the 3-tier `auto`-terminated chain with an explicit model set: `OPENCODE_REVIEW_REPORT_MODEL_PRIMARY` (gemini-3.1-pro-preview) → `OPENCODE_REVIEW_REPORT_MODEL_SECONDARY` (gemini-2.5-pro) for deep chunk review (LADR-002, now two-tier), and a separate `OPENCODE_REVIEW_REPORT_MODEL_ORCHESTRATOR` (gemini-3-flash-preview) for semantic grouping + aggregation + summary (LADR-022, rewritten). Retired the `auto` logical name, `resolve_model()`'s auto→flash mapping, `get_aggregation_model()` derivation, and `AGGREGATION_MODEL_*`. Orchestrator falls back at runtime to the resolved review model and is **not** probed at startup; the probe now tests only the two review models, and both-review-down soft-fails (LADR-021). Workflow_dispatch `model` choice `auto` → `gemini-3-flash-preview`. GitHub Variables renamed accordingly (`OPENCODE_REVIEW_REPORT_MODEL_PRIMARY/SECONDARY/ORCHESTRATOR`). Both CI and local use the single `litellm-gemini` gateway provider (LADR-024). | BNKI-001 |
| 2026-05-29 | **De-brand + env-var rename.** Skill dir `gemini-code-review` → `open-code-review-report` (`git mv`; `name:` frontmatter, all path refs, and the `.github/workflows/MANUAL_GEMINI_CLI_CODE_REVIEW_AGENTS.md` symlink retargeted). Workflow filename `manual-gemini-cli-code-review.yml` retained (CI identity / `/gemini-review` trigger). Credential env vars renamed: `GEMINI_API_KEY` → `OPENCODE_LITELLM_API_KEY`, `GOOGLE_GEMINI_BASE_URL` → `OPENCODE_LITELLM_URL` (both layers — `opencode.json {env:…}` and the workflow job-env; sourced from GitHub **Secrets** of the same names). Model chain env vars renamed `GEMINI_MODEL_*` → `OPENCODE_MODEL_*`, now sourced from GitHub **Variables** with literal defaults; `workflow_dispatch` input still overrides the primary. (The chain was subsequently finalised to the two-tier `OPENCODE_REVIEW_REPORT_MODEL_PRIMARY` → `OPENCODE_REVIEW_REPORT_MODEL_SECONDARY` + separate `OPENCODE_REVIEW_REPORT_MODEL_ORCHESTRATOR` — see the two-tier entry above; no `FALLBACK2`/`auto`.) `AGGREGATION_MODEL_*` left as script defaults (already brand-neutral). Dropped the bespoke `.gemini/.env` autosource in `local-review.sh` — local credentials now come from the shell env (documented in the new SKILL.md "Required Environment Variables" section). Retargeted the previously-dangling `knowledge-conventional-contexts-quality.instructions.md` symlink to `.ai/rules-scoped/_context-doc/`. Manual prerequisite: create the renamed Secrets/Variables in repo Settings before the next CI run. | BNKI-001 |
| 2026-05-28 | **Skill format migration.** `GEMINI_CODE_REVIEW_AGENTS.md` → `SKILL.md` per the Agent Skills standard (agentskills.io); added YAML frontmatter (`name`, `description`) so the skill is discoverable by progressive disclosure across Claude Code, opencode, Cursor, etc. `files/opencode.json` → `assets/opencode.json` (Agent Skills `assets/` convention). Changelog moved to `references/CHANGELOG.md` to keep SKILL.md focused on current-state instructions. Updated references in `setup-opencode-config.sh`, workflow YAML comments, `.github/workflows/MANUAL_GEMINI_CLI_CODE_REVIEW_AGENTS.md` symlink target, `.ai/commands/ai-review/execute.md`, and `.ai/docs/CONTEXT_INDEX.md`. | BNKI-001 |
| 2026-05-28 | **Migration: `@google/gemini-cli` → `opencode` CLI (LADR-023).** Public-tier Gemini CLI reaches EOL on 2026-06-18; we use the public-tier endpoint behind an internal LiteLLM proxy. Alternatives evaluated: Antigravity CLI (`agy`) — OAuth-only, no `--model` in print mode, not viable for headless CI; Pi (`@earendil-works/pi-coding-agent`) — viable but smaller ecosystem. Chose opencode (`opencode-ai`, MIT, 166k stars). Final shape: provider declared in `assets/opencode.json` as `@ai-sdk/google` with `baseURL={env:GOOGLE_GEMINI_BASE_URL}` (the Gemini-native LiteLLM path the old gemini-cli used) and `apiKey={env:GEMINI_API_KEY}` — `@ai-sdk/openai-compatible` was tried first but returned 401 against our LiteLLM proxy per anomalyco/opencode#5777. `lib/setup-opencode-config.sh` installs the config to `~/.config/opencode/opencode.json` (global scope) after `actions/checkout`. `lib/opencode-with-fallback.sh` preserves the LADR-002 three-tier chain at every call site (chunk review, semantic grouping, aggregation), feeds prompts via stdin, and resolves the LADR-022 `auto` logical id to `gemini-3-flash-preview` at the transport boundary. Invocations pass no `--agent` flag (opencode's built-in `build` agent has a pinned claude-opus model that overrode `--model` and silently no-opped in CI). Trade-off: LADR-015 file-state verification is currently unavailable since no agent is passed — chunk reviews verify against diff context only; restore via a custom narrow-tool agent in `opencode.json` is a tracked follow-up. Empty/tiny chunk output now produces a visible `## ⚠️ Review Failed for Chunk:` marker in the posted review body. CI workflow: `GEMINI_API_KEY` + `GOOGLE_GEMINI_BASE_URL` promoted to job-level env; opencode install + config + DB warmup (`opencode stats`) consolidated into one post-checkout step. Posted review title renamed `🤖 Gemini CLI Code Review` → `🤖 OpenCode CLI Code Review` (jq matchers widened to accept both old and new for backwards-compat OUTDATED folding on open PRs). Removed: `--yolo`, `GEMINI_CLI_TRUST_WORKSPACE`, dual-scope `settings.json`, auth-method pin, RTK Gemini hook install + `rtk gain` injection block + `show_rtk_gain` workflow input (LADR-014 marked superseded), `Invalid auth method` ERROR_PATTERN. Workflow filename `manual-gemini-cli-code-review.yml` retained for git-history continuity and `/gemini-review` trigger compatibility. | BNKI-001 |
| 2026-05-26 | Documented existing draft-PR behaviour as a Key Behavior: `pull_request` events skip drafts via the job-level `draft == false` guard; `/gemini-review`/`/ai-review` comments and `workflow_dispatch` are intentional human overrides and do not check draft state. No code change — rule was implicit, now explicit so future edits don't drop or over-extend it. | BNKI-001 |
| 2026-05-26 | Added LADR-022: default non-analytical Gemini calls to `-m "auto"`. Semantic grouping (`review-in-chunks.sh:149`) switched from `$GEMINI_MODEL_ID` (Pro) to `auto`. `get_aggregation_model()` in `aggregate-reviews.sh` collapsed — both Pro families now map to `auto` instead of Flash. Chunk review keeps Pro (LADR-002). Supersedes LADR-018 Flash-derivation. Motivated by per-PR Gemini cost reduction. | BNKI-001 |
| 2026-05-25 | Added LADR-021: all-models-failed (quota / API key / region) now posts a `--request-changes` review and exits the workflow green instead of failing with `exit 1`. Downstream steps gated on `all_models_failed != 'true'`. Triggered by run 26387093767 where every model returned a quota/auth error and the red workflow check blocked merges despite an infrastructure-side cause. | BNKI-001 |
| 2026-05-20 | Added Non-Negotiable + Key Behavior: source-of-truth precedence — diff + LADR/spec beats PR-body intent claims when they conflict. Added cross-reference to DR-014 in `.ai/skills/code-review-standards/SKILL.md`. Recurring false-positive pattern from PR #5258 (BNKI-1190) where the original PR-body wording was superseded by a later commit and an LADR-documented design choice, but Gemini flagged the current state as Critical×1 + High×14 against the stale description. | BNKI-1190 |
| 2026-05-12 | Follow-up fixes to LADR-017/018 from PR #5181 Gemini review: (1) single-chunk short-circuit now makes one targeted Flash call to generate a real `## 📋 Overall Summary` instead of a canned placeholder — LADR-017 updated; (2) placeholder-strip regex widened to case-insensitive + handles quoted/bolded/period-terminated "None found" variants; (3) `get_aggregation_model` rewritten with wildcard `case` patterns (`gemini-3*-pro*`, `gemini-2.5-pro*`) — forward-compatible with future model variants, LADR-018 Key Behavior updated. | BNKI-001 |
| 2026-05-12 | Aggregation step optimisations after observed 25-min total runtime on a 1-chunk PR (#5179, 15-min aggregation). Added LADR-017 (single-chunk short-circuit — skip Gemini call, build summary programmatically from chunk findings), LADR-018 (Flash model for aggregation step, Pro retained for chunk reviews), LADR-019 (no `read_file` at aggregation, chunk reviews own symbol verification), LADR-020 (skip Integration/DI/Test Coverage holistic sections when `TOTAL_CHUNKS <= 2`). Added matching Key Behaviors. Expected impact: 1-chunk PRs drop from ~25 min to ~10 min; multi-chunk PRs see Flash-tier aggregation latency (~3-5× faster). | BNKI-001 |
| 2026-04-24 | Fixed CI failure from new Gemini CLI "trusted folders" feature silently overriding `--yolo` to "default" mode. Added `GEMINI_CLI_TRUST_WORKSPACE: "true"` to workflow job env. Added Key Behavior for workspace trust in CI. | BNKI-001 |
| 2026-04-21 | Added Non-Negotiable: verify refactor suggestions are not already applied — check current class/method signature before recommending syntactic refactors (primary constructor, record types, collection expressions). Prevents trivial false positives. From PR #4992 (BNKI-1066). | BNKI-1066 |
| 2026-04-21 | Added Key Behavior: mode-aware regression analysis — before flagging a deleted throw/guard as a regression, trace pre-PR behaviour in every execution mode. If the throw was already dead code in the mode that survives the PR, no regression exists. Recurring false positive pattern from PR #4992 (BNKI-1066 continuous-worker refactor). | BNKI-1066 |
| 2026-04-16 | Removed gh CLI install/update from Install Dependencies step — gh is pre-installed on self-hosted runners. Updated runner requirement Key Behavior. | BNKI-001 |
| 2026-04-14 | Removed `pull_request_target: review_requested` trigger — caused duplicate runs on PR creation. Re-request review use case covered by `/gemini-review` comment and workflow_dispatch. | BNKI-001 |
| 2026-04-14 | Changed runner from `ubuntu-latest` to `self-hosted-high-memory` — litellm proxy on private network unreachable from GitHub-hosted runners. Added runner requirement Key Behavior. | BNKI-001 |
| 2026-04-08 | Fixed `---: command not found` (exit 127) in `aggregate-reviews.sh` — bare `---` markdown separator left outside heredoc after LADR-016 sync mode refactor. Restored missing `cat >> ci_temp/summary_prompt.txt << 'EOF'` wrapper. | BNKI-001 |
| 2026-04-07 | Added release branch sync review mode (LADR-016): branches matching `chore/bnk[uir]-001-sync-*` get narrowed review focused on merge conflicts, cross-PR breaking combinations, config drift, and migration ordering. Applied to chunk prompts (`review-in-chunks.sh`) and holistic aggregation (`aggregate-reviews.sh`). Added Key Behavior for sync mode. | BNKI-001 |
| 2026-04-07 | Updated model fallback chain to three tiers: `gemini-3.1-pro-preview` → `gemini-2.5-pro` → `auto`. Removed non-existent `gemini-3-pro` and `gemini-3-pro-preview`. Fixed false-positive error detection: filter `[STARTUP]` debug messages before checking error pattern. | BNKI-001 |
| 2026-04-02 | Expanded model fallback chain to five tiers: added `gemini-3-flash-preview` (Fallback 3) and `gemini-2.5-flash` (Fallback 4). | BNKI-001 |
| 2026-03-25 | Added Key Behavior: EF Core expression tree navigation — do not flag NRE inside `.Select()`/`.Where()` lambdas (SQL, not runtime C#). Recurring false positive pattern from BNKI-780 rounds 1 and 2. Cross-references DR-012 in code-review-standards. | BNKI-780 |
| 2026-03-24 | Added Clean Code principles as focus order item #9 in chunk review prompt, referencing `.ai/rules/clean-code-principles.instructions.md` | BNKI-001 |
| 2026-03-24 | Added RTK token optimization proxy to CI pipeline (LADR-014): installs RTK binary, configures Gemini hook for 60-90% token compression on `--yolo` tool outputs. Added Key Behavior for RTK integration. | BNKI-001 |
| 2026-03-23 | Added LADR-015: strengthened Critical/High verification (must confirm symbol in current file via `read_file`) and diff integrity checks (warn on large diffs). Updated `/ai-review:analyse` to auto-skip `[SPECULATIVE]` findings. Added Key Behaviors for symbol verification and diff integrity warning. | BNKI-970 |
| 2026-03-11 | Added migration/schema chunk detection (LADR-013): `*.sql`, `*_Migration.cs`, `*/Migrations/*.cs` → migration-focused prompt with reversibility, existing data, nullable safety, index impact, rollback strategy focus. Added concurrency/thread-safety as item #2 in code review focus order (elevated from sub-item in Correctness). Added API contract breaking changes as item #4. Added cross-file consistency as item #10. Added cross-layer field consistency and API contract breaking changes to aggregation holistic "What we looked for". Added concurrency safety check to aggregation full-review section. | BNKI-001 |
| 2026-03-11 | Fixed `read_file: command not found` error — escaped backticks in unquoted heredoc in `review-in-chunks.sh` to prevent bash command substitution on every chunk. Added Windows symlink fallback in `local-review.sh`: symlink → junction (`mklink /J`) → real directory. Fixed junction cleanup using `cmd.exe rmdir`. | BNKI-001 |
| 2026-03-11 | Removed `--open` flag from local CLI execution Key Behavior (flag not implemented); corrected output path from `ci_temp/` to `.context/ai-review/<timestamp>/`. | BNKI-001 |
| 2026-03-10 | Added `local-review.sh` wrapper script for local CLI execution of Gemini code review. Reuses existing CI scripts with macOS compatibility shim. Added Key Behavior entry for local CLI execution. | BNKI-001 |
| 2026-03-10 | Added LADR-012: confidence tagging (`[VERIFIED]`/`[SPECULATIVE]`) and verification-incomplete suppression. Added Key Behaviors for confidence tagging, verification-incomplete suppression, and semantic grouping "logic moved" detection. Updated LADR-011 with logic-moved rule. | BNKI-001 |
| 2026-03-02 | Added doc-only chunk detection (lighter review prompt for documentation chunks), Skip Areas enforcement (cap at Low Priority), AGENTS.md template sections are optional Key Behavior. Clarified context file discovery: dot-prefixed paths auto-included for every chunk. Fixed stale mandatory NFR file reference: `dotnet-standards` → `TOOL_SETUP_AGENTS.md`. | BNKI-001 |
| 2026-02-28 | Added AGENTS.md drift detection as HIGH priority review item in Key Behaviors and chunk review focus order. | BNKI-001 |
| 2026-02-28 | Remediation pass: aligned with updated template (Test References section, three-part value test, anti-pattern removal). No structural changes needed. | BNKI-001 |
| 2026-02-28 | Aligned with TEMPLATE_AGENTS.md: removed emoji prefixes, Overview/Business Value, file listings, script implementation details, shell best practices, testing locally, complete implementation history (#1-#115). Retained all 11 LADRs, key behaviors, and safety constraints. 2558→120 lines (~95% reduction) | BNKI-001 |
| 2026-02-21 | #115: Optimized chunk review focus areas and signal-to-noise guidelines | |
| 2026-02-17 | #113-#114: Adaptive chunk splitting (LADR-010), semantic business context grouping (LADR-011) | |
| 2026-02-06 | #111-#112: Fix Gemini miscounting "None found", fix broken fallback review action logic | |
| 2026-01-16 | #108-#110: Rename step IDs, fix blocking review detection, clarify AI_WORKFLOW_RULES exclusion | |
| 2026-01-05 | #104-#107: Blocking review detection, commit message triggers, fix full reviews being blocked | |
| 2026-01-02 | #103: Revert to selective concurrency (LADR-009), supersede LADR-008 | |
| 2025-12-22 | #98-#101: Model display name fix, quota detection, template exclusion, minimize race condition | |
| 2025-11-28 | #87-#97: Two-part aggregation, test file pairing, on-demand file access, mandatory NFR context | |
| 2025-10-28 | Initial chunked review implementation (LADR-001) | |
