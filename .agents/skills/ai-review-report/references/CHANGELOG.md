# ai-review-report — Changelog

> Load only when updating the skill or auditing past decisions; not needed for routine execution.
>
> **Imported history:** entries before 2026-06-01 predate this polyrepo. Legacy names (`.ai/`, `gemini-code-review`, `manual-gemini-cli-code-review.yml`) do not exist here; preserved as audit trail.
>
> Full LADR narratives (Context/Decision/Consequences) live in the skill `AGENTS.md`.

> **OpenCode v2 binding policy:** a confirmed foreign managed-config binding always fails closed (exit 3, fatal everywhere). An unreadable source list also exits 3 when the service is live; only when `/api/info` and config inspection both fail does it return exit 1 — advisory in CI, but fatal in local and eval preflights. Liveness never short-circuits the binding question.

| Date | Change | Ref |
|:-----|:-------|:----|
| 2026-09-24 | **Optional decision-model scoring of the merged findings (issue #156).** New `run-review.sh` Step 17.6 (and `local-review.sh` Step 5c), between the merge and aggregation, runs `lib/score-findings-decisions.sh`: a structured decision model (TypeSafe Jev 1.13) answers typed questions per finding (`supported`, `severity`, `pre_existing`, `actionability`) and per PR (`block_merge`, `dominant_risk`, `overall_risk`), and the answers are written into `findings.merged.json` as `decisions` / `decisions_summary`. Raw HTTP (curl + jq), not opencode — the model has no chat surface — with its own preflight; question text lives in the new `assets/decisions-questions.json`. Providers are a new `decisions` scope in `lib/resolve-provider.sh`: `OPENCODE-GO-DECISIONS` → `https://opencode.ai/zen/v1/systemone` (reuses `OPENCODE_GO_OPENAI_API_KEY`) and `OPENROUTER-DECISIONS` → `https://openrouter.ai/api/alpha/decisions` (reuses `OPENCODE_OPENROUTER_API_KEY`) — the fourth and fifth fixed public bases, no new Secret. Six Variables in both packagings: `OPENCODE_REVIEW_REPORT_ENABLE_DECISIONS` (default `0`), `_DECISIONS_PROVIDER`, `_DECISIONS_MODEL`, `_DECISIONS_MODE` (`annotate` default / `filter`), `_DECISIONS_MIN_PROBABILITY` (`0.5`), `_DECISIONS_TIMEOUT` (`20`). `annotate` renders `· decision: supported 0.91`, `[UNSUPPORTED]` and `(decision model: medium)` at the end of each finding line plus a Coverage line; `filter` also suppresses non-critical findings below the threshold, only on full sidecar coverage with no failed chunk, never `critical`, and lists what it removed. The PR-level answers go to the orchestrator as a `## 🎯 Decision-model verdicts` prompt block and into `metadata.json`; no verdict path reads them. `merge-findings.py` now strips a chunk-supplied `decisions` key. Off by default and byte-identical when off. Verified live against `zen/v1/systemone` with `jev-1.13` (a Console key authenticates on both `zen/v1` and `zen/go/v1`); OpenRouter path covered offline only. New `test-score-findings-decisions.sh`; `test-resolve-provider-scope.sh` extended and both registered in the blocking regression job. Self-review follow-up: a curl transport failure (e.g. timeout mid-body after a 200) is reported as `000` rather than as a malformed 200; `umask 077` is scoped to the key file so the rewritten merged document keeps its ordinary mode; `lib/annotate-suggested-fixes.sh` now counts decision-model suppressions, which previously left a filter-only run without its "no numbered finding" note. | LADR-091 |
| 2026-09-24 | **The aggregation summary call is bounded, and the bound is split so a hung orchestrator still leaves the fallback a turn.** `aggregate-reviews.sh`'s orchestrator summary call had **no timeout at all** — `lib/opencode-with-fallback.sh` has no clock of its own — so it was bounded only by the 6 h job default; consumer runs 35969611034 and 35995746041 spent 13+ minutes in a call that normally takes one or two. New `lib/run-split-chain.sh` reuses the LADR-081 mechanism instead of a single wrapping `timeout` (which would have let a hung orchestrator spend the whole budget with the review-model fallback never run): shares from `lib/split-chunk-budget.sh` — which gains optional caller floors, chunk behaviour byte-identical when omitted — stage 1 on the orchestrator with no in-chain fallback, stage 2 on the review model bounded by `total - elapsed`. Default total **600 s** (390 s orchestrator + at least 210 s fallback; floors 180 s / 120 s), configurable via the new Variable `OPENCODE_REVIEW_REPORT_SUMMARY_TIMEOUT`, declared in both workflow packagings and documented in `README.md` / `SKILL.md`; junk falls back to 600. A degenerate chain (orchestrator already rewritten to the review model by LADR-066's probe) gets the whole budget. A timeout exits 124 and takes the existing `agg_ok=false` path unchanged — LADR-085 still decides the verdict, and nothing on this path can make a review greener; the console and the fallback overview now say "timed out" when the budget ran out (only then — a fast provider error keeps the old wording), and the chain's status lines (split, which model timed out after how long, who answered) are lifted onto the console while `ci_temp/summary_stderr.log` keeps everything. New `test-summary-timeout.sh` (real `timeout` kills; the e2e part runs the real `aggregate-reviews.sh` at its 600 s default through a 1/100 time-scaling shim) is registered in `llm-eval-harness.yml`; `test-chunk-prompt-budget.sh`'s sandbox now carries the two libs the summary needs and asserts the stubbed summary actually succeeded, so a missing lib can no longer silently switch that harness onto the fallback template. | Consumer runs 35969611034, 35995746041; LADR-092 (new), LADR-066/081/085 |
| 2026-09-24 | **Complete chunk reviews are no longer discarded for format, and the failure marker says so when they are.** Consumer PR 99 (run 36024963902) fail-closed with 2 of 7 chunks "failed" although every model in the chain (`deepseek-v4-pro`, `glm-5.2`, on both attempts) had written a complete review; each was rejected by `lib/review-has-shape.sh` and the chunk's High finding (`DossierSelection.cs:117`) never reached the posted review. Three causes, three fixes. (1) `**Issues Found:** None found. (justification.)` failed the end-of-line placeholder rule — the predicate now accepts exactly **one** parenthetical (one nesting level, so a cited `Get()` is fine) closed at end of line after the period, list or inline form; truncated notes, period-less `None found so far (…`, and a closed note followed by narration (`None found. (a.cs is clean.) Now reading b.cs`) still reject. (2) A `NFR-01 … NFR-07` range heading left five files unnamed, and a Low finding cited only `(lines 147-154)` with no tier lines — neither shape was forbidden by the prompt, so the chunk prompt now states four format rules (one heading per file / never a range, all four tier lines — of which only Low is enforced, and the prompt says so — `filename:line` anchors, nothing after `None found` on its line); the anchor and inventory rules themselves are **not** relaxed. (3) The marker blamed "model API error" for shape rejections and, on the unsplit LADR-084 retry, claimed the secondary was never reached: `opencode-with-fallback.sh` now names each shape-rejected model on stderr as `SHAPE_REJECT_MARKER <provider/model> (<n> bytes)` (bytes via `wc -c`, not `${#var}`, which counts characters), `review-in-chunks.sh` parses that line back with a line-anchored `_shape_reject_marker` literal pinned equal to the transport's, the marker lists those models and reports a format failure for non-timeout exits (naming only the models that answered — a chain can mix a shape rejection with a provider error), and the retry timeout has its own branch that claims nothing about attempt 1. Pinned in `test-opencode-with-fallback-targets.sh` (marker literal equality, bytes under a UTF-8 locale), `test-review-chunk-threshold.sh` (four timeout branches; a runtime drive of the real failure block with a shape-rejecting transport) and `test-chunk-prompt-guards.sh`. The narrative is LADR-091. | Consumer PR 99, run 36024963902; LADR-091 (new), LADR-084/087 |
| 2026-09-22 | **Four defects fixed in the LADR-090 runtime-`AGENTS.md` mechanism, found by self-review of the branch.** (1) **The feature was never exercised by the suite meant to prove it.** `test-review-chunk-threshold.sh` builds its sandbox by copying a *whitelist* of libs, and `build-runtime-agents.sh` was not on it — so every chunk in that harness logged `⚠️ Failed to build runtime AGENTS.md`, the runtime file was never produced, and all three `run_case` assertions still reported PASS. Green CI on PR 153 therefore proved only that chunking still worked, not that LADR-090 did. The builder is now copied into all five sandbox sites, and `run_case` asserts one runtime `AGENTS.md` per chunk so a future whitelist omission fails loudly instead of silently. (2) **A latent `set -e` abort, unmasked by fixing (1)**: `ci_temp/chunk_<n>` is now a *directory*, and the harness's `rm -f ci_temp/chunk_*` cleanup exits 1 on a directory, which under `set -euo pipefail` aborts on the second `run_case`. It had not fired only because (1) meant the directory was never created. Changed to `rm -rf` at both sites. (3) **Fail-open contract broken at the callers**: `build-runtime-agents.sh` degrades gracefully, but both callers hardcoded `OPENCODE_RUN_CWD` at the generated directory, so a builder failure would make `cd` fail inside the transport, every model in the chain return 1, and the chunk fail-close — an enrichment failure converted into a `REQUEST_CHANGES` (and, for the aggregation pass, into the fallback summary template). Given (1), that path was one sandbox fix away from being live. Both callers now guard on the generated `AGENTS.md` existing and pass an empty cwd otherwise, which the transport already treats as "run where the caller is". (4) **False premise in every chunk prompt**: the replacement `## 📖 PROJECT RULES` block was emitted unconditionally and asserted the rules "were loaded" even when the scoped set was empty, and it carried a hardcoded "this project uses **C# 14** with .NET 10 SDK" claim inherited from one .NET consumer — false for every Python/TS/Go/Rust repo the gate reviews, and a false premise is the DR-015 hallucination class. The block is now gated on a non-empty context set and states the override rule language-agnostically; version facts belong in the consuming repo's own rules, which the runtime `AGENTS.md` now carries. `test-build-runtime-agents.sh` gains cases 7–9 pinning the fail-open guard at both call sites, the gating of the loaded-rules claim, and the absence of hardcoded language/framework facts (comment lines stripped before the structural greps, per the LADR-075 idiom). | LADR-090 |
| 2026-09-22 | **LADR-090's "no path-addressable instructions" claim scoped to the CLI and the config — a v2 plugin is not bound by it.** Researching `frap129/opencode-rules` as a possible rules loader turned up the correction: its `latest` (`1.0.0`) is V1 (`@opencode-ai/plugin ^1.3.7`, `chat.message` / `experimental.chat.messages.transform` hooks) and would not load on our v2 gate at all, but `2.0.0-beta.2` on the `next` tag is a real v2 port — `{ id, setup(ctx) }` registering `session.hook('context')` — which proves a plugin **can** read a file by path and push its content into the model request (`event.system` / `messages`). LADR-090 chose the cwd mechanism partly on the belief that no such channel existed; the narrative now records the plugin as the standing alternative, why it was not taken (first JS in a bash-only review path, and a silent-non-load failure mode of the same shape as LADR-053's false-`✅`, LADR-080's inert `instructions` and LADR-087's missing `index.js`), and the shape it would take if the cwd approach ever fails. **Adopting `opencode-rules` itself was rejected** on four counts beyond the V1/v2 split: discovery is fixed to `~/.config/opencode/rules/` + `.opencode/rules/` with no seam for `.agents/rules` or `.github/instructions`; it matches on `globs:` rather than the `applyTo:` frontmatter LADR-089 already parses; delivery is reactive (`tool.execute.after` → next turn) and so arrives after the single headless turn a chunk gets; and it has no per-chunk seam. A first-match-wins `.github/instructions` → `.agents/rules` → `.claude/rules` fallback chain was also rejected — `find-context-files.sh` takes the **union** of those trees, a chain silently drops the second when a repo carries both (inverting LADR-089's fail-open asymmetry), and `.claude/rules` is not a real convention (Claude Code uses `CLAUDE.md` + `.claude/skills/`; the rules directory is Cursor's `.cursor/rules`). No behaviour change. | LADR-090 |
| 2026-09-22 | **LADR-090 written for the runtime `AGENTS.md` mechanism, and the gate's own documentation gate satisfied.** The feature below shipped with a CHANGELOG row and **no `AGENTS.md` entry** — the same convention break LADR-088/089 were written to correct one day earlier, and this time the gate caught it: PR 153's FULL review posted `❌ BLOCKED - Missing or Invalid AGENTS.md` because the commit modified no `*_AGENTS.md`, `README.md` or `SKILL.md`. **LADR-090** records why the config cannot carry this (the `instructions` array is inert on v2, which applies equally to a `.claude/rules`-style tree a consumer might declare there), why the cwd is the selector rather than a path argument (no flag loads an instructions file by path, so per-chunk filtering requires a per-chunk directory), and the two load-bearing exclusions — exact `AGENTS.md` basenames (v2 walks cwd→$HOME and loads the consumer's chain natively) and the leading YAML frontmatter (`applyTo:`/`alwaysApply:` is filter metadata, and a bare `---` is a parsing hazard). It also records the probe methodology, because the first probe was run in a directory that was **not** a git repository, showed no ancestor walk-up at all, and would have led to the wrong design: without a git root v2 treats the cwd as the project root. Root `AGENTS.md` and `SKILL.md` gained the matching Key Behavior; the skill `AGENTS.md` gained an editing-time entry naming the three couplings an editor breaks first (absolutize-before-cd, absolute-only `read_file` guidance, never re-concatenate an exact `AGENTS.md`). | LADR-090 |
| 2026-09-21 | **Chunk and aggregation project rules now load as runtime AGENTS.md instructions instead of a "read these files" prompt block.** opencode v2 does not auto-load custom `*_AGENTS.md`, `.agents/rules/*.md`, `.github/instructions/*`, or `MANDATORY_CONTEXT_FILES` (only exact `AGENTS.md` is discovered by scope, LADR-087), so `review-in-chunks.sh` used to write a `## 🚨 MANDATORY: READ PROJECT CONTEXT FILES FIRST` block listing those paths and told the model to `read_file` each one — spending tool calls against the chunk exploration budget. A new `lib/build-runtime-agents.sh` concatenates the chunk's scoped context set into a runtime `ci_temp/chunk_N/AGENTS.md`; each chunk now runs opencode from that cwd (`OPENCODE_RUN_CWD`, forwarded by `lib/opencode-with-fallback.sh`, which absolutizes the prompt path first) so v2 loads the rules as active instructions with no `read_file` round-trip. The aggregation pass gets the same via `ci_temp/orch/AGENTS.md` from the merged context set. Consumer exact `AGENTS.md` chain still loads natively by the git-root walk; the runtime file adds no standard-`AGENTS.md` content, so guidance is not injected twice. The prompt keeps the task contract inline (review shape, severity taxonomy, LADR-055 sidecar) and drops only the path-list read block; file-access guidance is now absolute-only because the chunk session cwd moved off the repo root. New `test-build-runtime-agents.sh` (6 cases) pins the exact-`AGENTS.md` exclusion, fail-open minimal output, and scope-filter frontmatter stripping; `test-opencode-with-fallback-targets.sh` gained an `OPENCODE_RUN_CWD` case. | — |
| 2026-09-21 | **LADR narratives written for the two mechanisms added this round, and the completion-predicate summary brought back in step.** The fixes below shipped with CHANGELOG rows but **no `AGENTS.md` entry** — a convention break (`knowledge-conventional-contexts-quality.instructions.md`: the AGENTS.md must be updated in the same commit or PR), and every prior feature commit in the log had done it. **LADR-088** records the Suggested Fixes renumbering: why the prose has no mapping but the block anchor does, the conservative resolution ladder, and the three attempts it took to stop rewriting a reference that names several findings. **LADR-089** records per-chunk context scope filtering, with the fail-open asymmetry as its central constraint and the silent-drop glob bug as the worked example of breaking it — plus the process lesson that the filter shipped with no tests at all, which is how that bug reached a posted review. The **completion-predicate summary** in `AGENTS.md` carried an explicit "keep this paragraph in step with the code" warning and had gone stale on two counts: the placeholder line is now a **chain** anchored at both ends, and "Low tier present" now means present **in either form** — placeholder or real finding. That paragraph also now records the declined tightening and why, so the next reviewer reads the decision instead of re-deriving the opposite for a third time. Root `AGENTS.md` gained the LADR-089 Key Behavior and an LADR-088 clause on the LADR-055 line; `SKILL.md` gained the scope-filter mechanic. | LADR-088, LADR-089 |
| 2026-09-21 | **"Low present = complete" is confirmed as the completion rule, on both paths.** Two reviews in a row asked for the tiers above Low to be required — 5271360715 finding 1, implemented in `7dcacf8`, then 5271520178 finding 1 at **Critical**. Both are **declined**, and `7dcacf8` is reverted. The reason is a decision this repo already recorded: `test-opencode-with-fallback-targets.sh` has asserted `predicate_case accept "per-severity form with a middle tier omitted but Low present"` since review 5266682360, under the note that "the per-severity form is complete only when its last tier (Low) is present — the template emits the tiers in order, so a cut after any earlier tier leaves later tiers unreviewed". That argument holds: this predicate catches **truncation**, truncation removes a **suffix**, and Low is the last tier the template emits — so Low present means the model reached the end of the list whatever it printed above. A missing **middle** tier is a formatting deviation, which is the failure class that has repeatedly cost this gate whole reviews (the two fixes committed alongside this one both exist for exactly that). Implementing the tightening broke **four** pre-existing assertions, one naming the behaviour outright. `7dcacf8` had also left a real asymmetry — the finding path demanded upper tiers while the placeholder path did not — which is indefensible on the design's own logic and is what this row removes: **one rule, both shapes the low tier can take.** The truncation guard is unaffected (a cut after Critical or after Medium still rejects, as does a Low tier cut off before its content). Revisiting this means changing the LADR and those four fixtures, not the conditional alone. | reviews 5271360715, 5271520178 |
| 2026-09-21 | **Brace and character-class globs no longer silently drop review rules.** `context-scope-filter.py` escaped `{ts,tsx}` and `[jt]s` into literals, and — the deeper cause — `entry.split(",")` shattered `**/*.{ts,tsx}` into `**/*.{ts` and `tsx}` before a regex was ever built, so every rule using valid VS Code / Copilot glob syntax was dropped from every chunk **while the filter reported success**, contradicting its own documented fail-open contract. `split_patterns()` now splits only at brace depth 0; `glob_to_regex()` translates `{a,b}` to alternation and `[abc]` / `[!abc]` to character classes; anything still untranslatable raises `UnsupportedGlob` and the candidate is **included** with a warning, matching how unreadable frontmatter is already handled. New `test-context-scope.sh` (21 assertions) is the first test coverage this filter has had, and is registered in the blocking regression job; it pins the fail-open contract as hard as the matching, because dropping a rule the model needed is silent while including a spare one costs a path in a list. | review 5271520178 |
| 2026-09-21 | **Repeated singular Suggested Fixes references no longer collapse onto one number.** The plural and separator guards each inspected a single match, so `finding 1 and finding 2` looked like two solitary references and both were rewritten to the same anchor — `finding 7 and finding 7`, two distinct items merged into one. `rewrite()` now counts references first and returns the line untouched when more than one is present: one anchor resolves one finding. Solitary references still repoint. `test-merge-findings.sh` 28u–28w. | review 5271520178 |
| 2026-09-21 | **Plural Suggested Fixes references are no longer partially renumbered.** `renumber-suggested-fixes.sh` leaves multi-finding references alone because one `file:line` anchor cannot resolve a set — but it detected only a trailing `-` or `,` after the first number. Prose separators are at least as common, so `findings 1 and 2` was rewritten to `findings 7 and 2`: one correct number, one wrong one, pointing the reader at a finding that has nothing to do with the fix block. A mixed reference is worse than the untouched original, which is at least consistently stale. Two signals now suppress the rewrite, either sufficient: the **plural word itself** (`findings` can never denote one item), and a separator — punctuation or prose (`and`, `or`, `to`, `through`, `thru`, `&`, `+`) — followed by another number. Singular references under the same anchor still repoint. `test-merge-findings.sh` 28q–28t, verified to fail with both guards removed. | review 5271360715 |
| 2026-09-21 | **SUPERSEDED (same day, see the row above) — the low-finding completion route briefly required every tier above it to have been emitted.** `_rhs_ph_tier` proves only that SOME one complete placeholder token exists, so pairing it with any low line let a section carrying Critical, High and a Low finding — but **no Medium at all** — pass as finished, and the transport would accept an incomplete review instead of falling back. The hole predates the low-finding route (the old placeholder pair accepted a bare Critical plus Low just as readily), but that route widened it. Route B now additionally requires Critical, High and Medium to be present. **Presence, deliberately, not a placeholder**: a tier carrying a real finding was emitted just as surely as one saying "none found", and demanding "none found" for the upper tiers would re-create the exact defect the low-finding route closed — a section whose Medium finding writes its location as prose would fail both routes and be discarded. Route A (all tiers resolved to placeholders, one per line or the compact chain) is untouched. `test-opencode-with-fallback-targets.sh` +4 cases; both reject fixtures write their location as prose on purpose, because a `file:line` anywhere in the section satisfies the anchor route and would make the assertions vacuous. | review 5271360715 |
| 2026-09-21 | **A section with clean tiers plus one real Low finding no longer fails the chunk.** `_rhs_section_ok` accepted a section by one of two routes, and the commonest section shape there is satisfied neither: clean `Critical`/`High`/`Medium` placeholders plus a genuine Low finding has **no low placeholder** (the model found something), and the anchor route needs a literal `<in-chunk-file>:<line>` that a finding written as `(line 106)`, or one about a neighbouring file such as `npm/cli/_run.js:18`, does not carry. The complete review was then dumped to stderr by `opencode-with-fallback.sh` and returned as `rc 1` — reported as `❌ Chunk N review failed`, indistinguishable from an API error. Run 35645178034 lost chunks 0, 3 and 4 to it at 135–194s each (nowhere near the 650s budget), then chunk 0 twice more through the secondary and the LADR-082 retry, fail-closing PR 93 to `REQUEST_CHANGES` over a review that had been written correctly three times by two different models. Model-independent: `glm-5.2` lost its `AGENTS.md` section in the same run, on `(line 106)`. Minimal fix: the paired test proves the model **reached** the low tier, and a low-tier FINDING proves that exactly as a low-tier PLACEHOLDER does — so either satisfies it. The conjunction with a complete `_rhs_ph_tier` chain is what keeps it honest, so a response that stopped before the tier list still rejects, as does a bare `- 🔵 Low Priority:` cut off before any content (the `[^[:space:]]` after the colon is load-bearing). **Accepted trade-off:** this cannot distinguish a real low finding from one truncated mid-sentence, because a real finding is prose with no terminator to anchor on; discarding correct reviews and fail-closing clean PRs is the worse error. `test-opencode-with-fallback-targets.sh` +4 cases, each verified to fail with only this route disabled. | run 35645178034 |
| 2026-09-21 | **A clean file reported with all four tiers on one line no longer fails the chunk.** `lib/review-has-shape.sh` anchored its tier placeholder with `$` immediately after the first "none found", so the compact form models actually emit — `- 🔴 Critical: None found · 🟠 High: None found · 🟡 Medium: None found · 🔵 Low: None found` — matched no placeholder pattern, and a clean file has no `file:line` for the anchor fallback to catch. `opencode-with-fallback.sh` then wrote the ENTIRE complete review to stderr and returned 1, which `review-in-chunks.sh` reports as `❌ Chunk N review failed (exit code: 1)` — indistinguishable in the logs from an API error or a dead endpoint, and the reason two separate investigations read a full review with a well-formed sidecar sitting in a *failed* chunk's diagnostic log. Seen on consumer PR 95 (run 35640330645), where `deepseek-v4-pro` failed as secondary at 228s having written a complete review. It is **not** the only rejection of this class — the low-tier row below is the shape that actually cost run 35645178034 three chunks. The tier pattern is now a CHAIN — one or more complete tier placeholders joined by non-alphanumeric separators — anchored at both ends. The `$` anchor is deliberately NOT relaxed: it is what rejects a truncated response (review 5264311874, finding 1), and because a separator can only be followed by another COMPLETE placeholder, `- 🔴 Critical: None found yet, continuing to read the handler` still rejects. The paired low-tier test keeps the same chain shape on both sides so a truncated line cannot satisfy it. `test-opencode-with-fallback-targets.sh` +7 predicate cases (4 accept, 3 anti-truncation reject), verified to fail without the fix. | run 35640330645 |
| 2026-09-21 | **Suggested Fixes cross-references now track the merged numbering.** The orchestrator numbers `finding N` against the Issues Summary *it* wrote (the Formatting Rules tell it to reuse one stable identifier across sections), the LADR-055 splice then replaces that summary with one `merge-findings.py` numbered by severity-then-confidence, and every reference in `## 📝 Suggested Fixes` was left pointing at whatever took slot N. Observed on consumer PR 95, run 35640330645: the merge promoted `AGENTS.md:85` from third to first on confidence 100, and three of the four fix blocks then cited a finding in a different file; a fourth was right by coincidence, and a fifth cited a number the merge had suppressed, so it resolved to nothing in the posted body. A reader quoting one to `/ai-review` asks for a fix to a file the block above it never mentions. `lib/annotate-suggested-fixes.sh` said there was "no deterministic mapping from a prose fix paragraph back to a merged finding" — true of the prose, but each block is anchored by the ``### `file:line``` heading the prompt mandates, and file-plus-line is exactly the key the merge dedupes on. New `lib/renumber-suggested-fixes.sh` resolves through that anchor: file match, then line-in-spec (`7`, `52-53`, `87,161` are all real shapes), then a ±10-line tolerance for the drift between a model-quoted hunk and the sidecar line. Exactly one survivor repoints the number; more than one is left untouched rather than guessed; none means the merge dropped, suppressed or demoted it, so the reference is reworded to carry no number and `annotate-suggested-fixes.sh` (now run after) explains why with counts. The line-tier is load-bearing, not cosmetic: a file-only match would have handed the suppressed `docs/wiki/ci.md:161` block the number belonging to `docs/wiki/ci.md:52`. Rewrites are confined to the Suggested Fixes section and skip fenced code, plural/range references (`findings 1-4`) are left alone since one anchor cannot resolve a set, and the holistic and per-chunk sections stay verbatim (LADR-005). Idempotent; best-effort like its companion — a stale number is cosmetic, a mangled report is not. `test-merge-findings.sh` 28a–28p, including the two call-site assertions. | issue #125 |
| 2026-09-21 | **Chunk-budget coverage tests behavior instead of counting assignments.** The invalid-primary assertion now evaluates the shipped guard for junk/zero/negative/empty inputs and a valid split, preserving LADR-084's separate retry reset. Missing primary restoration, missing reserve clearing, and an inverted guard each fail the test. This removes the pre-existing red assertion exposed by the new blocking job. | Run 35617566069; LADR-081/084 |
| 2026-09-21 | **The blocking job runs the deterministic eval self-test.** `eval/test-evals.sh` covers scoring and DR claim matching with stubbed models; it no longer depends on the report-only paid job for pre-merge execution. | Review 5268318291, finding 3; LADR-033/087 |
| 2026-09-21 | **Root plugin inputs activate the blocking regression job.** Changes to `index.js`, `opencode-plugin.js`, `package.json`, or `.claude-plugin/plugin.json` run the offline suites; the paid eval scope stays limited to the reviewer and its workflow. | Review 5268318291, finding 2; LADR-087 |
| 2026-09-21 | **Local review validates the CLI through the shared installer on every run.** Cached v1 binaries and mismatched pins no longer bypass the v2 contract; the parent shell adds the install directory to PATH. Offline entrypoint tests cover v1 replacement, explicit pins, cached-v2 reuse, and invalid-pin failure. | Review 5268318291, finding 1; LADR-048/087 |
| 2026-09-21 | **Deterministic OpenCode v2 regressions now run on pull requests.** `llm-eval-harness.yml` adds a separate blocking, secret-free job for the installer, health, context, plugin, config, fallback/shape, RTK, and version suites. It runs safely on drafts/forks and uses an in-job relevance check so branch protection always receives a result. | LADR-087 |
| 2026-09-21 | **DR-002 recognizes named stores joined by “as well as”.** A narrow adverb + storage verb + named-store-pair branch catches “unnecessarily stores the payload in the object store as well as the database” without widening the retry-sensitive generic branch; the exact wording is pinned as a positive discriminator case. | LADR-033/087 |
| 2026-09-21 | **Emoji severity headings no longer masquerade as finding blocks.** `review-has-shape.sh` now terminates headings before checking severity emoji, so narration beneath `### 🔴 Critical Issues` cannot combine with a later `file:line` token and silently pass the completion gate. The adversarial shape is pinned in `test-review-chunk-threshold.sh`. | LADR-087 |
| 2026-09-21 | **Retain RTK on OpenCode v2 and bypass only its incompatible plugin.** `install-rtk.sh` now installs/keeps the binary, probes `rtk init --help`, initializes v1 with `--opencode`, and automatically initializes v2 with `--opencode-v2` once upstream exposes it. Until then the bypass is non-fatal and the review's Versions block shows the installed RTK version with an explicit warning instead of omitting RTK or rendering a false ✅. | LADR-053/054/087 |
| 2026-09-21 | **DR-002's reverse-order branch learns the generic `storage` noun (review 5267080615).** "Keeping the payload in the database and object storage is redundant" escaped the forbidden-claim ERE because the branch's second noun slot accepted `stores?` but not `storage`; the noun is added there and the exact wording is pinned in the positive discriminator cases. The review's suggested insertion into the leading subject alternation was not used because it still does not match this sentence. | LADR-087 |
| 2026-09-21 | **DR-002's adverb branches consolidated into one dual-storage form; the local-job pin re-bumped (review 5266991867).** (1) "A retry needlessly duplicates the object-store upload" matched the unconstrained adverb-plus-duplicate branch. The three adverb branches are now one — adverb, then a storage verb or "duplicat", then a multiplicity word within five words — and the duplicate-verb branch additionally accepts "(across|between) the database and the object store", so the round-three positive still counts while single-destination and "every retry" sentences do not. Consolidating rather than adding kept the pattern under the ugrep complexity canary, where two additive drafts failed. (2) The local-job example pin is re-bumped to this round's last code commit, per the file's stated contract. | LADR-087 |
| 2026-09-21 | **The anchor must name a chunk file; process substitution installs are caught; DR-002 drops the past participle from its both-stores branch (review 5266893822).** (1) `lib/review-has-shape.sh`'s anchor accepted any `token:number` whose token had a letter, so `HTTP:500`, `status:404` and `confidence:75` in prose counted as `file:line` evidence; with the inventory the gate always supplies, the path token must now end in one of the chunk's files, and without one it must contain a dot or a slash — an extensionless `Dockerfile:12` is therefore accepted through the inventory only. The regex reaches awk through the environment, not `-v`, which would strip the basenames' escaping. (2) `test-install-opencode.sh` now catches `bash <(curl … install)` with the same path/env/sudo prefixes as the piped form, while `diff <(curl …) expected` (an interpreter is required) does not count. (3) DR-002's both-named-stores branch accepted "duplicated", so "writes to both the database and the object store are duplicated when the command is retried" matched; it takes the present forms only now. | LADR-087 |
| 2026-09-21 | **Every per-file section must be complete; probes are terminated as a process group; DR-002 learns the duplicate verb (review 5266762643).** (1) `lib/review-has-shape.sh` judged completion on the last per-file section only, so a heading for file A followed directly by file B's heading — no result for A — satisfied the inventory through the heading and passed on B; the body is now split on `File:`/`Files:` headings and every section must carry a completion signal (preamble before the first heading excluded, no heading = one section). (2) `lib/opencode-health.sh` signalled only the probe's wrapper pid, so a wedged probe's child survived the timeout; probes now start in their own process group (`set -m` around the `&`, a bash builtin so macOS needs no `setsid`) and the terminator signals the group, with the test asserting the recorded child is gone. (3) "The handler duplicates the payload across both stores for no benefit" sat outside DR-002's branches; a narrow duplicate-verb → both-stores branch is added, and a retry sentence duplicating an upload to one archive is pinned as ignored. (4) The completion-predicate summary in the LADR now describes all four stages, and the earlier row no longer calls the severity label optional. | LADR-087 |
| 2026-09-21 | **The per-severity placeholder needs its Low tier; write nouns leave DR-002's adjective branch; same-line download-and-run is caught (review 5266682360).** (1) `lib/review-has-shape.sh` accepted `- 🔴 Critical: None found` on its own as a clean review, so a response cut after the first tier passed with three tiers unreviewed; the per-severity form now requires the Low line (the template's last tier, so every truncation point is caught without fail-closing an honest review that skipped a middle tier), and the compact `- None found` is unchanged. (2) "Retries cause unnecessary archive writes" matched DR-002's adjective-first branch; `write(s)/written` leave it, the redundancy sense of a write stays caught through the named-subject and both-stores branches, and two retry-amplification phrasings are pinned as ignored. (3) The download-then-execute matcher excluded the whole curl line, so `curl … -o /tmp/oc && bash /tmp/oc` passed; the line is no longer excluded — the output path in `-o` cannot match because it is not at command position — and same-line `&&` and `;` fixtures are pinned. | LADR-087 |
| 2026-09-21 | **Plural headings reset the tail; download-then-execute installs are caught; DR-002 learns verb-then-adverb; the nested-scope route corrected (review 5266540555).** (1) `lib/review-has-shape.sh`'s last-section split matched `file:` only, so a consolidated `Files:` heading never reset the buffer and an earlier clean section could vouch for a truncated last one; `files?:` now. (2) `test-install-opencode.sh`'s matcher saw only piped forms; the installer saved with `-o`/`--output`/`>` and later run by an interpreter, sourced, or executed directly is caught now, while a download that is only checksummed is not. (3) DR-002 missed "the payload is stored redundantly in both backends" (adverb after the verb); a verb-then-adverb branch with a multiplicity word is added, and two ordering/retry phrasings are pinned as ignored. (4) The previous round's note and changelog row named `MANDATORY_CONTEXT_FILES` as a deterministic route for a nested standard `AGENTS.md`; the finder excludes exact `AGENTS.md` basenames even from that list, so the one deterministic route is a custom `*_AGENTS.md` name. Both corrected. | LADR-087 |
| 2026-09-21 | **"Wasteful" joins DR-002's adjectives; nested AGENTS scope is documented as a trade-off (review 5266453301).** (1) "The object-store copy is wasteful and should be removed" is a redundancy objection the pattern missed because "wasteful" was known only to the both-places branch; it joins the subject-is-adjective list with "pointless", and a retry sentence using "wasteful" is pinned as ignored. (2) LADR-087's consequences claimed dropping standard `AGENTS.md` from chunk prompts "preserves v2's scoped behaviour"; v2 discovers a nested `AGENTS.md` only when the model reads or lists its scope, so an inline-diff-only review may never load it. The text now states the trade-off and the one deterministic route: expose the guidance under a custom `*_AGENTS.md` name the finder injects. Exact `AGENTS.md` basenames are excluded from explicit context even when listed in `MANDATORY_CONTEXT_FILES`. | LADR-087 |
| 2026-09-21 | **Blank-pin wording, the adverb branch, a GITHUB_ENV leak in two tests, and the pin re-bumped (review 5266343056).** (1) Three docs (root `AGENTS.md`, `CLAUDE.md`, LADR-087's consequences) said the installer "enforces" a pin or that blank means latest; a blank pin is valid and means "any v2" — an existing v2 binary is reused, latest is installed only when none is present — and all three now say so. (2) DR-002's adverb branch paired "unnecessarily/needlessly" with any storage verb, so an ordering finding ("unnecessarily persists the primary row before the archive call") matched; with a storage verb the adverb now also needs a multiplicity word within five words, while "unnecessarily duplicated" stands alone. (3) `test-prepare-opencode-v2-config.sh` and `test-run-review.sh`'s LADR-080 probe sourced `prepare-opencode-config.sh` with the runner's `GITHUB_ENV` inherited, so under Actions they appended a temp `OPENCODE_CONFIG` path that the test deletes on exit; both clear it now. (4) The local-job pin is re-bumped to this round's last code commit and its comment states plainly that an in-PR pin is intermediate by construction. | LADR-087 |
| 2026-09-21 | **Structure decides mentions too; the heading split is case-insensitive; declared globs are tested behaviourally (review 5266192686).** (1) `lib/review-has-shape.sh`'s last-section split matched `File:` case-sensitively, so a lowercase `file:` heading let an earlier section's `None found` vouch for a last section the model never finished; it now lowercases before matching. (2) The inventory counted a file as mentioned anywhere in the body, so "I will inspect src/b.cs next." satisfied it and a response cut off right there passed as complete; a mention now counts only in structure — a heading line or a finding block and its continuation lines (indented, list, bold-label, fenced or blank, up to the next column-0 prose line) — with four cases pinning narration out and evidence lines and consolidated headings in. (3) `test-find-context-files.sh` collapsed every declared `instructions` glob to its directory root, so a new `.github/instructions/*.md` read as covered while the finder enumerates only `*.instructions.md`; every declared glob now gets a representative fixture that must be discovered, plus an undeclared control that must not, and the mutation (declaring that sibling pattern) turns the test red. | LADR-087 |
| 2026-09-21 | **The with-jq health test now requires jq; glob-path claim pinned as not reproducing (review 5266102870).** (1) `test-opencode-health.sh` iterated with-jq and without-jq, but on a jq-less machine both iterations exercised the fallback and reported both branches green; it now fails fast without jq, as `test-check-versions.sh` does. (2) The review claimed bracket paths such as `src/api/[id]/page.tsx` defeat the unique-suffix search in `lib/review-has-shape.sh`. They do not — the suffix is quoted inside the `case` pattern and the mention matcher escapes metacharacters — and four cases now pin bracket and star paths in both directions so the claim is not re-raised unverified. | LADR-087 |
| 2026-09-21 | **Placeholders are the documented shapes only; the raw-installer guard sees wrapped interpreters; health cleanup is bounded for both probes (review 5266005570).** (1) `lib/review-has-shape.sh`'s list placeholder was "any bullet ending in none found", so a narration bullet — `- Checked callers; none found.` — under the marker passed as a clean review; it now accepts only `- None found` and the per-severity `- 🔴 [VERIFIED] Critical: None found` (the emoji, the `[VERIFIED]`/`[SPECULATIVE]` tag and the word `Priority` are optional; the severity label itself is required), pinned in both directions. (2) `test-install-opencode.sh`'s matcher required the bare interpreter word right after a single pipe; `| /bin/bash`, `| env bash`, `| sudo -E bash` and `| tee log | sh` all read as "no raw installer" and are now caught, while `| sha256sum` is not. (3) `lib/opencode-health.sh`'s EXIT trap did a plain kill + wait on the liveness probe only — a SIGTERM-ignoring child hung cleanup, and an interruption during config inspection left that child running; both probes now go through the bounded `_oh_terminate`. (4) The editor-guidance bullet that still demanded full sidecar coverage for rendering (pre-existing, P1) is rewritten to the amended LADR-055 policy the code implements. | LADR-087 |
| 2026-09-21 | **Retry findings out of DR-002's pattern; the health-wiring test now proves the abort (review 5265948835).** (1) "Both writes are duplicated on retry" and "retrying creates duplicate copies" matched the forbidden claim: the past participle "duplicated" is dropped from the subject-is-adjective branch (with a word boundary so "duplicate" cannot match its own prefix) and "copies" from the generic duplicate branch; the redundancy sense stays caught through the named-subject and adverb branches, and the three retry sentences are pinned as ignored. (2) `test-run-review.sh`'s `_health_wiring` grepped for a call and a comparison anywhere in the file, so an exit-3 branch that logged and carried on still read as wired; it now extracts the `-eq 3` block and requires `exit 1` inside it, and mutates `run-review.sh` in a temp copy to prove the check goes red. (3) The previous changelog row and the LADR note said "every sentinel range" is stripped; the shipped strip follows the extractor's last-pair rules, and both now say so. The `npm-publish.yml` header comment named the retired `v1` floating tag. | LADR-087 |
| 2026-09-21 | **The predicate judges the prose, never the sidecar; DR-002 consolidated (review 5265814254).** (1) `lib/review-has-shape.sh` now strips the LADR-055 sidecar before evaluating, by the extractor's own rules (the last complete BEGIN…END pair, or a later unterminated JSON-looking block — not every quoted pair) — the transport saw raw output with the JSON sidecar while the chunk gate saw it stripped, and a sidecar carrying priority wording, an evidence `file:line` and "Issues Found" in a title let narration pass the transport, spend the fallback, and fail downstream. Five cases pin it, including an unterminated BEGIN and a file named only in the JSON. (2) DR-002's `forbidden_claim` missed "storing the payload in both the database and object store duplicates the data". Widening a window both broke the ugrep complexity canary and admitted a retry sentence, so the pattern was consolidated instead: the subject/noun and active-verb branches are one alternation, a named-both-stores branch is added, and "duplicates the object store" alone no longer counts (retry sense). Shorter than before, 24 redundancy and 16 retry phrasings pinned. | LADR-087 |
| 2026-09-21 | **The completion anchor no longer demands a file extension.** `lib/review-has-shape.sh`'s `file:line` anchor required `.ext`, so a complete finding on `Dockerfile:12`, `Makefile:4` or `LICENSE:1` was rejected as an incomplete review — the chunk burned its fallback and retry and then fail-closed, blocking a clean PR in any repo whose chunk cites only extensionless files (review 5264629523, finding 2). The path token now needs a letter or a slash instead, which is what separates a path from a clock reading or a ratio; `12:30` and `3:1` are pinned as non-anchors alongside the two extensionless accepts. The reviewer's suggested `[A-Za-z0-9_./-]+:[0-9]+` was not used: it matches those numerics and would reopen the narration-with-a-number hole. | LADR-087 |
| 2026-09-21 | **The placeholder must sit inside the `Issues Found` subsection; DR-002 learns "backend"; RTK inputs say they are dormant (review 5264530992).** (1) `lib/review-has-shape.sh` looked for the `None found` placeholder anywhere in the last per-file section, and the template's `**Pre-existing (informational):**` block carries its own — so an empty Issues Found followed by that placeholder was accepted as a clean review. The check is now scoped from the marker to the next bold label or heading; four cases pin both directions. (2) DR-002's `forbidden_claim` did not know "backend(s)", so "using both backends is redundant" passed as unrelated; the noun is added to both store-noun branches, and consistency findings about backends drifting are pinned as ignored. (3) The `rtk_version` input descriptions in the gate workflow and the caller template presented the pin as effective; they now say it is reserved and ignored on OpenCode v2 until a v2-compatible RTK integration ships (LADR-087). | LADR-087 |
| 2026-09-21 | **Inline `None found` must end the line; DR-002 learns the active-verb forms; the LADR heading matches its rule (review 5264311874).** (1) `lib/review-has-shape.sh` accepted `**Issues Found:** None found so far, but let me still check…` because the inline placeholder was not anchored to line end while the list-item form already was; both now require it, and an unfinished response trailing off a placeholder is rejected. (2) DR-002's `forbidden_claim` missed "writing to both stores duplicates the data" and "needlessly writes twice"; the active-verb and adverb forms are matched, keyed on an architectural subject so retry findings ("a retried command duplicates the upload") stay clear, and the pattern is held under the complexity ceiling of non-GNU greps — a longer draft compiled on GNU grep and was rejected outright by another implementation, which would read as "nothing forbidden". Fifteen redundancy and ten retry phrasings are pinned. (3) LADR-087 item (i)'s heading still said "the last finding decides" after the rule was changed to any-anchored-block; the heading is corrected in place and the side note `ai-analyse` had appended beneath it is removed. The local-job example pin remains an in-PR SHA by construction; it moves to the merge commit after merge. | LADR-087 |
| 2026-09-21 | **The completion signals must come from the same finding; plus two doc corrections from review 5264172516.** (1) `lib/review-has-shape.sh` tested the severity marker and the `file:line` anchor independently over the last section, so narration with a location followed by a finding truncated at its severity label passed as complete. Findings are now blocks (severity or priority line to the next such line or heading) and the anchor must be inside one of them; any anchored block accepts, because an honest review may end with a location-less advisory and a last-block rule fail-closed the chunk-threshold suite's honest-review control. Seven cases pin it, including anchors on indented and unindented continuation lines so honest reviews are not fail-closed. (2) The local-job example's tooling pin is bumped to this round's last code commit; it stays a full SHA and stays "bump after merge". (3) README and both example callers said an unset `OPENCODE_CLI_VERSION` installs the latest v2; `install-opencode.sh` reuses any v2 binary already on `PATH` when no pin is set, so that is true only on a fresh runner. The wording now says so. | LADR-087 |
| 2026-09-21 | **Four scope fixes from review 5263727118.** (1) DR-002's `forbidden_claim` paired "duplicate" with "write(s)", which is the retry sense almost every time a model writes it ("Retries can cause duplicate writes"), and missed the named-store redundancy forms ("the object-store write is redundant with the database write"); "duplicate" now pairs only with storage nouns and the named-store forms are matched explicitly, with both concise phrasings pinned in `test-evals.sh`. The reviewer's alternative — an "architecture-specific matcher" with an `allowed_claims` list — was not adopted: the harness scores one ERE per fixture on purpose, and an allow-list would exempt a whole finding on a substring. (2) The raw-installer guard scanned shell and YAML only; the root README, where the raw pipeline last shipped, and `.docs` Markdown are now in scope, with fenced-block fixtures both ways. `AGENTS.md`, `CLAUDE.md` and the changelog stay out because they quote the forbidden pipeline to explain the rule. (3) A file mention in `lib/review-has-shape.sh` is now a whole token: `app.js.map` and `myapp.js` no longer satisfy `app.js`. (4) `test-opencode-v2-plugin.sh` now packs the real package offline, asserts all four `SKILL.md` are inside and the eval corpus is not, and runs the shipped `index.js`'s `setup()` so the links are proven to resolve. | LADR-087 |
| 2026-09-21 | **Three exactness fixes from review 5263417133, and an eval regex that failed on the wrong sentence.** (1) `lib/opencode-health.sh` tested the managed-config binding with `grep -F` on the path — a substring test, so a service bound to `<managed>.bak` or any longer containing path passed as bound; it now requires exact membership among the `debug config` `path` fields (jq equality, or the path as a complete JSON string without jq), pinned in both branches. (2) `test-install-opencode.sh`'s raw-installer guard grepped per line, so `curl … \` / `| bash` split across lines read as "no raw installer"; the matcher now joins backslash and trailing-pipe continuations only (not a whole-file flatten, which would join unrelated statements) and is fixture-tested in both directions. (3) `lib/review-has-shape.sh`'s inventory check matched basenames, so one `index.ts` vouched for another; the required mention is now the shortest trailing path unique among the chunk's files. (4) DR-002's `forbidden_claim` allowed 60 characters between adjective and noun, so an atomicity finding — "retrying may then **duplicate** or conflict with the first **write**" — failed run 35566556046 while the same finding worded differently had passed the run before; the pattern now binds adjective to noun within two words, in either order, and `test-evals.sh` pins six redundancy phrasings as caught and four atomicity phrasings as ignored. Also the second stale RTK-pin row in README (finding 5's sibling, not cited). | LADR-087 |
| 2026-09-21 | **`None found` narration passed as a clean review; an omitted file passed as a complete one.** `lib/review-has-shape.sh`'s placeholder clause was a bare `none found` substring, so prose such as "checked the callers, none found so far" was accepted at both gates and the chunk passed unreviewed (review 5263305644, finding 1, Critical). It now requires the `Issues Found` marker plus the placeholder in template shape — a list item ending in `None found` or inline after the marker. Separately, last-section scoping could not see a file the model never mentioned (finding 2, High): `review_chunk` now passes the chunk inventory as `OPENCODE_EXPECTED_CHUNK_FILES` to all three predicate call sites, and with two or more files each must be mentioned by basename anywhere in the body; single-file chunks are unchanged. Both pinned in `test-opencode-with-fallback-targets.sh` and mutation-tested. Also: LADR-087 decision item (10) now states the dual-outage liveness exception the code implements (finding 3), and `eval/run-evals.sh`'s install hint includes the `export PATH` the installer cannot do for the caller (finding 4). | LADR-087 |
| 2026-09-21 | **The transport shape predicate is now authoritative, not an exemption from the byte floor.** `lib/opencode-with-fallback.sh` consulted `OPENCODE_OUTPUT_SHAPE_CHECK` only for output under 200 bytes, so a narration-only answer above the floor was accepted at the transport, spent the LADR-002 fallback, and was then rejected by the chunk gate with the secondary model never run — the disagreement the shared predicate was introduced to prevent (review 5261655825, finding 1). When set, the predicate's verdict now decides in both directions; unset callers keep the pure floor, pinned from the long side as well as the short one. It is invoked through `bash` so a lost exec bit cannot disable it, and a set-but-missing path warns and degrades to the floor. | LADR-087 |
| 2026-09-21 | **A leak fix deleted every chunk review; the helper now cleans up only what it owns.** `ai-analyse` closed the piped-stdin temp-file leak in `lib/review-has-shape.sh` by adding `$_rhs_src` to the EXIT trap — but in path mode that variable is the caller's file, so `review-in-chunks.sh`'s `chunk_<n>.md` was removed the moment the chunk gate validated it. The run log still said "completed", aggregation found nothing, and eval run 35535795942 reported INFRA on all 20 fixtures. Ownership is tracked in a dedicated variable and both directions are pinned by test: a passed file survives, a piped call leaves nothing in `TMPDIR`. The local-job example's pin comment no longer claims the in-PR SHA is the released v2 commit. | LADR-087 |
| 2026-09-20 | **Completeness is now scoped to the last per-file section, which is what four earlier rounds were missing.** A chunk is almost always multi-file, but `lib/review-has-shape.sh` searched the whole body — so a review reporting `None found.` for file A and truncated part-way through file B was accepted, with file A's result vouching for a file B that was never reviewed. The predicate now narrows to the final `File:` section before applying its signals (no heading = the body is its own section, preserving LADR-077). Same round: priority wording now additionally requires the `Issues Found` marker, because prose can carry a location too — "check the high priority areas in `run-review.sh:1196`" satisfied both signals while containing no review. Both halves of that conjunction had been tested separately and passed; the combination never was, which is the actual lesson: **fixtures must cover clause conjunctions and multi-file chunk shapes**, because every real chunk is multi-file. Mutation-proven. Separately, `lib/opencode-health.sh` now captures `opencode debug config`'s **exit status**: it could emit a partial document containing the managed path and then fail, and the grep would match that and pass the gate on an inspection that did not succeed. And the Key Behavior describing the predicate is rewritten against the shipped code — it had been wrong twice in opposite directions, once claiming `Issues Found` was required for every finding and once for none; every claim in it is now verified by running the predicate. | LADR-087 |
| 2026-09-20 | **Stopped tuning the completed-review regex and required two independent signals instead.** Four rounds each closed one truncation shape and the next round found a longer prefix — bare emoji, then emoji plus `[VERIFIED] High Priority:` — because the chunk template emits its parts in order, so every truncation point leaves a valid-looking prefix. A fifth candidate regex, tested before shipping, still accepted both. `lib/review-has-shape.sh` now requires a severity/priority marker **and** a `file:line` anchor (only the `None found.` placeholder exempt): the anchor sits at the END of the finding, so truncated scaffolding never reaches it, and the two signals are orthogonal. Cost: a finding with no location is rejected — already off-contract per LADR-055, and fail-closed is the right direction. Twelve shapes pinned. Separately: the cached-v1 installer test modelled the SHADOWING scenario (v1 earlier on PATH) while claiming to test the cache, then used `|| true` to hide the resulting failure — it now writes the shim to `$HOME/.opencode/bin` where the installer actually replaces it, and requires exit 0; mutation-proven. And the install-source-of-truth Non-Negotiable was violated in **five** places across this migration (README, local-review.sh twice, run-evals.sh), each found by hand one round at a time — `test-install-opencode.sh` now greps the tree for a raw install curl outside the shared lib, so the class is asserted mechanically rather than spotted. | LADR-087 |
| 2026-09-20 | **A severity marker with nothing after it passed as a completed review.** `lib/review-has-shape.sh`'s emoji clause was `grep -qF` on the character, which asks whether a byte is present rather than whether the model wrote a review — so `- 🟠`, and even a bare `🔴` with no newline, were accepted and an unreviewed chunk would have been aggregated with no failed-coverage signal. The clause now requires alphanumeric text after the marker on the same line, preserving LADR-077's deliberate emoji-only acceptance while rejecting truncation at the marker; the emoji patterns stay separate `-e` alternations because a bracket expression over multi-byte characters decomposes into bytes. The `None found.` clause was audited against the same threat and deliberately left a bare literal — there the marker IS the content, and `- None fou` does not match. **Third round of the same class**, and the lesson is the process not the regex: each time the reported clause was tightened and its neighbours were not re-audited. Eleven shapes are now pinned (seven reject, four accept) and mutation-tested. Also corrected a comment still naming the removed `OPENCODE_OUTPUT_MARKER`. | LADR-087 |
| 2026-09-20 | **Custom context discovery returns to `*_AGENTS.md`; the underscore is doing exclusion work, not styling.** The pattern had been widened to `*AGENTS.md ! -name "AGENTS.md"` to admit separator-less names such as `FooAGENTS.md`. It works, but only because of the companion carve-out: in `find(1)` a leading `*` matches **zero** characters, so `*AGENTS.md` matches the exact standard basename too — and v2 loads every `AGENTS.md` natively by scope, so the broad pattern injects the same file twice, which is the duplication LADR-087 exists to remove. Putting the exclusion in the pattern makes it unloseable; a carve-out in a second predicate can be dropped by a later edit with no visible symptom. Accepted cost, stated rather than hidden: a consumer file with no separator before `AGENTS.md` is not discovered and must be renamed or listed in `MANDATORY_CONTEXT_FILES`. `test-find-context-files.sh` now asserts both directions and is mutation-tested — widening the pattern fails on the duplicate-injection assertion. Reverses the widening from earlier today in this PR. | LADR-087 |
| 2026-09-20 | **Narration could pass as a completed review; two test suites could not see their own invariants.** `lib/review-has-shape.sh` inherited LADR-077's three clauses verbatim, but they had sat *behind* the 200-byte floor — promoted to the sole gate, `(critical|high|medium|low).{0,12}priority` matches prose, so 55 bytes of narration ("Let me check the high priority areas before reviewing.") was accepted and the chunk would have been aggregated as clean with zero findings. That clause now requires the mandated `Issues Found` marker to co-occur; emoji and the `None found.` placeholder stay standalone (narration emits neither, and LADR-077's generosity exists because the flag forces `REQUEST_CHANGES`), and `Issues Found` is deliberately not standalone or the earlier truncation hole reopens. Unit-tested directly rather than only through the gate, because the gate-level shape cases sit below `test-review-chunk-threshold.sh`'s known-red abort and never run. `test-find-context-files.sh` gained a **non-underscore** fixture (`FooAGENTS.md`): every custom fixture had an underscore, so narrowing discovery back to `*_AGENTS.md` left the suite green — the defect fixed twice already was untested both times. Mutation-proven. `test-opencode-health.sh` now runs every probe under `env -i`: the caller's `OPENCODE_CONFIG` leaked in and aborted the suite on its first nominal case, reading as a broken gate rather than a leaky harness. | LADR-087 |
| 2026-09-20 | **One shape predicate, asked by both gates; plus three smaller corrections from review 5261324773.** The transport gate (`lib/opencode-with-fallback.sh`) and the chunk gate (`review-in-chunks.sh`) were deciding "is this a completed review?" differently — the transport matched a single literal, which could not express a condition whose alternatives are a severity line OR the `None found.` placeholder. So `**Issues Found:**`-truncated output was accepted by the transport and rejected by the chunk gate, and accepting **spends** the LADR-002 fallback: the secondary model never ran and the chunk fail-closed with rescue capacity unused. Both now call `lib/review-has-shape.sh`; the transport opts in via `OPENCODE_OUTPUT_SHAPE_CHECK`, unset everywhere else so the summary/grouping/analyse callers keep the pure byte floor. `test-find-context-files.sh` now **derives** the instructions roots from `opencode.json` instead of hardcoding two, so a newly declared tree fails the build until the finder enumerates it (mutation-tested by adding a third). `lib/opencode-health.sh` escalates SIGTERM→SIGKILL after a 5s grace: `kill` + `wait` is not a bound, because a process ignoring SIGTERM leaves `wait` blocked forever and the timeout becomes a no-op — the test hangs if the escalation is removed. The posted report's context banner said only `*_AGENTS.md` were supplied, misleading consumers using names like `FooAGENTS.md`; corrected there and in three more discovery comments. Adding a lib under `scripts/lib/` needed four test harnesses updated — each materialises the script tree from a fixed list, and a missing file reads as a fail-closed gate bug rather than a harness gap. | LADR-087 |
| 2026-09-20 | **`.agents/rules/**` returns to the `instructions` defaults — in both lists, not just the declarative one.** The v2 migration had reduced the array to the `.github/instructions` pair; `.agents/rules/*.md` and its recursive companion are back, and `.agents/rules-scoped/**` is deliberately left out — scoped rules reach a review through `MANDATORY_CONTEXT_FILES`, which the consuming repo controls per run, so declaring the whole tree would duplicate them into every chunk. Because v2 resolves **no** `instructions` entry, the array alone would have been decoration: `find-context-files.sh` now enumerates `.agents/rules/**/*.md` as well, one recursive `find` per tree (the `*`/`**` pair is a v1 glob-engine artifact, not two file sets), existence-guarded like the `.github/instructions` block because both are consumer-repo paths absent from here. `test-find-context-files.sh` fails the build if the two lists drift, and is mutation-tested. Also corrected four stale discovery claims that still said the finder requires an underscore (`*_AGENTS.md`) in `SKILL.md`, the skill `AGENTS.md` ×2 and `README.md` — the same defect ai-analyse fixed in the root `AGENTS.md` only. | LADR-087 |
| 2026-09-20 | **Breaking OpenCode v2 migration (`2.0.0`).** All install paths now delegate to the `/v2/install` endpoint and configured CLI pins must be `2.x`; reusable callers/tooling fallbacks move to the `v2` major. Standard `AGENTS.md` files leave the per-chunk path list and use v2's native root/lazy-nested scope, while the custom `*_AGENTS.md` convention remains ancestor-discovered. The requested `.github/instructions/*.instructions.md` + recursive config array is retained but accurately treated as inert (current v2 resolves no `instructions` entries), so `find-context-files.sh` enumerates that tree explicitly to preserve effective rule loading. Health moves from unauthenticated `/global/health` to `opencode api get /api/info`; version notices use v2 update metadata instead of the V1 `opencode-ai` package. The npm plugin is ported to the v2 default-export contract `{id, setup}` and consumer config `plugins`, staying **dependency-free** — `Plugin.define` is `plugin => plugin` at runtime, and this package distributes skills rather than driving opencode, so an `@opencode/plugin` install (`effect` RC, `zod`, `@ai-sdk/provider`, four optional peers) could only ever stop four symlinks being created; package and Claude plugin manifests bump in lockstep. RTK is skipped loudly/non-fatally because its released OpenCode plugin still uses the V1 API. Two further v2 CLI breaks were caught by exercising a real `opencode v2.0.11` binary rather than reading docs. **`--log-level WARN` is rejected outright** — v2 validates the flag against `all|trace|debug|info|warn|warning|error|fatal|none` and raises `~effect/cli/CliError/InvalidValue` before contacting any model, so the v1 spelling would have failed both model probes and every chunk review at once, printing a stack trace that reads like a provider outage; all three call sites are lowercased and `test-run-review.sh` greps the gate's sources so it cannot come back. **A v2 background service binds its config when the service starts** — one shared service per user, and an already-running one ignores a later client's `OPENCODE_CONFIG` silently (reproduced: with a service up from config A, `OPENCODE_CONFIG=B opencode debug config` still reports A), which would leave LADR-071's managed provider, model ids and the LADR-029 permission lockdown absent with nothing in the log. Every CI entrypoint now runs `opencode service stop` immediately after `prepare-opencode-config.sh` (`run-review.sh` 5e, the analyse workflow's Initialize OPENCODE, and `eval/run-evals.sh` — whose workflow warms `opencode stats` *before* the harness prepares anything, so that path was already wrong), and `lib/opencode-health.sh` verifies via `opencode debug config` that the managed path is the one actually loaded, fatal wherever it fires, including CI. An unreadable source list counts as *not confirmed* and is fatal too — with one carve-out: when the `/api/info` probe ALSO failed, the service simply is not answering, so that is one fault already reported rather than a config mismatch, and it stays advisory (exit 1) to preserve LADR-028's non-blocking stance. Liveness is recorded rather than acted on immediately, so a probe failure can no longer skip the binding question altogether. **The npm plugin package would have shipped dead**: v2 resolves a plugin package directory by loading `<package>/index.js` and ignores `main`/`exports`, so the `opencode-plugin.js` entry was skipped with no error and no log line (`opencode plugin list` → "No plugins found"). A root `index.js` re-exporting the implementation is now published and is `main`; verified end-to-end from the packed tarball on 2.0.11 — plugin listed, all four skills linked, `.git/info/exclude` maintained. The GitHub Packages hop is fine: v2 installs plugin packages itself and honours the consumer `.npmrc` scope mapping (a missing PAT shows as `401 ... authentication token not provided` in the opencode log). **A correct review of a clean file was being thrown away for being short.** The 200-byte empty-output floor (LADR-029) was a proxy for "nothing came back", calibrated against v1 — which leaked the model's between-tool narration onto stdout. v2 routes narration to stderr, so every response shed a few hundred bytes and genuine clean reviews fell under the floor: rejected, re-asked from every model in the LADR-002 chain, retried once more by the LADR-082 sweep, then marked `chunk_<n>.failed`, which fail-closes the PR. Eval run 35525187790 lost DR-004/DR-012/DR-013 this way (precision stayed 11/11 and recall 5/6 — the run went red purely on infra count), and run 35523655319 lost a different fixture, DR-015: the failing set is stochastic, the failing class is not. DR-013 makes the mechanism exact — its v1 output was 446 bytes of which 308 were narration, leaving a review byte-identical in length to the 138-byte v2 one that was discarded. Fixed by deciding on structure rather than length: `review-in-chunks.sh` now gates on `chunk_review_has_shape` (which already encoded the right predicate and was simply running *after* the floor short-circuited it), 0 bytes keeps its own diagnosis, and the hardcoded `200` is deleted rather than re-tuned; `lib/opencode-with-fallback.sh` gains an opt-in `OPENCODE_OUTPUT_MARKER` that satisfies the check when present, declared by the two chunk-review call sites only so every other caller stays byte-identical. Six new assertions in `test-review-chunk-threshold.sh` and four in `test-opencode-with-fallback-targets.sh`, including a pin that the unset-marker path is unchanged. **The managed-config binding check was detection with no consequence in CI.** It shipped behind `bash opencode-health.sh || true`, so a confirmed foreign binding was logged and discarded — CI would keep reviewing under the wrong provider and model chain with no LADR-029 permission lockdown over untrusted PR code, while `local-review.sh` aborted on the identical condition. It also degraded to *no check at all* when `opencode debug config` returned nothing, on the reasoning that a false alarm beat a missed one; that was backwards, since a false alarm is a loud message naming the command to run and a false pass is silent. `opencode-health.sh` is now three-valued — 0 healthy, 1 liveness-only (advisory in CI per LADR-028, fatal locally), 3 managed config not confirmed (fatal everywhere, and "unreadable" counts) — and `run-review.sh` plus `pipeline-ai-analyse.yml` test for 3 explicitly. `test-run-review.sh` greps both call sites (mutation-tested) and `test-opencode-health.sh` pins all three codes. **A host already carrying opencode v1 silently skipped the whole migration**: v1 and v2 share the `opencode` command name and are [no longer installed side by side](https://opencode.ai/v2/docs/migrate-v1/), but `install-opencode.sh` accepted any cached version when `OPENCODE_CLI_VERSION` was unset — the documented default — so a self-hosted runner or a developer box printed `✓ opencode found on PATH (version: 1.18.31)`, installed nothing, and ran the v2 gate on a v1 binary. The post-install verify had the same hole for a **package-managed** v1 (brew/npm) sitting earlier on PATH than `$HOME/.opencode/bin`, which shadows even a successful install. A major >= 2 is now required in both places, failing loudly and naming the guide rather than reviewing on v1. The committed config was also audited field-by-field against that guide: it carries **none** of the accepted-but-unsupported fields v2 ignores-and-warns on, and keeps each nested provider/agent entry in a single format as the guide requires. Also verified live against 2.0.11: `opencode run` still takes its prompt on stdin, and the committed V1-shaped `provider`/`agent`/`permission` blocks normalize into v2 `providers`/`agents`/`permissions` (`bash`→`shell`, `task`→`subagent`) with our rules appended last, so last-match-wins preserves both the review agent's lockdown and `external_directory: allow`. Added focused offline tests for each boundary. | LADR-087 |
| 2026-09-19 | **Default provider flips `GEMINI` → `OPENAI`, default chain `gpt-5.6-sol` / `gpt-5.5` / `gpt-5.6-terra`.** The default lived as a literal in nine places (`run-review.sh`, `lib/resolve-provider.sh` review + analyse scopes, `aggregate-reviews.sh`, `review-in-chunks.sh`, `lib/detect-trivial-pr.sh`, `local-review.sh`, `eval/local-evals.sh`, all three workflows, `.docs/examples/code-review-local.yml`) plus a hand-written mirror in `test-run-review.sh`'s precedence harness — all moved together, along with the `OPENCODE_REVIEW_REPORT_PROVIDER_ID` fallback (`gemini` → `openai`). `GEMINI` remains a fully supported selector with its models declared. `OPENCODE_REVIEW_REPORT_OPENAI_URL` stays **required**: adding `https://api.openai.com/v1` as a fourth hardcoded base was rejected because the OpenAI slot is the LiteLLM-proxy relay point and a hardcoded base reproduces LADR-034's proxy-key misroute. README/SKILL.md corrected — both the OpenAI and Gemini URL rows had claimed a default the resolver never had. | LADR-086 |
| 2026-09-19 | **A summary-only failure no longer blocks a fully reviewed PR.** Consumer PR 79 review `5255179276` finished with `0 of 5` failed chunks and `5/5` valid sidecars after retry recovery, with no Critical/High findings, but the separate aggregation-summary call returned failed/empty and its fallback hardcoded `REQUEST_CHANGES`. The recommendation sync then refused to use the complete merged evidence solely because `agg_ok=false`. `aggregate-reviews.sh` now permits deterministic recommendation sync after summary failure when all chunks completed and sidecar coverage is full; partial sidecars or summary failure combined with any failed chunk remain fail-closed. The summary-unavailable warning stays visible. `test-merge-findings.sh` pins both sides. | LADR-085 |
| 2026-09-18 | Added official OpenAI model IDs `gpt-5.6-sol` and `gpt-5.6-terra` to the `openai` provider. Names verified against OpenAI model catalog; `gpt-5.6-sol` is the flagship model ID (alias `gpt-5.6`), while `gpt-5.6-terra` is the balanced tier. | #142 |
| 2026-09-18 | **OpenCode Go endpoint roster refreshed and Responses API added.** Compared `assets/opencode.json` against official endpoint table and live `/zen/go/v1/models`. Added six missing Chat Completions models (`glm-5.3-flash`, `glm-5.3`, `longcat-2.0`, `deepseek-v4.1-flash`, `deepseek-v4-flash-vision-exp`, `hy4-preview`) and missing Messages model `qwen3.8-flash`. Added `go-responses` (`@ai-sdk/openai`) for Responses-only `grok-4.6`, `gpt-5.6-luna`, `muse-spark-1.3-contributor`, and `muse-spark-1.2-contributor`; it reuses the Zen `OPENCODE_GO_OPENAI_API_KEY`. Wired selector, provider-id, credentials, analyse/eval/local paths, cross-surface fail-fast validation, docs, and tests. Preset dropdown remains intentionally non-exhaustive. | LADR-027 amendment |
| 2026-09-18 | **DR-014 gets a `forbidden_claim`; it was the last fixture left in strict mode and that cost it 2 of 3 samples.** Strict mode fails a `must-not-flag` fixture on **any** Critical/High/Medium finding. DR-014's own docstring already says "defensive validation of the resolved connection-string value ... is INTENTIONALLY out of scope ... a flag there is NOT a DR-014 re-raise" — but that carve-out was prose the scorer cannot read, so the same true validation finding failed the fixture on run 35322499612 attempt 2 and run 35329091011 (clean only on attempt 3). The wrong claim proved expressible: an objection to LADR-10's chosen approach (discriminator column, global query filter / `HasQueryFilter`, shared-or-single database, database-per-tenant called unscalable or an anti-pattern, isolation asserted unenforced because it rests on connection-string selection). Validated from both sides before shipping: 7/7 synthetic re-raise phrasings match, and 0/5 adjacent true findings match — including the two verbatim shapes observed in production. Replayed against the archived reviews of all three runs: 3/3 PASS with the finding correctly reclassified as a non-blocking Unrelated finding. New `test-evals.sh` Case Q pins it from both directions plus asserts every shipped `must-not-flag` manifest has a claim and that every claim compiles as an ERE; mutation-tested — `forbidden_claim: "tenant"` trips the too-loose assertion and a never-matching pattern trips the too-tight one. Self-test 32 -> 36. `eval/AGENTS.md`'s strict-mode guidance rewritten: the mechanism stays for future fixtures, but no shipped fixture depends on it, and the unfailable-fixture hazard is now named as the thing Case Q exists to prevent. | |
| 2026-09-18 | **Two `must-catch` severity bars recalibrated: MC-004 CRITICAL → HIGH, MC-003 HIGH → MEDIUM.** Both fixtures failed **identically twice** on run 35322499612 attempts 2 and 3 (`gpt-5.6-sol` via the OPENAI provider, `EVAL_SAMPLES=1` each) while quoting the exact seeded line — a reproduced signature, not sampling noise, and distinct from `DR-014`, which re-raised a false positive in attempt 2 and not in attempt 3 and is therefore left alone as flaky. In both cases the reviewer **found** the defect and graded it one tier below the bar. **MC-004** (SQL injection via interpolated `FromSqlRaw`) was the corpus's only `CRITICAL` while **MC-005** grades a comparable security defect (hardcoded secret) at `HIGH`, so the bar was inconsistent with its own neighbour; Critical and High both produce `request-changes`, so the gate's blocking behaviour is unchanged by this. **MC-003** (reachable null-guard deleted) regresses the exception *type* and message quality on an input that already threw, not success-to-failure — materially milder than **MC-001**, where an always-null navigation property turns a working path into an unconditional throw; MC-001 keeps `HIGH` to preserve that distinction. Cost recorded in `eval/AGENTS.md` rather than left implicit: `MEDIUM` does not block, and LADR-042 selects autonomous `ai-analyse` fixes on severity, so an MC-003-class regression now sits inside the autonomous fixer's remit. Re-scored both recalibrated fixtures against the archived reviews from both attempts using `eval/lib/score-review.sh` — 4/4 PASS. `eval/test-evals.sh` 32/32 unchanged. The new guidance in `eval/AGENTS.md` requires a reproduced signature plus a named sibling fixture before any future bar is lowered. | |
| 2026-09-18 | **The retry attempt no longer reproduces attempt 1's budget split.** LADR-082 settled on the *same budget* for the retry and its own Consequences conceded the hole: a same-budget retry "spends another 820 s and fails identically" when a chunk is systematically under-budgeted. Consumer PR #72 run 35316140325 is that sentence: chunks 2 and 3 (`.agents/skills@1` 94 KB, `@2` 84 KB) timed out at `600 s primary / 268 s` and `600 s primary / 208 s`, and the sweep re-ran both prompts under both identical splits to identical exit-124 failures — 2 of 10 chunks lost, review posted fail-closed REQUEST CHANGES over a merged set of 0 Critical / 0 High / 1 Medium / 2 Low whose own synced recommendation was APPROVE. Diagnostic logs show the retry's primary managing **four** tool calls in 600 s (vs ~24 on attempt 1), so per-turn provider latency dominates and primary clock is the only useful lever. `review-in-chunks.sh` now exports `CHUNK_RETRY_ATTEMPT=1` around the sweep and `review_chunk` collapses the split for that attempt (`_primary_budget=$_chunk_timeout`, `_secondary_budget=0`) — the pre-LADR-081 single-wrap shape `split-chunk-budget.sh` already documents as its degenerate case, so the secondary returns as an in-chain fallback for fast failures. On those chunks the primary goes 600 s → 868 s (+45%) with **no** Variable change and no movement of the `CHUNK_TIMEOUT_MAX` ceiling; total wall clock per chunk is unchanged. Flag is `unset` after the sweep. LADR-081's floors govern attempt 1 and are untouched — the test asserts normal mode still yields `600 268`. Not fixed here and recorded as live: `PRIMARY_MIN_SECONDS = 600` was measured with **four** chunks actually concurrent (run 35081703390) while the shipped `OPENCODE_REVIEW_REPORT_MAX_PARALLEL` default is **7**, so the floor is calibrated for less contention than the default produces; also unfixed, opencode's `Glob` returns 0 matches for single-level patterns under dot-directories (`.agents/skills/.../scripts/*.py` → 0 while `Read` opens those files; `**/*mcp*` found only root `.mcp.json`), which costs an extra directory-`Read` per miss in a repo that standardises on `.agents/`. `test-review-notes-extraction.sh` part 3 evaluates the collapse block cut out of the shipped source (not a copy) and is mutation-tested. | LADR-084 |
| 2026-09-18 | **Skip Areas bullets now reach the model; the channel the Non-Negotiables call load-bearing was inert.** `review-in-chunks.sh` and `aggregate-reviews.sh` both carried `awk '/^## AI Review Notes/{flag=1; next} /^## /{flag=0} flag'`, which captures only that one section — `## Skip Areas / Known Issues` is a **sibling `##`** heading, so it terminated the capture instead of being included and never entered any prompt, while the chunk prompt told the reviewer that "items listed under **Skip Areas** MUST be treated as out-of-scope" for Critical/High/Medium. Verified against consumer PR #72's live body: the shipped awk yields **zero** occurrences of that PR's skip bullet. It never presented as a total failure only because `ai-review` appends its `### AI Review Response` table at `###` level *inside* the notes section, so the human-facing table was load-bearing and the documented channel was dead code — exactly inverted from the docs. New `lib/extract-review-notes.sh` (pure stdin→stdout, always exit 0, Bash 3.2-safe because `local-review.sh` reaches both call sites without a Bash ≥ 4 guard) captures both sections in either body order, emits the Skip Areas body **with** a heading containing the literal `Skip Areas` (the prompt rule matches that string), **demoted to `###`** so it nests under the prompt's `## 📝 AI REVIEW NOTES` block, and **last** so it sits adjacent to the rule naming it. Bodies without a Skip Areas section are byte-identical to the old awk — asserted against the old expression directly. Bullets alone now work too; previously an `## AI Review Notes` section had to exist. `aggregate-reviews.sh`'s summary prompt gains the same out-of-scope rule the chunk prompt had: a holistic 🔴/🟠 has no per-chunk sidecar to demote it and blocks on its own. Suppression stays **model-trusted prose**, not deterministic filtering. New `test-review-notes-extraction.sh` (25 offline assertions) pins the behaviour and greps both call sites so neither can drift back to a local one-liner. | LADR-083 |
| 2026-09-16 | **A failed chunk now gets exactly one retry before the report is built, and LADR-081's stale floor is recalibrated.** Part A (hotfix — the bad floor was released as `f68ed42` / PR #134 and the `v1` tag moved onto it): `split-chunk-budget.sh`'s `PRIMARY_MIN_SECONDS` was 330, justified by a stale "299 s slowest success" copied from prose; consumer run 35081703390 (700 s base, max 7 parallel) has chunks **succeeding at 550 s and 563 s**, so the 455 s primary the old floor produced at that base would have killed both and turned a 1-failure run into 3. Floor is now **600 s** (measured 563 s ceiling + headroom, run ID in the lib), which moves the split's switch-on point 480 s → **750 s** and makes the lib correctly *refuse* to split at a 700 s base — refusal over starvation is the lib's design, and the retry sweep below becomes the rescue at common budgets. Part B (LADR-082): after the parallel chunk loop and before the report, `review-in-chunks.sh` sweeps `ci_temp/reviews/chunk_<n>.failed` flags (never exit codes — `review_chunk` always exits 0) and re-invokes `review_chunk` itself once per failed chunk, ascending index, max 2 concurrent (hardcoded), same prompt-scaled budget, skipping entirely (and saying so) when **all** chunks failed. Before each retry it deletes the stale flag, failure-marker `chunk_<n>.md`, `findings.json` and `findings.rejected.txt` — `review_chunk`'s success path never removes a pre-existing flag, so without the cleanup a rescued chunk would still fail-close the review while the logs looked correct. The failure count is recomputed from the flags post-sweep. No new env vars, no workflow YAML changes. Motivating runs: PR 63 run 34961934908 (2/9 failed), PR 65 run 35011956699 (2/5), and the `/ai-review` re-run 35081703390 that still lost 1/4 — a re-run is strictly more work than a retry and did not converge. `test-review-chunk-threshold.sh` gains a retry-sweep runtime harness (mode-driven stub: pass/flaky/flaky-sidecar/dead) pinning rescue, double-failure fail-closed preservation, exactly-one-retry, never-retry-passing, all-failed skip, ascending order, ≤2 concurrency, and the stale-artifact trap; the LADR-081 split-point assertions moved with the floor. Review follow-up in the same change: the skip requires **more than one chunk** — single-chunk mode is the default for PRs at or under the 10-file threshold and still reaches the sweep, so treating its lone failure as "all chunks failed" left the feature inert for most small PRs (probed: one invocation, flag kept, sweep skipped) — and retry concurrency is `min(2, MAX_PARALLEL)` so a consumer who lowered the parallel cap to protect a contended endpoint is not overridden. | LADR-081/082 |
| 2026-09-16 | **The per-chunk budget is split across the review chain, so a timeout no longer starves the LADR-002 secondary.** Three LADRs (065, 076, 077) had each recorded that a single `timeout` around `lib/opencode-with-fallback.sh` means exit 124 = "the primary ate everything and the secondary was never invoked", and each named the budget split as the deterministic follow-up without doing it. Motivating runs: consumer PR 65 run 35011956699 lost 2 of 5 chunks with a healthy `deepseek-v4-pro` secondary configured and never called (fail-closing a PR whose reviewed chunks were clean and whose recommendation was APPROVE), and PR 63 run 34961934908 lost 2 of 9 identically — the `.github` chunk was **54 KB / 2 files** and still burned its full 700 s, which against a 43 KB→113 s / 68 KB→299 s envelope is a stuck model rather than a slow one. New `lib/split-chunk-budget.sh` prints `<primary> <secondary>`; `review-in-chunks.sh` bounds stage 1 by the primary share and stage 2 by **what is left of the total**, so a primary that times out hands over exactly the reserve while one that fails fast hands over nearly everything — and total wall clock is unchanged (`elapsed + (total - elapsed) = total`), preserving the ceiling's deadlock-detector property. The split is **conditional**: floors of 330 s primary (above the 299 s slowest-ever *success*) and 150 s secondary mean it switches on at **480 s**, so the **default 450 s configuration is byte-identical** and only consumers who already raised the base get the new behaviour. Also fixed: the 124 marker interpolated the *unscaled base* Variable, so a chunk killed at its scaled 850 s reported ">700s" and advised raising a number the run had already passed; it now reports the resolved budget and says honestly whether both tiers were tried. Does **not** stop the primary wasting its own turn (LADR-076's prompt rules remain the lever), does not help when both tiers share a model, and cannot rescue a dead endpoint. `test-review-chunk-threshold.sh` grew 44 → 92 assertions, including a runtime case that drives the real gate with a stub chain whose primary exits 124 and asserts the secondary's review is what gets written with no LADR-031 flag left behind. | LADR-081 |
| 2026-09-15 | **Parallel chunk cap is consumer-tunable via `OPENCODE_REVIEW_REPORT_MAX_PARALLEL`, and the default drops 10 → 7.** `review-in-chunks.sh`'s `MAX_PARALLEL` capped concurrent chunk reviews but was never wired through either workflow packaging, so a consumer could not reduce endpoint contention. Motivating run: `smooth-ai-product-context-memory` PR 63 run 34949307952 — 8 chunks fired in parallel at glm-5.2 during provider degradation (orchestrator probe failed, grouping call timed out), and the three tool-call-heavy chunks (0/2/4) burned their whole scaled budgets mid-exploration and exit-124'd, forcing fail-closed REQUEST_CHANGES on a PR whose reviewed chunks were clean. New Variable, default `7` (10 is a known contention hazard), declared in **both** `pipeline-code-review-report.yml` and `.docs/examples/code-review-local.yml`; precedence `OPENCODE_REVIEW_REPORT_MAX_PARALLEL` → bare `MAX_PARALLEL` (kept as a fallback for existing local callers) → 7; non-integer/<1 falls back to 7. | |
| 2026-09-13 | **The `instructions` globs went one level deep, and the whole channel is opencode-v1-only.** `.agents/rules/*.md` and `.github/instructions/*.instructions.md` matched a single directory, so a consumer splitting rules by area (`.github/instructions/backend/…`) had them silently unloaded, and `.agents/rules-scoped/` was absent from the array even though the gate's own default `MANDATORY_CONTEXT_FILES` points there. Added `**` companions (`.agents/rules/**/*.md`, `.github/instructions/**/*.instructions.md`, `.agents/rules-scoped/**/*.instructions.md`) alongside the single-level entries — `**` is undocumented for v1's glob engine, so a non-matching entry is a no-op and both shapes are kept. Separately: opencode **v2** accepts `instructions` but resolves nothing ("V2 does not currently resolve its files, glob patterns, or URLs"), so bumping `OPENCODE_CLI_VERSION` across the major would kill LADR-070 with no error — `prepare-opencode-config.sh` now warns (never fails) on a v2+ binary carrying a non-empty array. `find-context-files.sh` is unchanged and stays load-bearing: v1 loads the root `AGENTS.md` only, and nothing but the context-file channel can scope context per chunk. | LADR-080 |
| 2026-09-13 | **A mangled END sentinel is no longer treated as truncation — prefix-match the closer, keep BEGIN exact.** Consumer run 34764534601 (`smooth-ai-product-context-memory` PR 38, glm-5.2) lost **all 3 sidecars** with complete, valid JSON in every LADR-077 rejected.txt: chunk 0 closed `<!-- FINDINGS_JSON_END →` (U+2192), chunks 1–2 `<!-- FINDINGS_JSON_END>` (dropped `--`). Exact-match awk never fired, logged the misleading `truncated mid-block`, then jq rejected the mangled closer as the payload's last line. Distinct from LADR-064 cut-off — the JSON was whole; only the HTML comment was normalized. `extract-findings-json.sh` now accepts any trimmed line starting `<!-- FINDINGS_JSON_END` as END; BEGIN stays exact so a quoted opener cannot forge the channel. Trailing `^[[:space:]]*<!--` lines are peeled last-lines-only before jq. Diagnostics split: no END-ish line → keep `truncated mid-block`; END-ish but malformed → `end sentinel present but malformed (matched by prefix)` in both the log and rejected.txt `sentinel_state`. jq remains the final gate; the sidecar still never writes `chunk_<n>.failed`. `test-merge-findings.sh` 10t–10z pin both observed shapes, genuine truncation, the diagnostic split, inline END quotes, the fenced self-review quote, and trailing-comment peeling. | LADR-079 |
| 2026-09-13 | **A transient GitHub 5xx no longer discards a completed review — every abortable `gh` call gets one bounded retry.** GitHub's write path degraded for ~40 minutes with nothing on its status page (`updated_at` stuck at 07:44, `incidents/unresolved.json` empty throughout) and killed four runs across two repos, all on writes: `smooth-ai-product-context-memory` PR 18 at 08:48 (six `minimizeComment` GraphQL 500s, then `gh pr review` → `non-200 OK status code: 502 Bad Gateway`), and this repo's PR 129 at 09:10 and 09:19 (`gh pr review` → `GraphQL: Something went wrong while executing your query`, refs `1C04:238DB6:17E2E3F:4DA1E11:6AA66894` and `AC42:6FB4A:176543E:4C4F5B6:6AA66A9F`). All three review-create failures hit runs that had **already finished reviewing** — body assembled, verdict computed (`APPROVE`, 12,616 chars at 09:19), artifact uploaded — so ~25 minutes of model spend was thrown away by one unretried mutation, three times. The failure was GitHub's; the amplification was ours: `run-review.sh` runs under `set -euo pipefail` with every `gh` call unguarded. New sourced `lib/gh-retry.sh` wraps all 12 previously-unguarded calls (9 in `run-review.sh` — 7 writes plus the two idempotent `PR_JSON` reads that could kill a run in Step 3 before any model spend — and 3 in `local-review.sh`): **one** retry after a fixed 30s, then fail as before. Retries only on an **allowlist** of HTTP-status phrasing and network signatures, so a `422`/`403` still fails fast and bare numerics like the `per_page=500` in the gate's own URL cannot trigger a spurious retry of a 404; writes first re-read a **count** of gate-authored reviews/comments and skip re-posting when it increased, a delta being chosen over a `submitted_at` window because a delta has no spurious-"landed" mode and a false "landed" would lose the review silently. Both counters fail toward retrying for the same reason — a duplicate is visible and self-heals via minimize, a silence does not. Gated by `OPENCODE_REVIEW_REPORT_ENABLE_GH_RETRY` (default `1`) in **both** workflow packagings. Two traps pinned by test rather than vigilance: the lib must not shadow the EXIT trap's `_rc` global, and lowercasing uses `tr` not `${v,,}` — on macOS Bash 3.2 the latter is a "bad substitution" whose failure read as "retry disabled", which would have left the feature silently dead on every local run. Does **not** retry `minimize-previous-reviews.sh` (already tolerant by design), and cannot help the CodeQL code-quality upload (GitHub default setup, no config here, no API retry path). `test-gh-retry.sh` pins it with 34 offline assertions. | LADR-078 |
| 2026-09-13 | **Narration is no longer accepted as a chunk review, rejected sidecars keep their payload, and zero-match globs are fail-fast.** Consumer run 34745786660 (`smooth-ai-product-context-memory` PR 18, glm-5.2, 6 directory chunks) degraded in three places, only one of them visible: chunk 1 (`.agents`) returned 733 B of between-tool narration and no review — over the 200-byte floor, so it logged `✅ … completed`, counted in `total_chunks`, left `failed_chunks: 0`, skipped the LADR-002 fallback and posted the narration as the report's `### Chunk 1` section; chunks 2 and 3 lost their sidecars to `truncated mid-block` (3/6 merged coverage) with the rejected payload discarded, leaving the artifact unable to say whether the JSON was cut off or merely closed in an unaccepted shape; and all three affected chunks were the three documentation-only ones, each having first burned its turn re-globbing `.docs/**`, `.docs/**/*`, `.docs/adrs/*` — dot-prefixed directories that `glob` reports as 0 matches and that the model then read successfully. Fixes: `chunk_review_has_shape()` sends output with no severity emoji, no `… Priority`, no `None found` and no heading down the LADR-031 fail-closed path (flag now names which reason fired; error-log label `_empty` → `_no_review`); `extract-findings-json.sh` writes every refused block to `chunk_<n>.findings.rejected.txt` (16 KB cap, reject reason + sentinel state, outside the merge glob, shipped by LADR-062's artifact copy); and a MANDATORY zero-match-glob fail-fast prompt rule beside LADR-076's exploration budget. Does **not** recover the lost chunk — detection happens after the chain returned success, so the fallback still does not get its turn (LADR-066 remains the deterministic follow-up). | LADR-077 |
| 2026-09-13 | **The `instructions` array now also loads the pre-`.agents/` rule layout.** `assets/opencode.json` listed only `.agents/rules/*.md`, so a consumer whose rules live in `.github/instructions/` (plus `docs/ADR/AGENTS.md`, `docs/hlds/AGENTS.md`) needed a full `OPENCODE_REVIEW_REPORT_CONFIG` / `opencode_config` override just to get them into the system prompt. Those four entries — taken verbatim from `LEGO/gf-recipe-editor`'s own override — are now in the shipped asset alongside the canonical glob. Non-matching entries are a silent no-op, so nothing changes for a repo on the `.agents/` layout; a repo where `.agents/rules` symlinks to `.github/instructions` may see the same files loaded twice, and a chunk whose changed files sit under `docs/ADR/`, `docs/hlds/` or `.github/instructions/` gets those `*AGENTS.md` files injected twice as well — prepended by the `instructions` array and listed by `find-context-files.sh`'s upward walk — costing duplicated tokens per affected chunk with no correctness impact. | LADR-070 |
| 2026-09-12 | **Chunk prompt gains a webfetch fail-fast rule and an exploration budget.** Two consecutive full-review runs on consumer PR 17 (`smooth-ai-product-context-memory`, runs 34707977945 and 34710614711) each lost one chunk to exit 124 — the whole chunk budget consumed by the review model *exploring*, so the LADR-002 secondary was never invoked and the gate fail-closed a PR whose reviewed chunks were clean. Two distinct routes, both prompt-shaped: a webfetch 404 loop researching secret/token formats (retrying alternate GitHub-docs URLs after each 404), and a zero-webfetch local sprawl (~35 read/grep calls cross-verifying a docs chunk against the whole `src/` tree, never reaching "write the review"). Two new MANDATORY prompt rules in `review-in-chunks.sh`: webfetch fail-fast (a failed fetch is never retried and never rerouted — tag `[SPECULATIVE]` and move on; hard cap 3 webfetch/websearch calls total per chunk (this review session); never fetch external docs for secret/token patterns, local `grep`/`read_file` only; restated in Critical/High workflow step 4 as "one fetch attempt per claim"), and an exploration budget (~20 tool calls; at budget stop exploring and write the review with evidence in hand, tagging the unverified `[SPECULATIVE]`). Both are zero-cost escapes via the LADR-012/015 grammar, so nothing is blocked — only the research loop is capped. SKILL.md documents the fail-fast rule. Not fixed here: the timeout still wraps the whole model chain (see LADR-066 for the deterministic follow-up shape) and glm-5.2's recurring sidecar truncation (LADR-064). | LADR-076 |
| 2026-08-28 | **Verdict and Issues Summary now share one post-validation finding set (issue #125).** A FULL review posted `REQUEST_CHANGES` while every severity section said "None found": the chunk emitter omitted `requires_verification` on two confidence-100 `[VERIFIED]` findings, `merge-findings.py` dropped them as `requires_verification: missing`, the Issues Summary rendered from the empty merged set, and the Recommendation/verdict still counted the orchestrator's pre-drop Issues Summary (1 High + 1 Medium). Two fixes, both load-bearing: (1) **schema drift** — `requires_verification` is no longer hard-required; absent defaults to `false` (same conservative reading as `verified`); present-but-wrong-type still rejects; the chunk prompt now explains the field. (2) **single source of truth** — when the Issues Summary is rendered from merged findings, `lib/sync-recommendation-from-findings.sh` rewrites Recommendation counts, rationale, and `MACHINE_READABLE_ACTION` from that same document so severity lists and the posted review state cannot disagree. Holistic Critical/High (no per-chunk sidecar) still block; failed chunks still fail-closed (LADR-031/036). `test-merge-findings.sh` 24a–24i + 25a–25k. **Hardening pass (same issue, after review of the first cut):** the sync can *soften* a verdict, so it now runs only where softening is evidence — `aggregate-reviews.sh` skips it on **partial** sidecar coverage (a truncated sidecar leaves a real Critical absent from the merged set while it sits verbatim in Part 2) and when the **orchestrator summary failed** (its fallback template's `REQUEST_CHANGES` is a fail-closed guard, and rewriting it to `APPROVE` inverted it); both cases keep the orchestrator's Recommendation plus the one-directional escalate-only override. A deliberate `COMMENT` is preserved via `SYNC_ORIGINAL_ACTION` rather than upgraded to a real GitHub `APPROVE`. The holistic backstop now treats a list item as content before testing it as a subsection header — `- **H1)** 🔴 Critical: …` was being consumed AS a heading, counting zero blockers. Count rewriting replaces only the number, so `**bold labels:**`, trailing notes and unfilled `[number - …]` placeholders survive (a bold label previously kept its stale count, and the pre-existing line was mangled into a duplicated number). `## 📝 Suggested Fixes` cannot be synced (orchestrator prose, no deterministic mapping back to a finding), so `lib/annotate-suggested-fixes.sh` states the gap with counts instead. Also fixed the harness that hid all of this: `test-merge-findings.sh` aborted at Test 17d on a brace-expansion bug (`{$var,"k":v}` expanded into two `doc` calls → unparseable JSON → `merge-findings.py` exit 2 → `set -e`), so Tests 18–25 never ran; the suite now also shouts when it ends early. 221 assertions, +26a–26p and +27a–27j. | issue #125 |
| 2026-08-10 | **The gate was reviewing other people's merged commits whenever the base branch advanced.** Reported by a consumer against v1 (`b4033e8`, upstream #120) and reproduced on `LEGO/gf-recipe-editor` PR #197 (review `4897240072`, head `190a991`): a 4-file documentation PR was reviewed as **34 files**, chunked into 6, and got four High findings — every one about an already-merged PR, read backwards ("the current diff **disables**…", "the **removed** draft-fork behavior"). Three compounding faults in one two-line idiom repeated at four sites in `run-review.sh`. `git fetch upstream "${base_ref}" --depth=1` writes a shallow graft **into a repository that was already complete** (the gate checks out with `fetch-depth: 0`), `git merge-base` cannot walk past it and exits non-zero, `\|\| echo "$base_sha"` substitutes the base **tip**, and the **two-dot** `git diff` — equivalent to three-dot only when the left side is an ancestor of the right — then spans the divergence in both directions. It passes whenever the branch is up to date with its base, so it only bites a PR left open while something else merges. The wrong base propagated into `changed_files.txt`, the `pr_diff.txt` body fed to the model, `review-in-chunks.sh`'s chunk boundaries, `validate-agents-md.sh`, and `detect-changes-graph.sh`'s risk scoring. All four sites now route through the new `lib/resolve-diff-base.sh`: fetch with ancestry (never `--depth=1`), repair an existing graft via `--unshallow` then `--deepen` 100/1000/10000 across both remotes, re-test after each pass, memoise — and on exhaustion emit an actionable `::error::` and **return 1** (every call site is `\|\| exit 1`). Two corrections to the reporter's proposed patch, both load-bearing: dropping `--depth=1` from the base fetches alone fixes nothing, because a plain fetch does **not** un-shallow a repository and Step 6 shallow-fetches the PR *head* on every `/ai-review` re-run; and widening to three-dot is **not** a safe degradation, because `git diff A...B` errors on a shallow repo and `pr_diff.txt`'s `2>/dev/null \|\| true` would swallow that into an empty diff — a review of nothing that still posts. New `test-diff-base.sh` (15 assertions, no network): Test 2 reproduces the old broken scope so the fixture is proven to exercise the bug, Test 4 is the oracle (exactly the PR's files, where the old code returned two and said nothing), Test 12 pins why three-dot was rejected, and 13–15 grep `run-review.sh` so the idiom cannot be pasted back in. The reporter's secondary `copilot-review.sh` finding does not reproduce here — this repo's copy has no `describe` subcommand, so that consumer is on a divergent fork of the `ai-review` skill. | LADR-075 |
| 2026-08-10 | **`test-select-ai-analyse-artifact.sh` was reporting a broken fake as a broken selector.** The suite failed 16 of 26 assertions on macOS, every one of them with `no gate-authored OpenCode review found`, and had been written off as "requires a newer bash". The cause was one line in the test's own fake `gh`: `endpoint="${args[-1]}"`. Negative array subscripts need bash 4.3, so under macOS's bash 3.2 the expansion raises `bad array subscript`, the endpoint comes out empty, every fixture read falls through to the `[]` branch, and the helper legitimately reports that it found nothing. Replaced with `${args[$((${#args[@]} - 1))]}`; the suite is 26/26 green with no change to the code under test. This matters beyond tidiness — the LADR-042 selector is what LADR-073 relies on to keep `ai-analyse` auto-fixing incrementals, and its case 11 (a trivial-skip comment must not become the selected artifact) had been silently unverified. | |
| 2026-08-10 | **The startup model probe now matches generic server errors.** A consumer gate posted `Review coverage incomplete: 4 of 4 chunks failed to review` fail-closed twice in an hour (PR #110 on `generic-automation-and-it/smooth-llm-imposter`, runs 31389552206/31388689391) even though the Step 5g probe printed `✓ Primary review 'glm-5.2' works correctly` — the probe's actual response was a gateway 500 (`UnknownError: Unexpected server error`) sharing no token with `ERROR_PATTERN`'s quota/exhausted/40x/auth vocabulary, so it passed and every real chunk call then died on the same 500. `run-review.sh`'s `ERROR_PATTERN` (shared by the two review-tier greps and the background orchestrator probe) gains `UnknownError\|Unexpected server error`. A dead gateway now falls through to the secondary and, on double failure, to `all_models_failed` (LADR-021 soft-fail), which names the provider outage instead of implying the PR broke the chunks. Fail direction unchanged — the pattern only widens what counts as "model unavailable", so no previously-valid run changes behaviour. | LADR-074 |
| 2026-08-10 | **A full review now minimizes every issue comment this pipeline leaves on the PR.** `minimize-previous-reviews.sh` matched only three comment shapes (ai-analyse summaries, LADR-059 trivial-skip, Step 16 blocked-incremental), so other review-report messages — notably the workflow's `## ❌ OpenCode CLI Code Review Workflow Failed` error comment, and any future gate comment shape — stayed on the PR forever. The selector is now header-driven: any comment whose first line is the gate header `^#+ 🤖 (Gemini CLI|OpenCode CLI) Code Review` (covers the trivial-skip and blocked-incremental notices), the failure header `^#+ ❌ OpenCode CLI Code Review Workflow Failed` (the error comment; anchored `^#+` for parity with the other two), or an `ai-analyse auto-fix` header is minimized. Formal reviews — including the AGENTS.md-validation BLOCKED review, which is a `gh pr review` subject — were already covered by the reviews path and still are. The narrow blocked-incremental exclusion is lifted: ai-analyse reads the REST timeline and never looks at `isMinimized`, so minimizing cannot alter its incremental cap count. Header anchoring at `^` keeps quoted copies from matching. Two guards make the widening safe in ways a per-shape match did not need: `.isMinimized != true` (the old selector matched at most one or two notices, so re-minimizing was free — a header match sees every gate comment the PR ever accumulated, and without the guard each full review would re-fire `minimizeComment` across the whole history and report an invented "Minimized: N"; `!= true` so a null field degrades to "minimize it", never to "skip everything"), and `.viewerDidAuthor` on the two gate headers (body-only matching would hide a *human's* comment that opens with the gate header — keyed on the authenticated identity rather than a hardcoded `github-actions[bot]` so it holds for gates posting under a PAT or GitHub App). The ai-analyse branch stays author-agnostic: it is posted by a different workflow and its behaviour is unchanged by design. `test-minimize-reviews.sh` Test 6 now **extracts** the selector program from the script and runs it against a fixture document — no mirrored copy to drift — asserting the three headers match and that quoted copies, foreign authors and already-minimized comments do not; it also asserts both guard fields are requested by the GraphQL query, since a missing field would silently minimize nothing or everything. Both queries also switch `first: 100` → `last: 100`: they are unpaginated and GitHub returns reviews and issue comments oldest-first, so past 100 items `first` fetched the window earlier runs had already minimized and never reached the clutter a reader actually sees. The same two guards and the same window fix are applied to the **reviews** branch, which had carried neither: `PullRequestReview` implements `Minimizable` (verified against the live schema — possibleTypes are CommitComment, DiscussionComment, GistComment, IssueComment, PullRequestReview, PullRequestReviewComment), so `isMinimized`/`viewerDidAuthor` are available and mean there what they mean for comments, and without them a long-lived PR re-minimized its entire review history every run and a human review opening with the gate header could be hidden. ai-analyse/ai-review skill postings remain handled by their own branches and are out of scope for the review-report gate rule. | |
| 2026-08-10 | **Incremental reviews are now a bare delta — header banner, Versions block and narrative overview all dropped.** An incremental re-reviews only changes since the last full review, yet the posted body still carried the full-PR recap: the OpenCode ASCII banner, the `📦 Versions` update notice (LADR-053), the `**Model:**`/`**Files Excluded:**` lines, plus `## 📋 Overall Summary` and `## ✅ Positive Highlights`. `aggregate-reviews.sh` gates all six on `REVIEW_TYPE != "incremental"` — the banner now renders from its own quoted heredoc (full-only, same position), the version/model/excluded lines are skipped (the chunk count is not), and `pr_summary_main.md` is run through an awk strip (before the cat into `final_review.md`) that removes the two overview sections by level-2-heading range (nested sub-headings suppressed too). An incremental header is now `## 🤖 …` → `**Review Type:** INCREMENTAL` → `**Changes Since:**` → `**Files Changed:**` → `**Reviewed in:**`. `**Reviewed in:** N chunks` is deliberately kept for both review types — it is coverage information, not recap (how much of the delta actually got reviewed), and it is the one header line an incremental reader cannot reconstruct, since the coverage banner only prints when a chunk failed; `**Model:**` is recap and stays full-only. `test-incremental-body.sh` test 7 asserts that split on the source, because the two lines are adjacent and were originally one guarded heredoc. Full-review bodies are byte-identical. The heading scan is fence-aware (mirrors `lib/balance-fences.sh`): the aggregation prompt requires the literal string `## 📋 Overall Summary` in the report, so a review quoting that contract inside a code block would otherwise open a strip range on the quoted line and delete the rest of Suggested Fixes — and a fenced `## ` inside a dropped section would close the range early and leak the recap. `test-incremental-body.sh` (new) extracts the awk from the script and pins both hazards plus the tolerant no-op and the `REVIEW_TYPE` guard. Tolerant of a missing either heading, and — load-bearing — `## 🔍 Issues Summary` is untouched: `select-ai-analyse-artifact.sh` (LADR-042) classifies actionable artifacts on it, so `ai-analyse` keeps auto-fixing incrementals. `## 📝 Suggested Fixes` and `## 🎯 Recommendation` unchanged; the LADR-030 aggregation LLM call still runs for every PR, this only edits the rendered body. | LADR-073 |
| 2026-08-06 | **A finding dropped by the sidecar merge now says which rule it broke.** A consumer review posted `🔵 Low Priority: None found` while its own Recommendation counted one Low and Suggested Fixes printed it — the markdown transport carried the finding and the sidecar entry did not, because its `confidence` was `70`, off-anchor, failing validation whole. Coverage reported `Malformed and dropped: 1 finding(s)` and stopped there, naming the loss and nothing actionable about it; recovering the cause meant downloading the LADR-062 artifact and diffing the raw sidecar against `valid_finding` by eye. `merge-findings.py`'s two boolean predicates become `finding_defect`/`return_defect`, returning a reason keyed on (field, rule) so a Counter aggregates to one entry per **cause**; `render-findings-summary.sh` renders them as sub-bullets under the unchanged count bullet and `merge-findings.sh` logs the same set to CI. Reason text reaches a posted body, so quoted values go through `safe_value` (LADR-067 autolink strip, whitespace collapse, 32-char cap) and the rendered list is capped at six causes with an explicit overflow line. Counts, validation rules and merged documents are otherwise unchanged; a document predating the feature still renders. | LADR-072 |
| 2026-08-05 | **`.agents/rules/*.md` in the repo under review is now loaded, via opencode's own `instructions` field.** A consuming repo's `.agents/rules/` tree reached the model not at all: the gate's only two context sources are discovered `*AGENTS.md` and the exact-path `MANDATORY_CONTEXT_FILES` list, neither of which globs. Extending that list was rejected — it is word-split (no globs), it would have to be enumerated in both workflow packagings, and it supplies paths with a "call `read_file`" instruction the model can silently skip. `assets/opencode.json` gains `"instructions": [".agents/rules/*.md"]` instead, so opencode expands the glob itself and prepends each file's **content** to the system prompt of every call — deterministic, and no workflow edit. Confirmed against opencode's resolver before adopting, since the config installs to the global location and a config-relative glob would have matched nothing: relative `instructions` entries resolve via `globUp(entry, ctx.directory, ctx.worktree)`, i.e. the repo under review. `setup-opencode-config.sh`'s `is_ours` jq guard gained the new key in the same commit — without it the managed config stops being recognized locally and `_inject_base_urls` is skipped. Opt out by overriding the config via `OPENCODE_REVIEW_REPORT_CONFIG` (LADR-047). | LADR-070 |
| 2026-08-05 | **The managed opencode config is now delivered via the native `OPENCODE_CONFIG` env var.** `setup-opencode-config.sh` (global install, `is_ours` clobber guard, self-heal) is replaced by `prepare-opencode-config.sh`: resolve source (LADR-047 override honoured unchanged), copy to a run-local path, inject LADR-034 baseURLs into the copy, export `OPENCODE_CONFIG` (absolute), and append it to `$GITHUB_ENV` so the analyse workflow's later steps still see it. A stale pre-change managed global config is detected by its exact old shape and moved to `*.pre-ladr-071.bak` — per-key config merging would otherwise let its stale injected baseURL survive underneath ours. Personal global configs are left in place and merge below ours. Amended same day: the first version set file-scope `SCRIPT_DIR`/`REPO_ROOT`, which — because the lib is sourced — shadowed `run-review.sh`'s `SCRIPT_DIR` and broke the gate with exit 127 at `find-context-files.sh` (run 31011726946); all lib state is now function-`local`, with source-safety tests in `test-run-review.sh`. | LADR-071 |
| 2026-08-05 | **Analyse workflow survives the LADR-071 script rename in both skew directions.** `pipeline-ai-analyse.yml` is `workflow_run`-triggered, so its YAML always executes from the default branch while the scripts come from the analysed PR's checkout — main's YAML calling the deleted `setup-opencode-config.sh` against a post-rename checkout died with exit 127 (run 31013229958). "Initialize OPENCODE" now dispatches on whichever config lib exists: source `prepare-opencode-config.sh` when present, else run the old installer (covers stale pre-rename PR branches after merge). Also sandboxed `HOME` in the second source-safety test so a local suite run cannot move a developer's real stale global config aside (gate review on 28c0086, Low finding). | LADR-071 |
| 2026-08-04 | **Eval precision now uses a majority of samples, judged per sample.** Run 30891074256 failed the required check with `DR-002` re-raising and `MC-003` missing — both flipped clean on a rerun of the same commit and model, i.e. a noisy draw at `EVAL_SAMPLES: 1`, not a regression. Raising samples was the obvious fix and would have made it worse: multi-sample precision was judged against the **last** sample only, so for the 13 of 14 fixtures with a `forbidden_claim` a 2-of-3 re-raise ending in a clean sample reported PASS and threw the regression away, while the one claimless fixture (`DR-014`) tripped on ANY sample and grew flakier per sample. Now: the claim is matched per sample, precision fails at `majority(EVAL_SAMPLES)` symmetric with recall, a minority re-raise passes but reports itself as a flaky PASS naming the hit count, and the triage artifact is the first offending sample rather than whatever ran last. Inert at the default of 1; `vars.EVAL_SAMPLES` added so the count is retunable without a commit, default unchanged because cost is linear in samples. New self-test seam `selftest-review.<N>.md` plus cases O/P — the discriminating one is 2-of-3 with a clean tail, since 1-of-3 passes under both old and new code. | LADR-069 |
| 2026-08-03 | **The holistic legend now names the findings shape of the path it ships beside, and that branch is tested.** LADR-068 moved the primary path to `1.` and left the legend asserting `1)`, so one posted body contradicted itself on the identifier shape — caught by the gate on PR 115, not by this suite. `ai-analyse` made the legend conditional on `FINDINGS_SUMMARY_APPLIED` (primary `1.`, fallback `1)`); the fallback deliberately keeps the literal form because that summary is model prose whose per-section numbering is not guaranteed contiguous, so an ordered list there would silently renumber. It could not add the test — LADR-045 bars it from touching tests — which is the gap this closes. New test 22a-h lifts the conditional out of the script and **executes** it under both values plus unset, so a future edit to the branch logic fails rather than a future edit to its wording. Verified against both the pre-fix hardcoded legend (22a) and inverted branches (22b-e, 22h). The gate had also observed that tests 20b/20g passed vacuously against this drift, which was accurate: 20g greps the legend for `H1)`, which never stopped matching. | LADR-068 |
| 2026-08-03 | **Findings render as an ordered list.** `- **1)** …` was a bullet AND a number for what is simply an enumerated list; findings are now `1.`, `2.` with the `why_it_matters` continuation indented 3 spaces to the ordered-item content column. The four prefixed classes (`R1)`/`T1)`/`P1)`/`H1)`) stay bolded bullets — a letter-prefixed token cannot be a list marker. Safe for one non-local reason: CommonMark takes an ordered-list start number from its FIRST item and disregards the rest, so a section holding a non-contiguous subset would silently renumber; it cannot, because `merge-findings.py` sorts on severity first and numbers only after suppression/partitioning. That makes the renderer correct only while **a sort key in a different file** leads with severity, so test 21c pins it by driving the real merge over scrambled input whose filenames sort differently from their severities (an earlier fixture where the two orders coincided passed against a deliberately broken sort). `filter-failing-test-findings.sh` moved with it — its item-start regex matched `- ` only, so ordered findings collapsed into one item and the LADR-056 failing-test guard became a silent no-op; its test now extracts that regex from the helper instead of restating it. | LADR-068 |
| 2026-08-03 | **Item identifiers collided with GitHub's issue autolinks.** GFM autolinks `#`+digits to an issue/PR in the repo the review is posted on, and `**` does not suppress it — so `- **#1** 🟡 [VERIFIED] Medium Priority: …` rendered as a link carrying issue #1's title, observed in production as a finding bullet beginning `chore: Initialize projects and folders with basic structure and configuration #1`. Beyond the unreadable bullet: every run left a permanent cross-reference on the low-numbered issues of the repo under review, and the identifier that `ai-review`'s Skip Areas bullets and `ai-analyse`'s FIX/SKIP table match on was no longer present as literal text. `(chunk #3)` and the `### Chunk #3` headings shared the defect. All five sequences moved to a trailing paren — `1)` findings, `R1)`/`T1)`/`P1)`/`H1)` for the prefixed classes (which never autolinked, but moved together so no consumer has to remember which is safe) — plus `(chunk 3)` and `### Chunk 3` (anchor slug unchanged; GitHub strips `#` when slugifying). Identifiers are always emitted **bolded**, because `1)` is also a CommonMark ordered-list marker and `- 1) foo` parses as a nested list that swallows the number. Backticking the old shape was rejected: the identifier travels through two skills into PR bodies, so its safety would have depended on every hop remembering to quote it. New test 19 asserts no `#<digit>` survives anywhere in a rendered summary. | LADR-067 |
| 2026-08-03 | Eval harness: retired the post-merge push-to-main canary — the `pull_request` required check now scores the same paths before merge; merged fork PRs need a manual dispatch. | LADR-033 |
| 2026-08-03 | LADR-066: background orchestrator probe (failed probe reroutes grouping/aggregation to the review model); semantic-grouping 60s budget split 35s/25s across the chain; code-review-graph pipx venv cached in both packagings; minimization parallelized (window 4, sleep removed). | LADR-066 |
| 2026-08-03 | **`intersect-changed-files.sh` no longer depends on `/dev/fd`.** It was the last remaining process substitution in the shipped scripts — `comm -z -12 <(sort -z …) <(sort -z …)` — while `merge-findings.sh` and `run-test-gate.sh` both already carry comments explaining why they avoid it. `/dev/fd` is absent on Git Bash, on some container images, and in the sandboxes this repo is developed in, where `cat <(echo hello)` fails outright. Because it sits on the production **incremental** review path (`run-review.sh`) under `set -euo pipefail`, the failure was fail-closed — the review aborted rather than silently reviewing zero files — but it also made `test-run-review.sh` red in every such environment (57 passed / 0 failed after the fix; the suite could not complete before it). Replaced with two `mktemp -d` temp files, mirroring the pattern already documented in `merge-findings.sh`. NUL delimiters verified preserved end-to-end. | — |
| 2026-08-03 | **Unescaped backticks were silently deleting text from the chunk prompt.** The prompt heredocs are unquoted (`<< EOF`) so the body can interpolate shell variables — which also makes every backtick a command substitution. LADR-064's praise rule shipped `` `IFS` `` and `` `🟠 [VERIFIED] High Priority` `` unescaped, and the non-findings catalogue shipped five lint-disable examples (`eslint-disable-next-line`, `# rubocop:disable`, `# noqa`, `#pragma warning disable`, `// ReSharper disable`) the same way. The runner executed all seven and substituted their empty output, so the model received a praise rule stripped of the concrete failure it exists for and a suppression catalogue stripped of its examples. Run 30808166239 logged `IFS: command not found` and `//: Is a directory` and still completed green — the failure is silent and one-directional, always toward a weaker review. Backticks escaped; new `test-chunk-prompt-guards.sh` check scans every unquoted heredoc in `review-in-chunks.sh` and fails on any unescaped backtick (verified to fail against the pre-fix file, naming lines 777 and 787). | LADR-064 |
| 2026-08-03 | **Per-chunk review budget now scales with prompt size.** PR #111 run 30792984316 lost **4 of 6 chunks to exit 124 at exactly 450 s**, fail-closing a PR whose reviewed chunks were clean. Cause is structural, not a bad provider moment: the 450 s was calibrated when a chunk prompt was ~88 KB, and ~33 KB of instruction growth (LADR-055/058) now rides on top of a diff the chunker already allows up to 100 KB — the same code ships as a 135 KB prompt against the same clock. The trend is monotonic in prompt size: 88 KB → 0 timeouts, ~107 KB → 1, 135 KB → 4. `lib/validate-chunk-timeout.sh` now takes the prompt size and bounds the budget at both ends — the existing Variable is the floor, +6 s/KB above a 64 KB free allowance is the slope (derived from the same run's successful chunks: 43 KB → 113 s, 68 KB → 299 s), and the new `OPENCODE_REVIEW_REPORT_CHUNK_TIMEOUT_MAX` (default 1200 s, both packagings) is the ceiling that keeps the deadlock-detector property. Calling the validator with no argument is byte-identical to the old behaviour, so no existing caller changes. Not fixed: the instruction growth itself — if timeouts return, lower `MAX_CHUNK_SIZE` or trim the prompt rather than raise the ceiling again. | LADR-065 |
| 2026-08-03 | **OpenCode Zen rosters refreshed against the live endpoints — and one declared model never existed.** `assets/opencode.json` declared 3 models on `go-openai` and 4 on `go-anthropic`, against 11 and 7 actually served. Checked two ways: the [endpoint tables](https://opencode.ai/docs/go/#endpoints) and the live `GET /zen/go/v1/models` listing. `go-openai` (`/chat/completions`) now declares `grok-4.5`, `glm-5.2`, `glm-5.1`, `kimi-k3`, `kimi-k2.7-code`, `kimi-k2.6`, `deepseek-v4-pro`, `deepseek-v4-flash`, `mimo-v2.5`, `mimo-v2.5-pro`, `hy3`; `go-anthropic` (`/messages`) declares `minimax-m3`, `minimax-m2.7`, `minimax-m2.5`, `qwen3.8-max`, `qwen3.7-max`, `qwen3.7-plus`, `qwen3.6-plus`. **`qwen3.6-pro` was removed because it does not exist** — not in the docs, not in the live listing — while the `OpenCode Qwen3.6 Plus` preset has always resolved `qwen3.6-plus`, which was *not* declared. So the config named a model nothing serves and omitted the one the preset actually requests; the same stale id had propagated into `SKILL.md`, LADR-027 in `AGENTS.md`, and a `test-resolve-provider-scope.sh` fixture. That drift ran the other way too: the preset dropdown had long offered nine OpenCode Go models the provider block never declared (grok, kimi, glm-5.2, hy3, mimo, minimax-m2.5, qwen3.7-max), so declaring the full roster closes it — every preset-referenced model is now declared, and the only newly-declared model without a preset, `qwen3.8-max`, gets one. Preset wiring stayed atomic per the repo rule: dropdown + provider + provider-id + all three model tiers in `pipeline-code-review-report.yml`, plus the duplicated dropdown in `code-review-caller.yml` and the full expression set in `code-review-local.yml`. Deliberately **not** added: the third Zen surface, `/responses` (`gpt-5.6-luna`, `@ai-sdk/openai`) — it is a different SDK package, so per LADR-027 it wants its own provider entry rather than being folded into either of these two. Also not added: the six ids the live listing serves but the docs tables omit (`kimi-k2.5`, `glm-5`, `qwen3.5-plus`, `mimo-v2-pro`, `mimo-v2-omni`, `hy3-preview`) — the curated tables are the supported roster, and those read as the deprecated tail. | LADR-027 |
| 2026-08-03 | **`test-code-graph.sh` now runs in its own sandbox instead of the caller's cwd.** It drives `build-code-graph.sh` / `detect-changes-graph.sh`, which write `.code-review-graph/` and `ci_temp/` **relative to the current directory**, and it resets between tests with `rm -rf ci_temp` — so run from a repo root it deletes that repo's `ci_temp/`. Under ai-analyse's LADR-057 test gate that was the gate's own `ci_temp/test_gate/` state directory, and the deletion turned every subsequent suite into an unrunnable phantom regression (PR #111 run 30788458834: all 21 suites green, gate reported `test-suite-failed` on `test-holistic-numbering.sh`, which never executed). The whole suite now `cd`s into a throwaway `git init` sandbox — sufficient because the only git the scripts under test need is `rev-parse --show-toplevel` for path relativization, with the base ref going to a mocked CLI — and the EXIT trap no longer deletes anything from `$PWD`. Fixed at the gate end too (see the `ai-analyse` skill's changelog); both halves are needed, since any suite could do this. | LADR-049/057 |
| 2026-08-03 | **Sidecar slimmed; partial structured coverage now renders instead of falling back.** Two production runs lost the LADR-055 merged summary entirely to a single truncated sidecar (PR #106 run 30756015689 at 5/6 chunks, PR #111 run 30786473904 at 4/5) — the block is emitted **last**, so it is the first casualty of cut-short output, and the chunk that finds the most writes the most markdown and is likeliest to truncate its own block. The feature was disabled by truncation on 2 of 3 observed multi-chunk runs. Fixed at both ends: (1) the `evidence` array is no longer requested, no longer required by the schema or `merge-findings.py` (nothing ever read it; it duplicated quotes already in the markdown, and `first_evidence` alone carries the quote-the-line gate) and the prompt asks for compact one-line-per-finding JSON — ~32% off the per-finding cost, 1,334 B → 898 B on the worked example; a present `evidence` array is still accepted and validated. (2) Aggregation renders whenever the merge ingested at least one chunk, and `render-findings-summary.sh` prints a blockquote warning at the top of the Coverage block naming the chunks that contributed nothing and pointing at the `### Chunk #N` sections that still hold their findings verbatim. The hazard was never "partial" but "silently partial": Part 2 loses nothing (LADR-005), and the verdict is unaffected because it is parsed from the orchestrator summary, which saw every chunk, while the structured-findings escalation is one-directional. Separately, the chunk prompt gains an explicit **praise-is-not-a-finding** rule with the concrete failure it exists for — PR #111 review 4840983861 filed a `🟠 [VERIFIED] High Priority` line *complimenting* the `IFS` bug fix, which `score-review.sh` reads as a High flag and which a reader cannot distinguish from a blocker under a severity heading. `test-merge-findings.sh` 17a–17h + 18a–18f; `test-chunk-prompt-guards.sh` pins the praise rule and that the sidecar never regrows `evidence`. | LADR-064 |
| 2026-08-03 | **Eval fixtures now run in a bounded worker pool — ~25 min of wall clock down to roughly a quarter of it.** The corpus is ~20 live chunk-review calls at 35s-3min each, run strictly serially; as a required PR check that is the slowest thing in the merge path. Isolation was verified rather than assumed before parallelising: `run_fixture` already builds its own `mktemp -d` sandbox and `cd`s into it inside a subshell, so the `ci_temp/` review-in-chunks.sh writes is per-fixture, and artifact copies are keyed on the unique fixture id — the only shared resource is the model endpoint. The blocker was bookkeeping, not isolation: a background subshell cannot append to the parent's `RESULTS` array or increment its counters, so each worker now writes one `kind\|id\|verdict\|detail` result file plus a console log, and the driver replays and tallies them **in launch order**, making a parallel run byte-identical to a serial one. `EVAL_PARALLEL` (default 4, dispatch input or repo Variable) is the knob; it trades wall-clock for provider rate-limit pressure and nothing else, and a rate-limited fixture fails the run as an INFRA failure — a flaky required check being exactly the state this harness had just been rescued from, the default is deliberately conservative. Two traps handled: `wait -n` is bash 4.3+ so the pool falls back to serial on macOS's bash 3.2 (the documented `local-evals.sh` path) rather than launching all 20 at once, and an INFRA verdict is counted as infra ONLY — the serial version reached that via `continue`, and folding it into the precision/recall denominators would quietly understate both rates. `test-evals.sh` grew two cases (19 checks): one diffing the parallel RESULTS table against the serial one to pin ordering, one asserting a precision regression still fails the gate when found by a worker. The self-test defaults to serial but honours an explicit `EVAL_PARALLEL`, so the pool is reachable without a paid run. | LADR-033 |
| 2026-08-02 | **Closed the two gaps the Tier 3 audit left open.** (1) `ai-review/SKILL.md` — the human-gated flow that actually **writes** the Skip Areas bullets the gate reads — had never been told the identifiers exist. LADR-063 numbered every posted item and told `ai-analyse` to quote them, but the writer of the routing channel was not updated, so `#R`/`#T`/`#P`/`#H` were decoration for the one consumer that matters most. Its skip-bullet format now leads with the identifier verbatim, its fix/skip table reuses it instead of renumbering, and items with no `file:line` of their own (the soft buckets and holistic items carry none by design) get a bullet anchored on the identifier plus the affected area rather than being silently dropped for lacking an anchor. (2) `minimize-previous-reviews.sh` never minimized the LADR-059 trivial-PR skip notices: they are posted with `gh pr comment`, so the review query cannot see them, and the issue-comment query only matched the `ai-analyse auto-fix` markers — on a repo with active dependency automation every skipped push left one behind permanently. Matched on the gate header **and** the distinctive `Trivial-PR skip` line rather than the header alone, so the blocked-incremental notice (same header) is deliberately untouched: `select-ai-analyse-artifact.sh` classifies that one as a cycle artifact and counts it toward the incremental cap, and keeping the scope to the audited shape means this cannot perturb the analyse loop. `test-minimize-reviews.sh` Test 6 pins all three cases — trivial-skip matches, blocked-incremental does not, a quoted copy does not. | LADR-059/063 |
| 2026-08-02 | **The eval harness had been red for 12 consecutive runs, and the corpus was at fault, not the reviewer.** Failures went back to 2026-07-31 — before Tier 2 and long before Tier 3 — and the failing fixture set changed almost every run (only DR-009 recurred, 4/5), which is the signature of fixtures that are marginal rather than of a prompt regression. Triaging run 30766652401 against its uploaded reviews showed all three precision failures were findings **unrelated to their DR**: DR-007 flagged a `ToListAsync` whose result was only `.Count`ed (a real inefficiency, sitting one line above an existing `CountAsync`), DR-015 flagged a genuine `${{ inputs.pr_number }}` → `run:` script-injection vector, and DR-009 flagged a `"baseURL": "{env:OPENCODE_GEMINI_URL}"` that **the DR-009 context injected alongside the fixture explicitly calls a design violation** ("deliberately ships no baseURL … never a static `{env:}` placeholder") and that the real `assets/opencode.json` does not contain. The reviewer was right three times out of three. Fixed the corpus rather than the scorer: DR-009 now mirrors the real file, DR-015 passes the input through `env:`, DR-007 counts with `CountAsync`, and DR-015's dead `push: tags` trigger is reconciled with the job `if:` (kept, not deleted — the tags glob is itself intended bait). **Deliberately NOT changed: the severity-only precision rule.** Narrowing it to "did the reviewer re-raise *this* DR" would let a reviewer that invents a new wrong finding on every fixture score 100%, which is the opposite of what a precision instrument is for; the obligation belongs on the fixture, and that is now a Non-Negotiable in `scripts/eval/AGENTS.md`. Sampling was also left at 1: `flagged_any` fails if **any** sample flags, so raising `EVAL_SAMPLES` makes precision strictly harder, not more stable. DR-009 was **rewritten rather than retired** — `run-review.sh` still defaults `OPENCODE_REVIEW_REPORT_PROVIDER` to `GEMINI` and the shipped config still declares that provider with `npm: "@ai-sdk/google"`, so the SDK-vs-gateway false positive is reachable in the product default, not a legacy corner. Removing the contradictory `baseURL` alone would have left the fixture toothless (nothing in it would suggest a gateway at all), so the `after/` tree now carries a second file — a faithful `lib/setup-opencode-config.sh` excerpt showing the install-time injection — which presents the `@ai-sdk/google`-talking-to-a-gateway shape the DR is about without contradicting the DR text. Precision failures now print the offending finding labels inline, because the old message named only the DR and read as "re-raised DR-XXX" even when the reviewer had flagged something else entirely — diagnosing this required downloading the run artifact. | LADR-033 |
| 2026-08-02 | **Every posted item is addressable, not just findings.** Observed on PR #110: three `🟡 Residual risk:` bullets and every holistic cross-chunk item posted with no identifier, so no reader or agent could quote one in a fix/skip decision — which is exactly how a skip reaches the PR body Skip Areas bullets that the next run reads. Numbering now covers all five classes, each in **its own sequence with a class prefix**: `#N` findings (unchanged), `#T` testing gaps, `#R` residual risks, `#P` pre-existing (array-indexed in `lib/render-findings-summary.sh`), `#H` holistic items (new `lib/number-holistic-items.sh`). Separate sequences rather than one continuous one, because a shared sequence renumbers every soft-bucket item whenever a finding is added — silently rebinding a skip written on a previous run to a different item — and because `**#1**` must keep meaning exactly what it meant to consumers that map it onto the findings array. Holistic numbering is deterministic post-processing rather than a prompt rule (a model asked to number its own bullets duplicates and restarts them, and nothing downstream can tell): top-level bullets only, only after the `**Cross-Chunk Issues Found:**` anchor, skipping placeholders / indented continuations / fenced blocks / already-numbered lines, run after `balance_fences` so the fence detection is reliable, and leaving the file untouched on any failure including a line-count mismatch. Soft bullets keep carrying no `[VERIFIED]` tag and no severity keyword, so `score-review.sh` still does not count them as flags (pinned by 13h/8g); the bullet still starts at column 0 with `- ` for `extract-ai-analyse-scope.sh` and the LADR-056 filter. Legend emitted in two places on purpose — the Issues Summary note renders only on full sidecar coverage, so the holistic section carries its own conditional one-liner. New `test-holistic-numbering.sh` (18 checks) plus 7 new render assertions. Not done: numbering the orchestrator free-text summary on the partial-coverage fallback path. | LADR-063 |
| 2026-08-02 | **SKILL.md split by audience — runtime contract stays, maintenance moves to AGENTS.md.** `SKILL.md` had grown to 421 lines / ~27k tokens, of which the largest single block was a second copy of all 41 LADRs; every one of them was already in `AGENTS.md` in longer form (verified per-LADR: AGENTS/SKILL content ratio 1.08–5.0, none shorter), with Date and Context on top. That whole section is now a four-line pointer. Also lifted to `AGENTS.md`: seven prompt-authoring / maintenance Key Behaviors (schema-field guard, advisory precedence and routing, pre-existing partition, "a prompt addition edits every rule already in the prompt", eval harness, version-notice best-effort), the nine `OPENCODE_ANALYSE_*` env rows (read only by `pipeline-ai-analyse.yml`, never by the gate), and the `model_preset` five-expression coupling rule. Three bullets were dropped outright as verbatim duplicates of ones already in `AGENTS.md`. Runtime content was compressed in place, not moved: six tuning/diagnostic env cells, the transport bullet, and the `local-review.sh` bullet that restated the Local runs paragraph above it. Net: SKILL.md 421 → 142 lines (~27k → ~10k tokens), AGENTS.md 708 → 717 lines. Also corrected `AGENTS.md`'s LADR-001, which still said "Gemini API call" where SKILL.md had the current "model API call". | — |
| 2026-08-02 | **The reusable workflow fetched its own scripts from the caller's repo — one wrong context property, four months latent, 100% fatal cross-repo.** `github.workflow_sha` in a reusable call is the **caller's** workflow commit in the **caller's** repository, not the called workflow's commit here; the side-checkout ref expression had used it since `e243ac2` (#58, 2026-07-04) while five doc sites asserted the opposite semantics. `actions/checkout` was therefore handed a foreign SHA and asked to fetch it from this repo: `fatal: remote error: upload-pack: not our ref`, three retries, exit 128, before any review logic ran. First seen on the first real cross-repo consumer — all eleven onboarding runs of `fadi-labs/reporting-agent-prototype` failed, each with a different SHA tracking its own PR head (run 30764997922, ref `a93ff1be…` = that repo's `Merge e0db990 into f7f876f`, a 422 in ours). Invisible from here: the `hashFiles` guard means this repo's own PRs take the in-repo skill tree and never execute the step. Fixed to `github.job_workflow_sha` (one term in one expression), so the default lock is now genuinely a commit SHA of this repo. Documented policy for the `tools_ref` / `SMOOTH_AI_REVIEW_TOOLS_REF` overrides: a full commit SHA for supply-chain pinning, or a trusted floating ref (`main`, `latest`, `v1`) — not a branch. Five doc sites corrected rather than annotated. | — |
| 2026-08-02 | **Tier 3 structural options — and the grouping-split bug that made two of the four dead.** Landed four items adapted from `EveryInc/compound-engineering-plugin` @ `a5cd949` (MIT): a two-stage trivial-PR skip (LADR-059), the silent-pass verification lens (LADR-060), precomputed blame provenance (LADR-061), and per-run artifacts (LADR-062). Five defects were caught in review, all silent, none visible to the pre-existing suites. **(1)** The trivial-PR detector documented `OPENCODE_MIN_OUTPUT_BYTES=1` in a comment but never set it, so the helper's 200-byte floor scored every `yes` as a model failure and the skip could never fire. **(2)** The blame wiring guarded on `$LIB_DIR` (defined by `run-review.sh`, never exported) and `$HEAD_SHA` (named `$TO_SHA` in `review-in-chunks.sh`) — two independent reasons it never ran once. **(3)** `build-blame-digest.sh` produced nothing even when reached: multiple `-L` ranges joined into one argument, a BRE hunk-count regex with unescaped `+`/`(` that never matched, and 40-char SHA extraction against abbreviated non-porcelain blame output. Its own new test was **red in the delivered tree** (4/6). **(4)** `actions/upload-artifact` was pinned to a **non-existent SHA**; GitHub cannot resolve an unknown SHA, so the step would fail on every run, marking the whole workflow failed and permanently starving `pipeline-ai-analyse.yml`'s `workflow_run.conclusion == 'success'` gate. Re-pinned to the verified v4.6.2 SHA. **(5)** `OPENCODE_REVIEW_REPORT_ENABLE_RUN_ARTIFACTS` was declared only in the local-job example, not in the reusable workflow. Two further fixes: the provenance block was emitted unconditionally (telling the model to cite "the digest below" with nothing below it, on every chunk), and `_blame_inline`/`_blame_truncated` were plain globals leaking across the chunk loop, so a chunk with no digest inherited the previous chunk's. **Root cause behind two of them: `while IFS='::' read -r dir file`.** IFS is a *set* of delimiter characters, not a string, so this is `IFS=':'` and the second colon of the `::` pair lands at the front of the last field — every path in `files[]` arrived as `:path/to/file`. Suffix globs and `git diff -- :path` tolerate it (which is why it survived, visible only as a stray `:` in the prompt's file list), but prefix matching does not: the LADR-060 `.github/workflows/*` detection could never match, the chunk-context ancestor test could never match, and the blame digest could not stat the path. Split explicitly on `::` now. New suites: `test-blame-digest.sh` (12 checks, including a two-hunk fixture and an integration case that runs the real chunk script) and `test-verification-lens.sh` (5 checks, asserting the lens fires on a workflow-only chunk and does *not* fire on a markdown-only one); `test-chunk-prompt-guards.sh` +5 and `test-select-ai-analyse-artifact.sh` +2. Artifact assembly moved to an EXIT trap so the five exits that never reach aggregation — including the new trivial skip — still leave a record. Not done: the `select-ai-analyse-artifact.sh` consumer migration (LADR-042 contract change, deliberately separate). | LADR-059…062 |
| 2026-08-02 | **Test gate for ai-analyse before push (LADR-057).** `pipeline-ai-analyse.yml`'s `Commit fixes` step now runs `run-test-gate.sh` after `filter-test-self-fix.sh` reverts test edits and before staging. Gating is **regression-relative**: every failing suite is re-run in a pristine `git worktree` at HEAD, and only a green→red transition blocks (`push_skipped=true` + `skip_reason=test-suite-failed`); a suite already failing at HEAD is reported as `PREEXISTING_SUITE=` and does not block, so one stale suite cannot disable the loop permanently. A timeout, or a baseline that cannot be produced, fails closed. The `Post ai-analyse summary` step renders regressed suites, pre-existing failures separately, the last `OPENCODE_ANALYSE_TEST_LOG_LINES` (default 40) lines of each failure inside fences, and the `git diff` of unpushed fixes inside `<details>` + a ```diff fence, truncated by whole lines under a 30 000-byte cap (`head -c` would split multi-byte characters and make the API reject the comment). Three new Variables: `OPENCODE_ANALYSE_TEST_COMMAND` (explicit shell command, overrides auto-detect), `OPENCODE_ANALYSE_TEST_TIMEOUT` (default 600s, non-integer/≤0 falls back), `OPENCODE_ANALYSE_TEST_LOG_LINES` (default 40). Auto-detect runs tracked `test-*.sh` files at HEAD in `LC_ALL=C sort` order; no suites + no explicit command → no gate, plus an actionable log line. Only tracked-at-HEAD scripts are executed (security: `git ls-tree -r HEAD --name-only`, not filesystem glob). The `filter-test-self-fix.sh` LADR-045 matcher is extended to cover `test-` prefix scripts (e.g. `test-review-chunk-threshold.sh`), closing the hole where every one of this repo's own `test-*.sh` suites was unguarded. | LADR-057 |
| 2026-08-02 | **Nine prompt-level precision rules — and the two coupling bugs that would have made three of them no-ops.** Adopted the Tier 2 set adapted from `EveryInc/compound-engineering-plugin` @ `a5cd949` (MIT): a nine-shape non-findings catalogue appended to DR-001…015 (two shapes had no local equivalent — lint-disable comments and uncodified code-quality concerns), an advisory test with the precedence rule that the catalogue outranks it, finding-description rules with a weak-vs-strong pair, an anti-punt rule for suggested fixes, three-tier Primary/Secondary/Pre-existing diff classification, soft-bucket routing into `testing_gaps`/`residual_risks`, an exhaustive-coverage honesty rule, an aggregation completion gate plus ASCII-safe formatting rules with the severity emoji explicitly exempt, and a Coverage ledger extended with failed-chunk, pre-existing and soft-bucket counts. Four coupling defects were caught in review and fixed — three of them the same shape: a new rule contradicting an older one it did not reconcile, which no test can see because the model resolves it silently and the review still posts. **(1)** The three-tier rule told the reviewer to report pre-existing items in a separate markdown section while the sidecar rule admitted only items listed under **Issues Found** — so `pre_existing` would never have been `true`, `pre_existing_findings` never non-empty, and the LADR-055 render path for it dead on arrival. Pre-existing items are now explicitly required in the `findings` array, where the merge partitions them out of the verdict. **(2)** Prose naming `residual_risks`, `pre_existing` and `why_it_matters` sat in **unguarded** heredocs, so a run with `OPENCODE_REVIEW_REPORT_ENABLE_STRUCTURED_FINDINGS` falsy would instruct the model to populate fields for a JSON block it was never asked to emit — silent, since the review still posts. Behavioural rules are now field-agnostic in the unconditional prose with the field mapping inside the guard. Also removed: three work-order sentences that had been pasted into the production prompt verbatim ("Without this sentence the predictable failure is…", "Write the exemption into the prompt sentence itself, not into a comment"), a `**Formatting rules:**` block sitting *inside* the output template the orchestrator is asked to reproduce, a duplicated and self-contradictory completion gate telling the orchestrator to "use the fallback template below" if its own call failed, and upstream's P0–P3 vocabulary from the soft-bucket rules. New `test-chunk-prompt-guards.sh` statically pins the guard invariant, the three blocks that must stay guarded, and the absence of P-scale vocabulary; verified against an injected leak. Prompt growth +7.9 KB chunk / +1.7 KB summary. Not taken: table-cell `\|` escaping (premise checked — the report is bullet-based, no model-authored table reaches a parsed surface) and the "removable surface" ledger line (no schema field carries it). | LADR-058 |
| 2026-08-02 | **LADR-056's filter was dead for 20 minutes, and the test that should have caught it was the one that broke it.** An autonomous `[ai-analyse]` pass (`4b1ad8a`) set out to word-boundary `TEST_WORD_RE` so `latest`/`contest`/`attest`/`testify` stop tripping the tier-2 gate — a real over-match — and shipped `grep -qiPE`. `-P` and `-E` are mutually exclusive in both GNU grep and ugrep: every call exited 2, `is_failing_test_finding()` always returned false, and the LADR-056 guard withheld nothing while reporting success. Its left-hand anchor was independently wrong, rejecting the `FooTests`/`IntegrationTest`/`BazSpec` CamelCase identifiers the suite already pinned. Repaired as two alternations — `(^\|[^A-Za-z])[Tt]est…` for a word start and `[a-z](Test\|Spec\|…)` for a CamelCase start — matched **case-sensitively**, because `-i` folds the only signal that separates `FooTests` from `latest`. Four fixtures added pinning the over-match half of the contract (confirmed to fail against the pre-`4b1ad8a` substring matcher, which silently withheld "the **latest** migration is failing"). The autonomous pass ran with the suite green in CI only because it never ran the suite — this is the concrete second instance of the LADR-055 self-referential class, now on executable code rather than prose. | LADR-056 |
| 2026-08-02 | **The chunk-timeout validator is a lib, so the test pins the real thing.** `test-review-chunk-threshold.sh` asserted the fallback-on-bad-value contract against a self-contained subshell **reproduction** of the validator, which had drifted to `^[0-9]+$` while `review-in-chunks.sh` used `^[1-9][0-9]*$` — the two disagree on `007` (real → 450, reproduction → passes `007` to `timeout`), and the only assertion tying the test to the real script was a `grep -c` for the variable name, which stays green with the validation deleted outright (verified: removing it now fails two assertions that previously both passed). Extracted to `scripts/lib/validate-chunk-timeout.sh` — one definition of the default and the accepted form, printing a bare integer on stdout and a complaint about a rejected value on stderr instead of swallowing it. Both sandbox-building tests now copy it alongside `count-changed-files.sh` and `extract-findings-json.sh`; `test-chunk-prompt-budget.sh` had been missing the extractor too, so that call path was unexercised there as well. | LADR-002 |
| 2026-08-02 | **Two silent-degradation sites made audible.** `review-in-chunks.sh`'s sidecar extraction dropped its exit status on the floor with `|| true` — the shape that let the extractor go unexercised in the test sandbox from the day it landed, erroring to stderr on every chunk with nothing in the log to say so. It now emits `::warning::` on a non-zero exit; non-blocking remains the contract (still no `.failed` flag — LADR-031 owns that channel), invisible no longer is. `lib/report-error-log.sh` defaults its `wc` results to `0`, so a log the failing step is still writing — or one a parallel cleanup has just unlinked — cannot render the header as `last 40 of  lines,  bytes`; a diagnostic that runs only when something is already broken should not become the second puzzle. Also moved `.gitignore`'s `ci_temp_logs/` up beside `ci_temp/` with the sibling rationale spelled out, replacing an EOF entry whose comment had been appended by an autonomous pass mid-sentence. | LADR-055 |
| 2026-08-02 | **`ai-analyse` failing-test rule (LADR-056).** A failing test is a signal, not a defect — the autonomous fixer must not make a red test green by any route, including editing production code to satisfy a stale test. Two-layer guard mirrors LADR-045's pattern: a deterministic filter (`scripts/lib/filter-failing-test-findings.sh`) withholds failure-basis findings before the model sees them, and the prompt rule reinforces this unconditionally (regardless of `OPENCODE_ANALYSE_ALLOW_TEST_SELF_FIX`). Withheld findings are reported in the summary comment so the filtering is auditable. New offline test `scripts/test-filter-failing-test-findings.sh`. | LADR-056 |
| 2026-08-02 | **Diagnostic logs survive cleanup and print to the console; chunk timeout default corrected 900 → 450 s.** New `lib/report-error-log.sh` copies a failing step's stderr to `ci_temp_logs/` — a **sibling** of `ci_temp`, which the `if: always()` cleanup `rm -rf`s — and prints a bounded tail inside a `::group::`, because workflow logs outlive the workspace and the gate uploads no artifact. Wired into both chunk-failure branches (non-zero exit and the quieter empty/tiny-output path) and summary generation; always exits 0. Previously the run only *named* a path that was deleted before anyone could read it, which is why PR #106 run 30756015689's two exit-1 chunk failures had to be diagnosed by inference. Separately, the chunk timeout default introduced earlier the same day was walked back from 900 s to **450 s**: the slowest chunk that has ever succeeded on the deployed pair took 209 s, so 450 is >2x the observed worst case, and the ceiling doubles as a deadlock detector — past that point a chunk is stuck rather than slow, and waiting it out only holds the job open. | LADR-002 |
| 2026-08-02 | **The rendered Issues Summary is inserted when there is nothing to replace.** `aggregate-reviews.sh` gated the LADR-055 splice on an existing `## 🔍 Issues Summary` heading in the orchestrator's output. When the summary LLM call fails, aggregation falls back to a template that has no such heading — so a healthy merged findings document was silently discarded. PR #106 run 30756015689: 2 of 6 chunks failed (exit 1, both models), the summary call also failed, the merge still produced **5 findings + 1 suppressed**, and the posted review carried no findings section at all — `ai-analyse` read `has_low_medium=false` off a review that had real medium findings computed. Now: replace when the heading exists, insert before `## 🎯 Recommendation` when it does not, append when neither is present, and verify the splice produced output before it replaces the summary. | LADR-055 |
| 2026-08-02 | **Per-chunk review timeout is configurable, and its default raised 300 → 450 s.** New `OPENCODE_REVIEW_REPORT_CHUNK_TIMEOUT` Variable (non-integer/≤0 falls back to 450), present in both workflow packagings. The 300 s was calibrated before LADR-055 roughly tripled per-chunk model output (markdown ~2.9 KB → ~5.7 KB, plus ~3.4 KB of JSON sidecar); on PR #106 a 96 KB-prompt chunk hit exactly 300 s and fail-closed while a mid-size chunk in the same run took 209 s. The budget wraps the **whole** model chain — `lib/opencode-with-fallback.sh` has no internal timeout — so an exit 124 means the LADR-002 secondary was never reached; the failure marker now says so instead of claiming fallbacks were exhausted. The ceiling doubles as a deadlock detector — the slowest chunk that has ever succeeded took 209 s, so past ~450 s a chunk is stuck rather than slow and waiting it out only holds the job open. | LADR-002 |
| 2026-08-02 | **Review follow-up to the structured-findings landing (same day).** Two defects fixed and two follow-on corrections. (1) `lib/extract-findings-json.sh` anchored the strip on the **first** `<!-- FINDINGS_JSON_BEGIN -->` in the chunk body, so a review that merely *quoted* the sentinel — the canonical case, since this repo reviews its own PRs and six of its tracked files contain the literal string — had everything after the quote swallowed as "the block": the review was truncated, the real sidecar was consumed with it and failed to parse, and the surviving markdown could fall under the 200-byte empty-output floor and turn a clean chunk into a fail-closed REQUEST_CHANGES. Reproduced at 597 B → 73 B. Now anchored on the **last complete `begin`…`end` pair** (the prompt requires the block to be last), and an unterminated `begin` is left in place — a fenced JSON block rendered in the body is cosmetic, a truncated review is not. Same defect class as LADR-031: a control channel must not be forgeable by review content. (2) Aggregation's full-coverage precondition counted sidecar **files on disk** (`ls`), so a sidecar rejected at the slurp or as a malformed document still counted as covered and could render a summary silently missing that chunk while reporting complete coverage. `merge-findings.py` now reports `merged_chunks` — the chunks it actually ingested — and aggregation gates on that. (3) `residual_risks` and `testing_gaps` were collected by the merge and rendered nowhere — dead data no human or agent ever saw. They are now deduplicated across chunks (`soft_key`, same normalisation as the title leg of `fingerprint()`) and rendered as 🟡 `Testing gap:` / `Residual risk:` bullets inside `### 🟡 Medium Priority Issues`, **without** a `[VERIFIED]` tag: `eval/lib/score-review.sh` counts a flag only when the label carries the tag *and* a severity keyword, and DR precision is zero-tolerance, so tagging them would let an honest "no test covers the new branch" note fail a must-not-flag fixture. Untagged they still reach the reader and `extract-ai-analyse-scope.sh`. (4) The eval caveat named `GEMINI`/`gemini-3.1-pro-preview` as the pair to re-validate on; the deployed pair is **MiniMax M3**, and the Gemini ids in `OPENCODE_REVIEW_REPORT_MODEL_*` are literal fallbacks rather than what the gate is run with — corrected, so the instruction does not send a reader to validate a pair nobody uses. `test-merge-findings.sh` 61 → 86 assertions (10j–10s, 11b2–11b3, 13a–13i). | LADR-055 |
| 2026-08-02 | **Structured findings — a JSON sidecar alongside the markdown, deterministic cross-chunk merge, mechanical quote-the-line gate.** Chunk reviews now append a sentinel-delimited JSON block (`<!-- FINDINGS_JSON_BEGIN -->` … `<!-- FINDINGS_JSON_END -->`) conforming to the new `assets/findings-schema.json`; `lib/extract-findings-json.sh` extracts it to `ci_temp/reviews/chunk_<n>.findings.json` and strips the sentinel range from the markdown inclusive — parsed or not — so the posted body stays byte-identical to pre-feature. `lib/merge-findings.sh` (interpreter resolution + collection) drives `lib/merge-findings.py` (pure stdin→stdout) through validate → fingerprint dedup on `(file, line, title)` → conservative route merge → **demotion of any confidence ≥ 75 with no `first_evidence` to 50** → confidence gate (`< 75 and not critical → suppressed`) → partition → deterministic sort + stable `#` numbering, into `ci_temp/findings.merged.json`. `lib/render-findings-summary.sh` renders Part 1's `## 🔍 Issues Summary` from that document with identical headers/emoji/`[VERIFIED]` grammar plus a stable number and chunk back-reference, and an always-present `### 📊 Coverage` block — **only when every non-failed chunk contributed a sidecar**, so partial coverage can never silently drop a chunk from the decision surface. The chunk prompt gains the five behavioural confidence anchors and the quote-the-line gate (four worked shapes + the ORM/source-generator carve-out, cross-referenced to DR-012). Aggregation escalates approve→request_changes, one-directionally, when the rendered summary carries a Critical/High the orchestrator missed, so the body and the posted state cannot contradict. Fully degradable: no sidecar, malformed JSON, partial coverage, or a missing Python interpreter each fall back to the exact pre-feature path — verified by diffing the assembled body against the pre-change scripts (byte-identical in both the no-sidecar and partial-coverage cases). Gated by `OPENCODE_REVIEW_REPORT_ENABLE_STRUCTURED_FINDINGS` (default `1`) in both workflow packagings. New `test-merge-findings.sh` (61 assertions) covers the algorithm, the extractor, the wrapper, and re-parses the rendered output with the real `score-review.sh` and `extract-ai-analyse-scope.sh`. Design adapted from `EveryInc/compound-engineering-plugin` `skills/ce-code-review` @ `a5cd949` (MIT) — mechanisms taken, shape rewritten for chunked CI. Eval A/B over the full corpus (toggle off vs on, same model both sides — `deepseek/deepseek-v4-pro`; see LADR-055 caveat for production-pair coverage): precision **71% → 85%** (10/14 → 12/14 clean; `DR-003` and `DR-007` stopped being re-raised), recall **100% → 100%** (6/6), **zero regressions** — but measured on `deepseek/deepseek-v4-pro` via OpenRouter, **not** the production `gemini-3.1-pro-preview`, so the zero-re-raises precision condition is still unverified on the production pair. | LADR-055 |
| 2026-07-31 | **Renamed version-pin Variables to match the install scripts.** Workflow env blocks (`pipeline-code-review-report.yml`, `pipeline-ai-analyse.yml`, `llm-eval-harness.yml`) previously set `OPENCODE_REVIEW_REPORT_CLI_VERSION` / `_GRAPH_VERSION` / `_RTK_VERSION` from the corresponding `vars.*` keys, but `install-opencode.sh` / `build-code-graph.sh` / `install-rtk.sh` and `run-review.sh` (lines 110/118/126) read `OPENCODE_CLI_VERSION` / `OPENCODE_TOOL_CODE_REVIEW_GRAPH_VERSION` / `OPENCODE_TOOL_RTK_VERSION` — the pin was silently dropped and every prior install pulled `latest`. Renamed all three workflow env keys (and the `rtk_version` input descriptions) to the names the scripts actually read, closing the LADR-048 pin-parity gap for real. Consumers must rename their repo/org Variables to match. | LADR-048 |
| 2026-07-31 | **Fix: pinned rtk installs 404'd — release tags need the leading `v`.** PR #100's CI run (job `91252702249`) showed `install-rtk.sh` downloading `.../releases/download/0.44.1/...` (404) instead of `.../releases/download/v0.44.1/...` (302) whenever `OPENCODE_REVIEW_REPORT_RTK_VERSION` was pinned (e.g. the repo's own `0.44.1` Variable) — rtk's install.sh uses `RTK_VERSION` verbatim to build the download URL, but its GitHub release tags carry a leading `v` that the script had stripped for internal version comparisons and never re-added. The failure was silent: `install-rtk.sh` degrades gracefully by design (LADR-054), so the review posted normally, just without the `rtk` line in the `📦 Versions` block — caught only by noticing its absence and cross-checking the CI run logs, not by any test. Fixed by re-prefixing `v` only on the value passed to the upstream installer, keeping `REQUESTED_VERSION` bare everywhere else (cache-hit check, post-install verify) so it still matches `rtk --version`'s numeric-only output. Verified live end-to-end in a scratch sandbox (real download, checksum verify, plugin init) and confirmed the root cause with direct HEAD requests (`0.44.1` → 404, `v0.44.1` → 302). New `test-install-rtk.sh` (4 tests, offline-stubbed `curl`/installer) asserts the upstream installer receives a `v`-prefixed tag regardless of whether the input Variable already had one, and that an unpinned (latest) install still leaves `RTK_VERSION` unset — confirmed this test fails against the pre-fix code. | LADR-054 |
| 2026-07-31 | **RTK version-update notices added to the review header (LADR-053, amended).** As in PR #99 for `code-review-graph`, `lib/check-versions.sh` now also checks the installed `rtk` version against its latest GitHub release and surfaces an "update available" notice when behind, pinned by `OPENCODE_REVIEW_REPORT_RTK_VERSION`. `rtk` has no npm/PyPI package, so this is a new `_cv_github_latest_tag` helper hitting `GET /repos/rtk-ai/rtk/releases/latest` (`.tag_name`, leading `v` stripped, `User-Agent` header required or GitHub 403s). Sourced after both graph analysis (Step 13.5) and rtk install (step `5c-bis`) so `rtk --version` reflects what `install-rtk.sh` actually installed. The single-slot footer's priority-ordered takeover logic (CLI → graph) is extended one more tier to rtk: it only claims the footer if nothing else has already claimed it with a "behind" notice. `test-check-versions.sh` gains a "Testing rtk resolution" section (absent → no line/no lookup; update available → header + footer takeover + Variable name; up to date → ✅) plus an ordering assertion that `check-versions.sh` runs after `install-rtk.sh`; 22/22 tests pass. | LADR-053 |
| 2026-07-31 | **RTK re-adopted via OpenCode plugin (LADR-054).** `rtk-ai/rtk`'s Gemini-CLI hook (LADR-014) was superseded when the pipeline moved to `opencode` transport (LADR-023) — the hook had no equivalent interception point on opencode. Checked against `rtk-ai/rtk@8a24ce2` and confirmed RTK now ships a first-class OpenCode plugin (`rtk init --opencode`), closing that gap. New shared lib `scripts/lib/install-rtk.sh` (mirrors the LADR-048/049 pattern) installs the pinned binary and runs `rtk init -g --opencode --auto-patch --hook-only`. Wired into `run-review.sh` as step `5c-bis`, right after opencode is installed, gated by `OPENCODE_REVIEW_REPORT_ENABLE_RTK` (default on) and pinned by `OPENCODE_REVIEW_REPORT_RTK_VERSION`. Unlike `install-opencode.sh`, degrades gracefully on failure — RTK is an optional token-optimization enhancement, not a review dependency. The version pin is additionally exposed as a `rtk_version` workflow input (`workflow_dispatch` + `workflow_call`), following the `opencode_config` input precedence pattern. Both workflow packagings and the caller template updated. | LADR-054 |
| 2026-07-31 | **Fix: incremental reviews now preserve NUL delimiters in `run-review.sh`.** The incremental diff branch used `tr '\0' '\n'` before `sort -z`, collapsing the NUL-delimited file list into a single record so `comm -z -12` never found matches. The branch now keeps the file list NUL-delimited end-to-end, preserving `changed_files.txt` for downstream consumers and restoring incremental reviews for PRs with changed files. Added regression coverage for both a strict-subset intersection and a path containing a newline. | issue #95 |
| 2026-07-31 | **Fix: graph paths are now repo-relative.** `detect-changes` reports ABSOLUTE paths (`changed_functions[].file_path`, `test_gaps[].file`), which on a CI runner carry a ~70-char `/home/runner/work/<repo>/<repo>/` prefix — overflowing the 60-char truncation in the per-file risk table so it cut the filename and kept the prefix, and leaving `graph_file_risks.txt` unjoinable against the repo-relative paths that graph-aware chunk grouping (LADR-051) will match on. `detect-changes-graph.sh` now resolves the repo root once (`git rev-parse --show-toplevel`, falling back to `pwd`) and strips it via jq `ltrimstr` at all three render sites; `ltrimstr` no-ops on a prefix mismatch, so a symlinked checkout degrades to the previous absolute-path behaviour instead of mangling output. Test 6's mock previously used relative paths and so could not catch this — it now emits absolute paths like the real CLI, and asserts the rendered output is repo-relative in both `graph_file_risks.txt` and the markdown summary. | LADR-049 |
| 2026-07-31 | **Fix: `detect-changes` has no `--format` flag — it never did.** `detect-changes-graph.sh` always tried `code-review-graph detect-changes --base "$BASE_REF" --format json` first, which always fails (`--format` doesn't exist on this subcommand on any version of the CLI), so every CI run silently fell into a "text-fallback" branch that double-wrapped the *actual* JSON (the bare, unflagged invocation's real, intended output) as an opaque string, discarding all structured fields. This had been true since the feature's introduction earlier the same day — the "structured analysis" code path had never once executed in production. Fixed by dropping the `--format` attempt entirely and treating the bare invocation's stdout as the JSON it already is. Unlocking that dead code path surfaced three more latent bugs in the same file, now also fixed: a jq count expression (`[.x // []] \| length`) that always evaluated to `1` regardless of actual array size; `changed_functions[]`/`test_gaps[]` field names that didn't match the real schema (`file`→`file_path`, `line`→`line_start`, `.function`→`.name`, and a `callers[]`/`tests[]` pair that don't exist on this object at all); and `context_savings.savings_pct` vs the real `context_savings.saved_percent`. Root cause and full reproduction steps: `.context/bug-detect-changes-format-flag.md` (gitignored, workspace-local). Test 6's mock rewritten to match the real CLI contract (previously it mocked the *wrong* assumption, which is why it never caught this); new Test 6b asserts the script never references `--format`. | LADR-049 |
| 2026-07-31 | **Code graph analysis, enabled by default (Phase 1).** Added two shared libs under `scripts/lib/` following the LADR-048 single-source-of-truth pattern: `build-code-graph.sh` (installs `code-review-graph` via pipx for PEP 668 compatibility, builds/updates SQLite graph) and `detect-changes-graph.sh` (runs `detect-changes`, produces risk-scored output). Wired into `run-review.sh` as Step 13.5, gated by `OPENCODE_REVIEW_REPORT_ENABLE_GRAPH_ANALYSIS` (truthy `1/true/yes/on`, case-insensitive, default `1` — set to a falsy value `0/false/no/off` to disable). `OPENCODE_REVIEW_REPORT_GRAPH_VERSION` pins the package version. Both scripts degrade gracefully on failure. Cache step added to both workflow packagings, keyed on source-tree + lockfile hashes. Phase 1 produces `graph_detect_changes.json`, `graph_risk_summary.md`, `graph_file_risks.txt` for downstream LADR-050/051/052 consumption. | LADR-049 |
| 2026-07-31 | **Version-update notices in the review header — feature repaired.** The initial implementation (`04adef5`) was inert in three ways and is fixed here. (1) It queried `registry.npmjs.org/opencode`, which is HTTP 404 — the CLI publishes as **`opencode-ai`** — so the latest version was always empty and the header printed `✅ up to date` while an update existed (verified: installed `1.18.9`, npm latest `1.18.10`). The package name is now the named constant `OPENCODE_CLI_NPM_PACKAGE`. (2) The footer block in `aggregate-reviews.sh` read `OPENCODE_CLI_CURRENT_VERSION`/`_LATEST_VERSION`, which were never exported and never passed — the aggregator is a child process, so the block was dead code. The footer is now pre-rendered as `OPENCODE_VERSION_FOOTER` and passed positionally as `$10` alongside the header's `$9`. (3) `OPENCODE_PROVIDER_CURRENT_VERSION` was hardcoded `""`, making the provider's current-vs-latest branch unreachable; the provider SDK is compiled into the opencode binary, so the line is now an explicitly-labelled latest-available reference and the dead branch is gone. Also: the duplicated provider→npm map is replaced by a read of `.provider[<id>].npm` from the installed `opencode.json` (the old map shipped `@ai-sdk/github-copilot`, which 404s); provider-id falls back to `gemini` matching `run-review.sh`'s existing guards; update detection uses `sort -V` so a pin ahead of the registry renders no bogus notice; the update line names `OPENCODE_REVIEW_REPORT_CLI_VERSION` and links the release notes; `_cv_*` temporaries and helpers are unset so nothing leaks into the sourcing shell. New `test-check-versions.sh` (16 tests, fully offline via stubbed `opencode`/`curl` + fixture registry) covers the requested URL path, both rendered strings, the pin-ahead case, config-driven provider resolution, percent-encoding of scoped packages, graceful degradation, shell hygiene, and the aggregator wiring. `test-run-review.sh` still 54/54. | LADR-053 |
| 2026-07-31 | **Version-update notices — swapped provider-package check for `code-review-graph`.** The provider-SDK line from the same-day repair above is removed: `@ai-sdk/*` packages are compiled into the opencode binary, so there was never a real installed version to diff against, only a permanently-unreachable "update available" branch dressed up as an informational reference. Replaced with a check against **`code-review-graph`** (LADR-049), pinned by `OPENCODE_REVIEW_REPORT_GRAPH_VERSION` — a real CLI tool installed via pipx with a genuine current-vs-latest diff, looked up on PyPI's JSON API (`https://pypi.org/pypi/code-review-graph/json`, `.info.version`). Because the graph tool isn't installed until Step 13.5 (`build-code-graph.sh`), `check-versions.sh`'s source line in `run-review.sh` moved from immediately after `resolve-provider.sh` (step 5f-bis) to immediately after Step 13.5's `graph_analysis_available` write — so `code-review-graph --version` always reflects what this run actually installed, and the graph line is silently omitted (not rendered stale) when graph analysis is disabled or its install failed. `test-check-versions.sh` provider-resolution tests replaced with graph-resolution tests (graph absent → no line, no PyPI call; update available → both versions + Variable name; up to date → ✅, no bogus notice) plus a new ordering assertion that `check-versions.sh` is sourced after the graph-analysis-available write, not before. 16/16 tests pass. | LADR-053 |
| 2026-07-31 | **Shared `install-opencode.sh` lib + version-pin parity.** Extracted the opencode install logic from `run-review.sh` (lines 373–442) into `.agents/skills/ai-review-report/scripts/lib/install-opencode.sh` so every install path in this repo delegates to one implementation. Closes the `pipeline-ai-analyse.yml` pin gap (always installed `latest` regardless of `OPENCODE_REVIEW_REPORT_CLI_VERSION`) and the npm consumer template gap (bare `curl | bash`). All four paths (gate, analyse, eval, npm-example) now call the shared lib. `pipeline-ai-analyse.yml` gains `OPENCODE_REVIEW_REPORT_CLI_VERSION` in its env block. Dead `CACHE_KEY` var in `run-review.sh` dropped. README gets a prominent "Recommended: pin the opencode version" callout + updated env-var table rows flagging "unset = latest" as the weak default. Consumer templates (`code-review-caller.yml`, `code-review-local.yml`) get pin-prominence comments. Root `AGENTS.md` (symlinked as `CLAUDE.md`) gets a Non-Negotiables bullet requiring delegation to the shared lib for any new install path. The lib exports `$HOME/.opencode/bin` only to its own subshell, so single-bash-script callers (`run-review.sh`) re-export PATH in their own shell; workflow-step consumers rely on the lib's `$GITHUB_PATH` append for cross-step propagation. | LADR-048 |
| 2026-07-30 | **Fix PR #86 example pin regression.** `.docs/examples/code-review-local.yml` had its upstream `ref:` pinned to `6de63b3…` (the commit before `run-review.sh` existed), so a consumer copying the example verbatim would 404 the entrypoint script at the next step. The example now pins to `c729371…` (the PR #86 head, which contains the entrypoint) with a comment instructing the consumer to re-pin to the post-merge `main` SHA. New regression test in `test-run-review.sh` parses the example, extracts the pin, and asserts (a) it's a 40-char hex SHA (not a floating tag/branch — the README explicitly forbids that), and (b) `git ls-tree` against that SHA returns a `run-review.sh` entry. 42/42 tests pass. | — |
| 2026-07-30 | **Fix PR #86 chunker size-cap regression.** `review-in-chunks.sh` now treats `MAX_CHUNK_SIZE=102400` as a hard upper bound, regardless of file count. A PR with 9 files but a 208KB diff used to build a single-chunk prompt (because 9 ≤ `REVIEW_MIN_FILE_COUNT_BEFORE_CHUNCKING=10`) and timeout at 5 minutes (exit 124), forcing the LADR-031 fail-closed safety net to override the aggregation's APPROVE decision with `REQUEST_CHANGES`. The chunker now sums per-file diff sizes via `git diff ${FROM}..${TO} -- $file` and falls through to adaptive splitting whenever the total exceeds the cap. `MAX_CHUNK_SIZE` is now initialized once, before both the single-chunk check and the adaptive-split block. New regression test in `test-review-chunk-threshold.sh` exercises a 3-file PR whose diff exceeds the cap and asserts the chunker produces ≥ 2 chunks. | LADR-031 |
| 2026-07-30 | **Fix PR #86 opencode PATH regression.** `run-review.sh` now explicitly exports `PATH="$HOME/.opencode/bin:$PATH"` after the opencode installer runs. The installer writes to `$GITHUB_PATH` (a GitHub Actions special: it appends to PATH of SUBSEQUENT steps, not the current shell) and to `$HOME/.bashrc` (which `bash` does not re-source in the same process), so `command -v opencode` in the same step used to fail with `❌ opencode is not on PATH after install.` The post-install update also appends to `$GITHUB_PATH` for any later step (e.g. the Post Error Comment step) that needs to call `opencode`. New regression tests assert the post-install block is present in the source and that a stub `opencode` at `$HOME/.opencode/bin/opencode` becomes reachable after the export (40/40 tests pass). | — |
| 2026-07-30 | **Fix PR #86 GITHUB_TOKEN regression.** The reusable workflow's `Run review gate` step now forwards `GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}` in its `env:` block; the local-job caller does the same. Bare `run:` shells do not auto-inject GITHUB_TOKEN from the runner, so the entrypoint's preflight `❌ GITHUB_TOKEN is not set` was firing on every run after the refactor. New regression tests cover the preflight (fails unset, passes set, error message names GITHUB_TOKEN) and assert that both caller templates forward `secrets.GITHUB_TOKEN` on the `Run review gate` step's `env:` block (38/38 tests pass). | — |
| 2026-07-30 | Extracted the review gate's 32 inline workflow steps (28 `run:` blocks and 4 `uses:` invocations) into a single env-driven entrypoint `.agents/skills/ai-review-report/scripts/run-review.sh` (~580 lines). Driven entirely by env vars and `$GITHUB_EVENT_PATH` (no `inputs.*` / `steps.*.outputs` / expression interpolation), so the same code runs from both the reusable workflow and the new local-job packaging (`.docs/examples/code-review-local.yml`) that consumers under `allowed_actions: "selected"` can adopt when a cross-org reusable-workflow call is refused. The reusable workflow is now a thin wrapper (Checkout PR head → Fetch review tooling → `run-review.sh`); the local-job caller mirrors the wrapper without the `uses:` reference and pins the upstream `ref:` to a specific commit SHA. The new env-var contract is documented in the entrypoint header; new tests cover event-payload parsing (`pull_request` / `issue_comment` / `workflow_dispatch` + the `dependabot` and `draft` gates), env-var precedence, the `MAX_FILE_COUNT` validation, and the provider→key/URL mapping (all 33 pass). | — |
| 2026-07-30 | Added an overridable opencode.json source: new `opencode_config` input (`workflow_dispatch` + `workflow_call`) → `OPENCODE_REVIEW_REPORT_CONFIG` env var (input → Variable → committed default). `setup-opencode-config.sh` installs the custom file when set, resolved as a repo-relative path against `GITHUB_WORKSPACE` (absolute paths not honoured), else the committed `assets/opencode.json`. Lets reusable-workflow callers ship a customized provider block from their own repo; override must keep `{env:OPENCODE_*}` placeholders. | LADR-047 |
| 2026-07-25 | Capped AGENTS.md / documentation drift findings at 🟡 Medium — never High or Critical — regardless of chunk type. Updated SKILL.md Key Behavior ("AGENTS.md drift detection"), doc-only chunk prompt, and shared signal-to-noise guideline in `review-in-chunks.sh`. | LADR-046 |
| 2026-07-31 | Added `bypass_mandatory_context_file` workflow/caller input and `OPENCODE_REVIEW_REPORT_BYPASS_MANDATORY_CONTEXT_FILE` Variable. When truthy (`1`/`true`/`yes`/`on`, case-insensitive), bypasses both the mandatory context file loading (`MANDATORY_CONTEXT_FILES` injection) and the AGENTS.md validation gate (`validate-agents-md.sh`). The workflow input overrides the Variable when non-empty; the Variable is used when the input is null/empty. Default: empty (no bypass). Wired in the reusable workflow, the local-job packaging, and the caller template. | NO-TICKET |
| 2026-07-24 | "Test in the loop": `pipeline-ai-analyse.yml` no longer edits tests or the test framework by default. New `OPENCODE_ANALYSE_ALLOW_TEST_SELF_FIX` Variable (off by default) and deterministic reverter `ai-analyse/scripts/lib/filter-test-self-fix.sh` (+ `test-filter-test-self-fix.sh`) that restores any test/test-framework edit before commit unless the switch is truthy; the analyse prompt and PR summary reflect the setting. | LADR-045 |
| 2026-07-06 | Full review minimization now also hides previous `ai-analyse` auto-fix summary and limit-exceeded issue comments, using the same `minimizeComment(... OUTDATED)` pass as old AI reviews. Added an offline marker-regex regression test and documented that the analyse-comment markers are coupled to `pipeline-ai-analyse.yml`. | LADR-042 |
| 2026-07-05 | `pipeline-ai-analyse.yml` now detects staged `.github/workflows/**` edits and reports `push_skipped` unless a working workflow-scoped `OPENCODE_ANALYSE_GH_TOKEN` can push them, instead of retrying a guaranteed-to-fail `GITHUB_TOKEN` push. | LADR-042, LADR-043 |
| 2026-07-05 | Hardened downstream integration templates after smooth-llm-imposter PR #64 review: `pipeline-ai-analyse.yml` now ignores scratch/tooling paths, requires non-trivial analyse output, rebases auto-fix commits before push, caller docs forward `disable_claude_code` from the Variable fallback, and review prompts no longer count passing consistency checks as findings. | LADR-042 |
| 2026-07-05 | `pipeline-ai-analyse.yml` now selects analyse scope from a paginated unified descending timeline of PR reviews and issue comments, skips when the latest gate artifact is a skip-incremental comment, and counts incremental reports plus skip comments until the latest full review. Added `select-ai-analyse-artifact.sh` regression coverage. | LADR-042 |
| 2026-07-05 | Added `OPENCODE_ANALYSE_PROVIDER` for autonomous analyse runs with a custom `OPENCODE_ANALYSE_MODEL`. The analyse workflow now leaves `OPENCODE_ANALYSE_MODEL` empty unless explicitly configured, fails fast when a custom analyse model has no analyse provider, resolves analyse/review providers separately, passes provider-qualified model targets through `opencode-with-fallback.sh`, and keeps review-provider fallbacks valid for mixed-provider chains. Added resolver and fallback-target regression tests. | LADR-042 |
| 2026-07-05 | `pipeline-ai-analyse.yml` now fetches + rebases onto the latest PR head before pushing auto-fixes, and records a no-push summary when fetch/rebase cannot proceed cleanly instead of attempting a non-fast-forward update. | LADR-044 |
| 2026-07-05 | `pipeline-ai-analyse.yml` guard skips auto-fix when the triggering gate run did not post a fresh OpenCode review (blocking skip, no changes, too many files, all models failed), avoiding stale-finding re-processing. | LADR-042 |
| 2026-07-04 | `pipeline-ai-analyse.yml` now prefers the optional `OPENCODE_ANALYSE_GH_TOKEN` Secret for the auto-fix push and falls back to `GITHUB_TOKEN` only when the PAT is unset. | LADR-043 |
| 2026-07-04 | Fixed commit-message review trigger scope: the gate now checks only the current head commit for `/ai-review`, preventing old trigger commits from forcing every later `ai-analyse` follow-up commit through a full review. | LADR-042 |
| 2026-07-04 | Hardened privileged triggers: only `OWNER`/`MEMBER`/`COLLABORATOR` `/ai-review` comments can start a run; same-repo review PRs keep local-tooling precedence for both the review gate and `pipeline-ai-analyse.yml`, while fork PRs force pinned `.smooth-ai-review-tools`; tooling-fetch steps default to `v1` (floating major tag) instead of `main`; and `pipeline-ai-analyse.yml` disables persisted checkout credentials and pushes with an explicit masked-token remote. Caller template updated with the same trusted-comment guard and the same name-coupling required by `workflow_run`. | LADR-043 |
| 2026-07-03 | Added a `workflow_dispatch` manual entry path to `pipeline-ai-analyse.yml` (`pr_number` required, `max_incremental` optional cap override). Guard `if:` now also allows the dispatch event, derives the PR from the input, and the concurrency group keys on `inputs.pr_number` too. | LADR-042 |
| 2026-07-03 | Follow-up from PR #57 review: normalized `OPENCODE_REVIEW_REPORT_DISABLE_AGENTS_MD_CHECK` matching to be case-insensitive, documented that behavior, aligned the caller-template description with the main workflow, and added a focused validator disable test. | PR #57 review 4626304325 |
| 2026-07-03 | Added `OPENCODE_REVIEW_REPORT_DISABLE_AGENTS_MD_CHECK` and `disable_agents_md_check` workflow/caller inputs to globally bypass the full-review AGENTS.md / README.md / SKILL.md documentation validation when explicitly set. Default remains enabled. | NO-TICKET |
| 2026-06-30 | Added autonomous `ai-analyse` low/medium auto-fix loop: new `pipeline-ai-analyse.yml` `workflow_run` guard/analyse workflow, new `.agents/skills/ai-analyse` skill, edit-only `analyse` opencode agent, scoped review-section extractor, parameterized `opencode-with-fallback.sh`, package/plugin registration, docs, and loop cap variables `OPENCODE_ANALYSE_MODEL` / `OPENCODE_ANALYSE_MAX_INCREMENTAL`. | LADR-042 |
| 2026-06-30 | **Cross-org reusable-workflow fix.** Declared all seven `OPENCODE_*_API_KEY` secrets under `on.workflow_call.secrets` (`required: false`) so out-of-org callers can pass them by explicit mapping — GitHub honors `secrets: inherit` only same-org/enterprise, so a cross-org caller using `inherit` previously startup-failed ("workflow file issue"). Caller template `code-review-caller.yml` and README now map keys explicitly (works same-org **and** cross-org); `inherit` documented as the same-org-only shortcut. SKILL.md/AGENTS.md LADR-037 amended. | LADR-037 |
| 2026-06-29 | Disabled opencode session sharing: added `"share": "disabled"` to `opencode.json` (LADR-041). Prevents PR diff content from being auto-shared via opencode's share feature. `setup-opencode-config.sh` `is_ours` predicate updated to allow the `share` key at the top level so self-heal still triggers. | LADR-041 |
| 2026-06-26 | Added **Anthropic** as the seventh provider (`OPENCODE_REVIEW_REPORT_PROVIDER=ANTHROPIC` → provider-id `anthropic`, `@ai-sdk/anthropic`, key `OPENCODE_ANTHROPIC_API_KEY`). Fixed public base `https://api.anthropic.com` hardcoded in `opencode.json` (no URL Variable, not in `_inject_base_urls` — like OpenCode Go/OpenRouter). Three Claude models (`claude-opus-4-8`, `claude-sonnet-4-6`, `claude-haiku-4-5`). Wired through resolver (incl. `claude*` model-family fail-fast), setup-config `is_ours` set, gate `env:`/PROVIDER_ID map/bootstrap case + three `Anthropic Claude …` `model_preset` options, caller template, local-review harvest + help text, README Step 3 matrix + providers section + Secrets/Variables tables + env-var table, SKILL.md env-var table + model-family fail-fast + opencode-transport LADR ref, root `AGENTS.md` Non-Negotiables (two exceptions → three) + System Context provider list. LADR-040. | LADR-040 |
| 2026-06-14 | Added **OpenRouter** as the sixth provider (`OPENCODE_REVIEW_REPORT_PROVIDER=OPEN_ROUTER` → provider-id `openrouter`, `@openrouter/ai-sdk-provider`, key `OPENCODE_OPENROUTER_API_KEY`). Fixed public base `https://openrouter.ai/api/v1` hardcoded in `opencode.json` (no URL Variable, not in `_inject_base_urls` — like OpenCode Go). 13 vendor-prefixed models (DeepSeek/Qwen/GLM/MiniMax/MiMo/Hunyuan/Step/Nemotron, incl. the go-anthropic equivalents minimax/minimax-m2.7 and qwen/qwen3.6-max-preview; no Anthropic/OpenAI). Wired through resolver, setup-config `is_ours` set, gate `env:`/PROVIDER_ID map/bootstrap case + four `OpenRouter …` `model_preset` options, caller template, eval harness, local-review/eval harvest, aggregate display name, README, SKILL.md, both AGENTS.md. LADR-039; root `AGENTS.md` "sole exception" → two exceptions. | NO-TICKET |
| 2026-06-13 | Added skill `AGENTS.md` "Self-Review — Known Intentional Patterns (do NOT flag)" registry so self-review false positives (downstream `PR #<n>` provenance refs, `cmd_threads` EXIT trap, `MACHINE_READABLE_ACTION` `tail -1` parse, compressed one-line changelog rows) are durably skipped on any machine, not just via per-PR `/ai-review skip`; root `AGENTS.md` changelog pointer notes LADRs are not re-inlined. From PR #44 review 4491689018. | NO-TICKET |
| 2026-06-13 | Self-review remediation from PR #149 review: validator SHA resolution, provider-agnostic footer/parsing, portable chunk scripts, Copilot pagination, guidance drift fixes (switches, LADR-019, root-doc path). | NO-TICKET |
| 2026-06-11 | Platform-semantics hallucination guard: prompt edits for claim verification / `[SPECULATIVE]` downgrade; DR-015 rule + fixture; LADR-015 extended. | NO-TICKET |
| 2026-06-11 | Fence balancing for posted review bodies (`lib/balance-fences.sh`); GFM parity fix for nested code fences. | NO-TICKET |
| 2026-06-11 | Root `skills` symlink removed; Claude Code plugin loads via manifest `"skills"` field. | NO-TICKET |
| 2026-06-11 | npm/opencode plugin channel (LADR-038): `@generic-automation-and-it/smooth-ai-review` on GitHub Packages. | LADR-038 |
| 2026-06-11 | README install runbook updated for reusable-workflow + plugin channels. | — |
| 2026-06-10 | Hard chunk-prompt bounding + coverage-gaps-never-block + visible fail-closed banner (LADR-035/036); `test-chunk-prompt-budget.sh`. | LADR-035/036 |
| 2026-06-10 | Reusable-workflow channel via `$REVIEW_SKILL_DIR` indirection (LADR-037); caller template. | LADR-037 |
| 2026-06-10 | Claude Code plugin `smooth-ai-review` (all three skills). | — |
| 2026-06-09 | Per-provider gateway `baseURL` injected at install time (LADR-034). | LADR-034 |
| 2026-06-09 | `review` agent `bash` denied; `OPENCODE_GATEWAY_API_KEY` removed from `$GITHUB_ENV`; `actions/cache` SHA-pinned. | LADR-029 |
| 2026-06-09 | SKILL.md trimmed to runtime contract; editor context confirmed in AGENTS.md. | — |
| 2026-06-07 | Chunk-failure signal → out-of-band flag files (LADR-031). | LADR-031 |
| 2026-06-07 | Holistic aggregation runs for every PR incl. single-chunk (LADR-030, supersedes LADR-017). | LADR-030 |
| 2026-06-07 | Chunk review on locked-down `review` agent (LADR-029); empty-output fallback. | LADR-029 |
| 2026-06-07 | Max-file-count gate + `OPENCODE_*` → `OPENCODE_REVIEW_REPORT_*` env rename (LADR-032). | LADR-032 |
| 2026-06-07 | Opt-in LLM eval harness (LADR-033): precision vs DR-001…014 + recall; triage archive; post-merge canary. | LADR-033 |
| 2026-06-07 | `model_preset` dispatch dropdown + `minimax-m3` model. | — |
| 2026-06-07 | Eval harness env-var rename to `OPENCODE_REVIEW_REPORT_*`. | #6 |
| 2026-06-06 | Health check → opencode server `/global/health` (LADR-028); per-provider probes removed. | LADR-028 |
| 2026-06-06 | OpenCode Go as two providers split by SDK surface (LADR-027). | LADR-027 |
| 2026-06-06 | Provider key `litellm-gemini` → `gemini`; `baseURL` placeholders removed. | BNKI-001 |
| 2026-06-06 | PR #1 review fixes: fail-closed on empty output, fork handling, `is_ours` hardening. | BNKI-001 |
| 2026-06-06 | `/gemini-review` retired; `/ai-review` sole trigger. | BNKI-001 |
| 2026-06-06 | Env-selected provider GEMINI/COPILOT/OPENAI (LADR-026). | LADR-026 |
| 2026-06-01 | Allow `external_directory` reads (LADR-025); fixes failing chunks. | BNKI-001 |
| 2026-05-29 | Fix local deadlock on unreachable gateway; process-group `timeout` shim. | BNKI-001 |
| 2026-05-29 | Zero-setup local runs: credential harvest from shell rc files. | BNKI-001 |
| 2026-05-29 | Two-tier review chain + explicit orchestrator model (LADR-002/022). | BNKI-001 |
| 2026-05-29 | De-brand + env-var rename: skill dir, credential vars, model chain. | BNKI-001 |
| 2026-05-28 | Skill format migration to Agent Skills standard (`SKILL.md`, `assets/`, `references/`). | BNKI-001 |
| 2026-05-28 | Migration: `@google/gemini-cli` → `opencode` CLI (LADR-023). | LADR-023 |
| 2026-05-26 | Draft-PR behaviour documented as Key Behavior. | BNKI-001 |
| 2026-05-26 | LADR-022: non-analytical calls default to `auto` (later → explicit orchestrator). | BNKI-001 |
| 2026-05-25 | LADR-021: all-models-failed posts request-changes, exits green. | BNKI-001 |
| 2026-05-20 | Non-Negotiable: diff+LADR beats PR-body intent claims (DR-014). | BNKI-1190 |
| 2026-05-12 | LADR-017/018/019/020: aggregation optimisations (single-chunk short-circuit, Flash model, no read_file, skip sections ≤2 chunks). | BNKI-001 |
| 2026-04-24 | Fix Gemini CLI "trusted folders" overriding `--yolo`. | BNKI-001 |
| 2026-04-21 | Non-Negotiable: verify refactor suggestions not already applied. | BNKI-1066 |
| 2026-04-21 | Key Behavior: mode-aware regression analysis. | BNKI-1066 |
| 2026-04-16 | Removed gh install (pre-installed on self-hosted). | BNKI-001 |
| 2026-04-14 | Removed `pull_request_target: review_requested` (duplicate runs). | BNKI-001 |
| 2026-04-14 | Runner → `self-hosted-high-memory` (private-network proxy). | BNKI-001 |
| 2026-04-08 | Fix `---: command not found` in `aggregate-reviews.sh`. | BNKI-001 |
| 2026-04-07 | Release branch sync review mode (LADR-016). | BNKI-001 |
| 2026-04-07 | Model fallback chain → three tiers; removed non-existent models. | BNKI-001 |
| 2026-04-02 | Model fallback chain → five tiers (added Flash variants). | BNKI-001 |
| 2026-03-25 | Key Behavior: EF Core expression tree navigation (no NRE in `.Select()`). | BNKI-780 |
| 2026-03-24 | Clean Code principles added to review focus. | BNKI-001 |
| 2026-03-24 | RTK token optimization proxy (LADR-014; later superseded by LADR-023). | BNKI-001 |
| 2026-03-23 | LADR-015: strengthened Critical/High verification + diff integrity. | BNKI-970 |
| 2026-03-11 | Migration/schema chunk detection (LADR-013). | BNKI-001 |
| 2026-03-11 | Fix `read_file: command not found` (escaped backticks); Windows junction fallback. | BNKI-001 |
| 2026-03-11 | Removed `--open` flag; corrected output path. | BNKI-001 |
| 2026-03-10 | `local-review.sh` wrapper for local CLI execution. | BNKI-001 |
| 2026-03-10 | LADR-012: confidence tagging (`[VERIFIED]`/`[SPECULATIVE]`). | BNKI-001 |
| 2026-03-02 | Doc-only chunk detection; Skip Areas enforcement. | BNKI-001 |
| 2026-02-28 | AGENTS.md drift detection as HIGH priority. | BNKI-001 |
| 2026-02-28 | Remediation pass: template alignment. | BNKI-001 |
| 2026-02-28 | Template alignment: 2558→120 lines (~95% reduction). | BNKI-001 |
| 2026-02-21 | #115: Chunk review focus areas optimisation. | |
| 2026-02-17 | #113-#114: Adaptive chunk splitting (LADR-010), semantic grouping (LADR-011). | |
| 2026-02-06 | #111-#112: Fix Gemini miscounting "None found", fallback review action logic. | |
| 2026-01-16 | #108-#110: Rename step IDs, fix blocking review detection. | |
| 2026-01-05 | #104-#107: Blocking review detection, commit message triggers. | |
| 2026-01-02 | #103: Selective concurrency (LADR-009), supersede LADR-008. | |
| 2025-12-22 | #98-#101: Model display name, quota detection, template exclusion, minimize race. | |
| 2025-11-28 | #87-#97: Two-part aggregation, test pairing, file access, NFR context. | |
| 2025-10-28 | Initial chunked review implementation (LADR-001). | |
